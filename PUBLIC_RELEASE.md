# 公开前检查清单

当前仓库用于私人实验，GitHub 可见性保持为 private。本清单用于将来评估公开方式，不代表已经授权公开、再分发题面或替用户选择许可证。

## 推荐的公开方式

优先新建一个干净的公开仓库，从当前私有仓库导出允许公开的自研工具、解答、方法记录和脱敏统计。这样可以保留私人实验仓库的完整追溯能力，也能避免历史对象随可见性切换一起公开。

如果一定要把当前仓库改为 public，需要先重写 Git 历史，再检查重写后的所有对象。仅在最新提交删除文件没有作用，旧题面、旧日志和其他历史内容仍能从 Git 历史访问。

## 题面与第三方内容

- `archive/problems/` 保存了同步到的题面和模板原始归档。
- `problems/*/problem.md` 保存了开始作答后生成的题面副本。
- 部分题面引用 LeetCode 托管的图片链接。

这些内容不属于本项目原创代码。公开前应核对 LeetCode 的服务条款与内容许可。推荐的公开副本只保留题号、slug、难度、原题链接和必要的接口元数据，不再分发完整题面与题图。

## 实验日志与账号信息

`data/attempts.jsonl` 约 12 MB，包含提交 ID、时间戳、题目、Profile、判题结果和代码哈希。它不应包含 Cookie，但会暴露账号活动时间线和详细实验行为。

公开前在以下方案中选择一种。

1. 只公开 `stats/summary.md` 与经过检查的聚合统计。
2. 生成脱敏逐题结果，删除提交 ID、精确时间戳和不必要的内部字段。
3. 确认愿意公开完整时间线后保留原始日志。

LeetCode 个人页链接同样属于主动披露项。公开仓库前再次确认账号名、提交记录与排行榜信息都可以长期公开。

## 凭证与历史扫描

- `.secrets/`、`.runtime/` 和 `.ai/identity.env` 已被 Git 忽略。
- pre-commit hook 会扫描新增的 LeetCode 会话、CSRF token、私钥和常见 API key。
- 当前树与当前全部 Git 历史已按上述模式检查，未发现真实凭证或私钥。
- 模式扫描不能证明绝对没有秘密。公开前仍应使用专门的历史扫描工具复查，并重新登录 LeetCode 使旧会话失效。

不要在检查输出中打印 Cookie、token 或本地配置内容。

## 许可证

当前仓库没有 LICENSE 文件。公开前需要为本项目原创的 CLI、脚本、测试、解答和文档选择许可证。许可证只能覆盖有权授权的原创内容，不能替 LeetCode 题面或其他第三方材料授予许可。

## 公开前最终核对

- [ ] 选择干净公开仓库或完成历史重写
- [ ] 移除完整题面、题图引用和无权再分发的第三方内容
- [ ] 决定原始事件日志、提交 ID、时间戳和账号链接的公开范围
- [ ] 对最终公开历史执行凭证扫描
- [ ] 轮换 LeetCode 会话
- [ ] 为原创内容选择许可证并写清第三方内容例外
- [ ] 运行全部测试、audit 和 stats
- [ ] 人工检查 README、统计数字和公开文章链接
- [ ] 确认 GitHub 可见性变更得到明确授权
