# 3331. 修改后子树的大小

## 思路

## 算法

先按原树做 DFS。对每个字符维护当前根到节点路径上同字符节点的栈，栈顶就是该节点修改后的最近同字符祖先，因此可一次 DFS 同时确定所有新父节点。新父节点确定后，节点编号保证父节点编号更小；从后向前把每个节点的子树大小累加给新父节点即可。

## 复杂度

时间复杂度为 `O(n)`，空间复杂度为 `O(n)`。

## 边界

根节点没有祖先；没有同字符祖先时保留原父节点。所有改边是同时生效，DFS 始终在原树上寻找祖先。
