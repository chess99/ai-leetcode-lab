# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

按层广度优先遍历。每层开始时队首就是该层最左节点，持续覆盖答案；遍历结束时留下最深层的最左值。

## 复杂度

时间 `O(n)`，队列最坏空间 `O(n)`。

## 边界条件与本地验证

题目保证根节点非空；单节点直接返回自身。对 800 棵随机树与独立按层遍历所得的最后一层首值对拍通过。
