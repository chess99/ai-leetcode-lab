# 3362. 零数组变换 III

## 算法

目标是保留最少查询。从左到右处理位置，把左端点已到达的查询放入按右端点降序的堆；若当前保留查询覆盖数不足，就优先选择右端点最远的查询，能最大化对后续位置的帮助。差分数组记录已选查询的失效位置。

## 复杂度

排序和堆操作的时间复杂度为 `O((n+q) log q)`，空间复杂度 `O(n+q)`。

## 边界

过期查询不能再选；任一位置无法达到所需覆盖次数时返回 `-1`；答案是总查询数减去最少保留数。
