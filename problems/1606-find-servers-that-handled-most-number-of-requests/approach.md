# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
忙碌服务器用结束时间堆；可用服务器堆存相对当前请求编号平移后的键，从而优先选到循环顺序中第一个服务器。释放时按当前编号重新编码。

## 复杂度
每个请求至多一次入出堆，时间 `O(n log k)`，空间 `O(k)`。

## 边界条件与本地验证
没有可用服务器时丢弃请求；结束时间等于到达时间时服务器已可用。
