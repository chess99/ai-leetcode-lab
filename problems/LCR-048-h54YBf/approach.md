# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

采用先序序列化，空孩子写成 `#`，因此结构无歧义。序列化用显式栈；反序列化用栈记录每个节点下一次要填左孩子还是右孩子，避免深链递归溢出。

## 复杂度

时间和输出空间均为 `O(n)`，辅助栈 `O(h)`。

## 边界条件与本地验证

空树编码为 `#`；支持负数。本地构造随机树，验证反序列化后的再次序列化完全一致。
