# 解题记录

## 思路

枚举每个起点和长度不超过 r 的终点，记录长度至少 l 的最小正和。

固定起点后向右扩展终点，并增量维护当前子数组和。当长度进入 `[l, r]` 且和大于零时，用它更新答案。若遍历结束仍没有合法正和，则返回 `-1`。

## 复杂度

每个起点最多向右扩展 `r` 个元素，时间复杂度为 `O(nr)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

和等于零不属于正和；不存在合法子数组时返回 `-1`；`l = r` 时只检查固定长度窗口。使用题目三个样例，并对短数组枚举所有合法区间作交叉验证。
