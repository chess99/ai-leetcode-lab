# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

统计整数十进制表示中每个出现数字的频次，再按“频次升序、数字值升序”选择最小项。

## 复杂度

设整数位数为 `d`。时间复杂度为 `O(d)`，额外空间复杂度为 `O(1)`（最多 10 个数字）。

## 边界条件与本地验证

多个数字具有相同最小频次时，排序键保证选择数字值更小的一个。
