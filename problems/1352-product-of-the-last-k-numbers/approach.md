# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
维护最近一个零之后的前缀积。查询若跨过零返回 0，否则以两前缀积相除。
## 复杂度
每次操作 `O(1)`，空间 `O(N)`。
## 边界条件与本地验证
覆盖零后查询、跨零查询和连续乘积。对 500 个实例各执行 1000 次随机添加/查询，与保存完整数字列表并直接求积的模型对照。
