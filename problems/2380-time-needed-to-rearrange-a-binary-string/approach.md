# 2380. 二进制字符串重新安排顺序需要的时间

## 算法

扫描字符串并统计此前 `0` 数。遇到 `1` 且此前有零时，以 `max(seconds + 1, zeros)` 更新它完成越过所有此前零的时刻。

## 正确性说明

该 `1` 至少需越过此前全部零，也不能早于前一需要移动的 `1` 的下一秒完成，递推式恰为这两个下界的最大值。最后完成移动的时刻就是总秒数。

## 复杂度分析

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界情况

- 没有 `01` 时答案为 0。
- 全零或全一无需交换。
- 多个 `1` 的移动会相互等待。
