# 解题记录

## 思路

掩码 `0x55555555` 保留所有偶数位，左移一位；掩码 `0xAAAAAAAA` 保留所有奇数位，右移一位。将两部分按位或即可完成每对位的交换。

## 复杂度

时间复杂度和额外空间复杂度均为 `O(1)`。

## 边界条件与本地验证

全为 0 或某对位相同的数交换后保持不变。已对 `2 (0b10)` 断言得到 `1 (0b01)`，并对 `3` 断言保持为 3。
