# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按值统计频次。频次为 1 无法删除；其他频次优先每次删除 3 个，只有余数为 1 时将一组 3 改成两组 2，最少操作数统一为 `ceil(count / 3)`。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

频次 2、3 分别只需一次操作；频次 4 需要两次。
