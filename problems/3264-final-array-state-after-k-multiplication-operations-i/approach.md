# 解题记录

## 思路

按题意模拟 k 次操作：每次先求当前最小值，再用 `index` 找到它第一次出现的位置，从而满足并列时选择最小下标的要求，随后将该元素乘以 multiplier。

## 复杂度

设数组长度为 n，时间复杂度 O(kn)，额外空间复杂度 O(1)。

## 边界条件与本地验证

最小值重复出现时必须修改最左者；`multiplier = 1` 时每次仍会选同一最左最小值但数组不变。已用独立逐步模拟核对随机小数组。
