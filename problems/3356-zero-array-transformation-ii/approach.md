# 3356. 零数组变换 II

## 算法

使用前 `k` 个查询可否归零具有单调性。二分最小 `k`，每次用差分数组累加各查询对每个位置提供的最大可减值，检查是否均不少于对应需求。

## 复杂度

检查一次为 `O(n+q)`，总时间复杂度 `O((n+q) log q)`，空间复杂度 `O(n)`。

## 边界

若全部查询仍不足则返回 `-1`；全零数组可在二分中得到 `k=0`。
