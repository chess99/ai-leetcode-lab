# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

只需确定 `arr1`，因为 `arr2[i] = nums[i] - arr1[i]`。设当前 `arr1[i]=x`、前一项为 `y`，两个单调条件给出 `y <= x` 且 `nums[i-1]-y >= nums[i]-x`，即 `y <= x + nums[i-1]-nums[i]`。

合法上一值形成从 0 开始的连续区间。为上一层 DP 建前缀和，即可在常数时间完成每个状态转移，适应本题更大的数组长度和值域。

## 复杂度

令 `M=max(nums)`，时间复杂度为 `O(nM)`，空间复杂度为 `O(M)`。

## 边界条件与本地验证

当前值下降或上升时使用同一上限公式；上限为负的状态保持 0。每层都取模。本地枚举随机小数组的全部非负拆分，与 DP 结果对拍一致。
