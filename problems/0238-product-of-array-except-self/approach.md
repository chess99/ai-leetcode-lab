# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

第一遍写入每个位置左侧乘积，第二遍从右向左维护右侧乘积并乘入结果，因此无需除法。

## 复杂度

时间 `O(n)`，除输出数组外额外空间 `O(1)`。

## 边界条件与本地验证

- 零由前后缀乘积自然处理。

本地断言示例和包含一个、两个零的数组，并通过 `py_compile`。
