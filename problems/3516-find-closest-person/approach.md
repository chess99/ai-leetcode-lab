# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

两人速度相同，到达时间只由到第三人位置的绝对距离决定。比较 `abs(x-z)` 和 `abs(y-z)`，距离更小者先到，相等则同时到。

## 复杂度

时间复杂度和额外空间复杂度均为 `O(1)`。

## 边界条件与本地验证

两人位于第三人两侧且距离相等时返回 `0`。
