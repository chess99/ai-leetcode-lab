# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

同时维护模 1..9 的状态：counts[d][r] 是以当前位置结尾、模 d 余数为 r 的子串数。加入新数字后把旧余数乘10并加当前位，同时加入单字符子串；若末位非0，就累加对应模数的余数0计数。

## 复杂度

时间复杂度 O(45n)，空间复杂度 O(45)。

## 边界条件与本地验证

末位0不能作除数但仍参与后续子串。随机短数字串直接转整数枚举全部子串，对拍一致。
