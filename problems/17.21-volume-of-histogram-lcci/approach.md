# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

双指针从两端向中间移动，维护左右最高柱。较低一侧的水位已由该侧最高柱确定，因为另一侧至少不低于它；累加当前最高柱减柱高后移动该侧指针。

## 复杂度

时间 `O(N)`，空间 `O(1)`。

## 边界条件与本地验证

空数组、单调数组和平台均返回 0。官方样例得到 6；随机柱高与逐位置左右最大值 oracle 对拍 5000 组通过。
