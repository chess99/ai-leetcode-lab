# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
任意子数组的按位与不大于其中任一元素，最大可能值就是数组最大值。要使按位与仍为该值，所有元素都必须等于最大值，因此统计最大值连续段的最长长度。
## 复杂度
时间 `O(n)`，额外空间 `O(1)`。
## 边界条件与本地验证
- 最大值只出现一次时答案为 1。
- 已验证样例 `[1,2,3,3,2,2]` 返回 2，`[1,2,3,4]` 返回 1，全相同数组返回其长度。
