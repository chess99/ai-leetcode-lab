# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从低位到高位模拟加一产生的进位。当前位加进位为 1 时需先加一再除二（两步），否则仅除二（一步）。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

- 输入 `1` 直接返回 0。
- 本地验证了题目示例和连续进位。
