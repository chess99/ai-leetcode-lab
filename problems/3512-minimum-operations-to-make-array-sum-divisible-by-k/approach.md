# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

每次操作使总和减少 1。若总和除以 `k` 的余数为 `r`，恰好减少 `r` 次即可到达下一个更小的 `k` 的倍数，且任何更少操作都无法消除余数。

## 复杂度

设数组长度为 `n`。求和时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

总和本身能被整除时余数为 `0`，无需操作；可以将元素减到 `0`，所以始终可完成。
