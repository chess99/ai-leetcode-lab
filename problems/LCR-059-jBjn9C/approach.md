# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护一个最多保留 `k` 个元素的小根堆，其中保存数据流中最大的 `k` 个数。堆顶正是第 `k` 大元素；加入新值后若超长便弹出堆顶。

## 复杂度

初始化时间复杂度为 `O(n log k)`，每次 `add` 为 `O(log k)`，额外空间复杂度为 `O(k)`。

## 边界条件与本地验证

重复值同样计入排名；本地断言覆盖了题目给出的连续加入示例。
