# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护普通栈和同步的最小值栈。每次压入元素时记录截至该位置的最小值，弹出时两栈同步弹出，因此可常数时间取得最小值。

## 复杂度

所有操作时间复杂度均为 `O(1)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

重复最小值会被分别记录；本地断言覆盖了题目操作序列。
