# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护处理前缀后“不选当前”和“选当前”的最大收益。若当前与前一房颜色相同，选当前只能接前一房未选状态；颜色不同则没有相邻冲突，可接任一最优状态。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

金额为正但仍需保留跳过状态；首房无前驱。保留隐藏变量，覆盖三组示例并用子集枚举对拍。
