# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将数字转为字符串，枚举每个长度，同时取该长度的前缀和后缀并用试除法判定质数；全部通过才是完全质数。

## 复杂度

数字位数至多 10，每次质数判定 `O(sqrt(num))`，总时间 `O(d·sqrt(num))`，空间 `O(d)`。

## 边界条件与本地验证

小于 2、包含导致某个前后缀合数的数字均失败；单个质数数字成功。验证 `23`、`39`、`7`。
