# 3377. 使两个整数相等的数位操作

## 思路

## 算法

预筛 10000 内的质数。将每个非质数的同位数整数视为图节点，改变任意一位 `±1` 是一条边，进入新节点的边权为该节点数值；初始成本包含 `n`。所有边权非负，使用 Dijkstra。

## 复杂度

节点数最多 9000、每个节点至多 8 个邻居，时间复杂度 `O(V log V)`，空间复杂度 `O(V)`。

## 边界

起点或终点为质数直接无解；最高位不能减为 0，否则会改变整数位数；成本包含变换过程的起点与终点。
