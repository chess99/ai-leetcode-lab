# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

分别按行优先和列优先展平网格，用 KMP 找出所有模式出现位置。对每次匹配在差分数组上标记覆盖区间，前缀和恢复水平和垂直覆盖，最后统计同时覆盖的格子。

## 复杂度

时间 `O(mn + |pattern|)`，空间 `O(mn + |pattern|)`。

## 边界条件与本地验证

匹配可跨行或跨列但不循环回首；重叠匹配通过差分自然合并。覆盖单格和跨边界匹配。
