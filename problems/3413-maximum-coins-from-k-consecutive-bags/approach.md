# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

窗口最优端点可贴在某个区间的左端或右端。对左端排序后滑动长度为 `k` 的窗口，完整纳入的区间维护总和，末尾相交区间只计入覆盖部分；再将坐标取反重复一次以覆盖右端贴齐情形。

## 复杂度

排序后双指针扫描，时间 `O(n log n)`，额外空间 `O(n)`。

## 边界条件与本地验证

验证窗口完全落在一区间、跨越空隙、只覆盖区间前缀，以及最优窗口贴右端。
