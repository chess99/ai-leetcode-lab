# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:51Z

## 算法

二分总时间 `t`。无人机 `i` 可工作的小时数是 `t-floor(t/r[i])`；至少一架可工作的小时数是 `t-floor(t/lcm(r1,r2))`。分别满足两架需求，并保证可分配小时总数不少于总任务数，即存在合法调度。

## 复杂度

每次检查 `O(1)`，总时间 `O(log answer)`，空间 `O(1)`。

## 边界

两架同时充电的小时不能用于任何送货；两架都可用的小时只能分配给其中一架。
