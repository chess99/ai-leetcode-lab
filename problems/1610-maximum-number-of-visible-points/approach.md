# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
同位置点始终可见并单独计数。其余点转为极角，排序后复制一份加 `2π`，用滑动窗口寻找角宽内最多方向。

## 复杂度
排序时间 `O(n log n)`，滑窗 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证
角度区间含端点；处理跨越 `-π/π` 的窗口；同位置点没有方向。
