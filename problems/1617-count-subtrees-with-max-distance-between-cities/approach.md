# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
枚举城市子集。先在子集诱导图中 BFS 判断连通并找一个最远点，再从该点 BFS 得到树的直径，按直径计数。

## 复杂度
时间 `O(2^n·n)`，空间 `O(n)`。

## 边界条件与本地验证
单节点子集直径为零不计入答案；原图是树，连通诱导子图的双 BFS 可求直径。
