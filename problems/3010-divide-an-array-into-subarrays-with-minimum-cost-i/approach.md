# 解题记录

## 思路

首段必须包含首元素，其余两段开头选择最小两个元素。

三个连续非空子数组的代价，是它们各自首元素之和。第一段的首元素固定为 `nums[0]`；在剩余位置中选择两个下标作为第二、三段起点，任何两个递增下标都能形成合法划分。因此选择 `nums[1:]` 中最小的两个值即可。

## 复杂度

当前实现对后缀排序，时间复杂度为 `O(n log n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

数组长度最小为 `3`，后缀一定能选出两个起点；值可重复，不影响选择。使用题目样例，并对短数组枚举两个切分点计算最小代价进行交叉验证。
