# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

逐字符扫描并维护是否处于块注释中。普通状态识别行注释与块注释起点，块状态只寻找终点；块注释跨行时把剩余代码拼接到同一输出行。

## 复杂度

时间 `O(总字符数)`，输出外临时空间 `O(单行长度)`。

## 边界条件与本地验证

- 行注释后内容直接忽略。
- 空输出行不加入结果。
- 本地执行 `py_compile` 并断言跨行块注释和混合注释。
