# 解题记录

## 思路

若数组已经全部相等，答案为 0。否则选择整个数组执行一次操作，所有位置都会被替换为全数组的按位与结果，从而立刻相等；因为初始状态不相等，至少需要一次操作，所以答案恰为 1。

## 复杂度

检查所有元素是否相等需要 O(n) 时间，集合最多使用 O(n) 额外空间。

## 边界条件与本地验证

单元素数组天然相等；多个相同元素返回 0，任意存在差异的数组返回 1。对小数组显式模拟所有子数组操作验证了最短步数。
