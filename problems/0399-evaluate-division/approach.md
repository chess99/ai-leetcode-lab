# 解题记录
- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium
## 思路
将等式建成带权双向图；每个查询 BFS，沿路径累乘权重，到达终点即答案。
## 复杂度
- 构图 O(E)；每次查询 O(V+E)，空间 O(V+E)。
## 边界条件与本地验证
- 未知或不可达变量返回 `-1.0`，同一已知变量返回 1。验证 `a/b=2,b/c=3`：`a/c -> 6.0`、`a/e -> -1.0`。
