# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

枚举子数组，维护其 GCD 和所有数中最小的二因子指数出现次数。只有将最小指数的所有元素都翻倍，GCD 才会翻倍；所需操作数不超过 `k` 时比较两倍 GCD 的分数。

## 复杂度

时间 `O(n^2)`，空间 `O(1)`。

## 边界条件与本地验证

覆盖单元素、最小二因子并列及操作不足。三个题面样例通过。
