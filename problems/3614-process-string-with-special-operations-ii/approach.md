# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

先记录处理每个字符后的结果长度（长度封顶）。若 `k` 合法则反向回溯：复制映射回前半段，翻转将下标镜像，普通字母命中其追加位置便返回。无需构造可能极长的字符串。

## 复杂度

时间 `O(|s|)`，空间 `O(|s|)`。

## 边界条件与本地验证

覆盖空串、删除、嵌套复制和多次翻转。5000 组短随机串同显式模拟一致。
