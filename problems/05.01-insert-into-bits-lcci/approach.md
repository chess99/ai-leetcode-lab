# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

构造覆盖 `i` 到 `j` 位的掩码，先清零 `N` 在该区间的比特，再将左移 `i` 位后的 `M` 用按位或合并。

## 复杂度

时间复杂度为 `O(1)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

从最低位开始插入和全部覆盖指定区间均可处理；本地断言覆盖两个题目样例。
