# 解题记录

## 思路

## 算法、正确性与复杂度

二分完成时间 `T`。在 `T` 内，耗时为 `x` 的车可完成 `T // x` 趟；总趟数不少于目标则 `T` 可行。时间越大可完成趟数单调不减，二分第一个可行时间。每次判定 `O(n)`，总时间 `O(n log(min(time)*totalTrips))`，额外空间 `O(1)`。

## 边界与验证

- 单辆车时答案为其耗时乘目标趟数。
- 上界由最快车独自完成全部趟数保证可行。
- 用题面样例、单车和多车同耗时断言验证。
