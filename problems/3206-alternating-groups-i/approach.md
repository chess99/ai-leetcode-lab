# 解题记录

## 思路

把每个位置视为长度为 3 的环形窗口起点，通过取模访问后两个位置。窗口中前两个颜色不同、后两个颜色也不同时，这三个连续砖块构成一个交替组。

## 复杂度

设颜色数组长度为 n，时间复杂度 O(n)，额外空间复杂度 O(1)。

## 边界条件与本地验证

取模访问能正确覆盖跨越数组末尾和开头的两个窗口。验证了全同色、完全交替和部分交替数组，并与直接构造环形窗口的参考实现核对。
