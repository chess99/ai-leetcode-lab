# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举长度为 `m + 1` 的每个子数组起点，把相邻数的比较结果规范为 -1、0、1，并逐项与模式比较；全部相同即计数。

## 复杂度

时间 `O(nm)`，额外空间 `O(1)`；本题 `n <= 100`。

## 边界条件与本地验证

相等元素的比较结果必须为 0，不能被归到任一严格不等关系。
