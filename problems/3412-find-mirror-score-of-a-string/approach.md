# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

为每个字母维护尚未匹配的位置栈。扫描字符时若其镜像字母的栈非空，弹出最近位置并累加距离；否则把当前位置压入本字母栈。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

验证没有可配对字符、多个同类候选必须取最近者，以及字符自始至终未配对。
