# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

迭代 DFS 建立每个节点的父指针，直到找到 `p` 和 `q`。把 `p` 到根的祖先放入集合，再从 `q` 向根移动，首次命中的祖先即最近公共祖先。

## 复杂度

时间和空间均为 `O(n)`。

## 边界条件与本地验证

- 一个目标节点是另一个祖先时直接返回祖先。
- 不依赖递归，适用于深树。

本地构造示例树，断言 LCA 为根及内部祖先，并通过 `py_compile`。
