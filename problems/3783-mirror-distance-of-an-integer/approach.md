# 解题记录

## 思路

反转十进制字符串并取与原数的绝对差。

将 `n` 转成十进制字符串后反转，再转回整数得到 `reverse(n)`。转换为整数会自然丢弃反转后的前导零，最后返回它与原数之差的绝对值。

## 复杂度

设十进制位数为 `d`。时间复杂度和额外空间复杂度均为 `O(d)`。

## 边界条件与本地验证

个位数及十进制回文数的镜像距离为 `0`；末尾为零时，反转后的前导零不会保留。使用题目三个样例验证。
