# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

冠军没有任何更强队伍，等价于入度为零的节点。统计所有节点入度，恰有一个零入度节点时返回它，否则返回 -1。

## 复杂度

时间 `O(n+m)`，空间 `O(n)`。

## 边界条件与本地验证

没有边时多个队伍都可能冠军；DAG 性质保证唯一零入度候选即冠军。
