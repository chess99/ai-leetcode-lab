# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用输入栈接收新元素，用输出栈服务队首访问。输出栈为空时，将输入栈全部弹出并压入输出栈以反转顺序。

## 复杂度

`push` 为 O(1)，`pop` 与 `peek` 的摊销时间复杂度为 O(1)，额外空间复杂度 O(n)。

## 边界条件与本地验证

输出栈已有元素时不能再次转移输入栈，以保持先进先出顺序。已进行 Python 语法检查及最小断言。
