# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

总长度必须能被四整除。将火柴降序排列后，回溯放入四条边；若超出目标边长则剪枝，同一层只尝试一种相同的当前边长，以消除四条边的对称重复。

## 复杂度

最坏时间 `O(4^n)`，递归栈 `O(n)`；四条边和每层去重集合为常数级辅助状态。

## 边界条件与本地验证

空数组、总和不能整除、最长火柴超过边长均提前失败。对 300 组至多 8 根的随机输入枚举所有四边分配，与回溯结果一致。
