# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

后序遍历返回向父节点延伸的最长同值边数。若子节点值相同则可延长；左右两条可拼成经过当前节点的路径并更新全局最大值。

## 复杂度

时间 `O(n)`，递归栈 `O(h)`。

## 边界条件与本地验证

- 空树和单节点长度为零；路径按边数计。
- 本地执行 `py_compile` 和分叉/链式断言。
