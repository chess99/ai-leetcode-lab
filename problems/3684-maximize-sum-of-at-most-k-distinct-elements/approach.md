# 解题记录

## 思路

去重后按降序保留前 k 个正数，得到最大和的选择。

题目中的数均为正数，所以在不超过 `k` 个且必须互异的约束下，多选一个不同元素总会增加总和。先去重，再从大到小选择至多 `k` 个值，既使总和最大，也直接满足返回值严格递减的要求。

## 复杂度

设数组长度为 `n`。去重后排序的时间复杂度为 `O(n log n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

不同元素不足 `k` 个时应全部返回；重复值只能选择一次。使用题目三个样例和 `k=1` 的输入验证。
