# 解题记录

## 思路
二分答案秒数。对候选前缀记录每个下标最后一次出现的位置，必须每个下标都出现。顺序扫描：非最后出现的秒可用于任意减一；在最后出现秒前，必须已积累足够空闲秒把该位置减到 0，然后在该秒标记它。

## 复杂度
设 `m` 为 `changeIndices` 长度，单次检查 `O(m+n)`，总时间 `O((m+n)log m)`，空间复杂度 `O(n)`。

## 边界条件与本地验证
缺少任一待标记下标立即不可行；值为 0 也仍需一个最后出现秒标记。已覆盖三个题目示例。
