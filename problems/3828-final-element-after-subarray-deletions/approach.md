# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

最终保留某个内部元素时，第一次删除后它仍可能受对手选择；区间删除博弈的端点策略使 Alice 能保证两个原始端点中较大者，并且 Bob 能阻止得到更大内部值。因此极小极大值为 `max(nums[0],nums[-1])`。

## 复杂度

时间与空间均为 `O(1)`。

## 边界条件与本地验证

单元素直接返回自身；保留隐藏变量，覆盖题面示例并用小数组状态博弈对拍。
