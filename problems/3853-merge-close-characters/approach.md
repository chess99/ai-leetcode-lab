# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按题意直接模拟：每轮从左到右寻找最小左端点，并在其后至多 `k` 个位置中找最小右端点；找到后删除右字符并重启扫描。

## 复杂度

长度至多 100，最坏时间 `O(n^3)`，空间 `O(n)`。

## 边界条件与本地验证

每次删除后下标会收缩，必须重新从最左侧按规则选择下一对。
