# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先比较相邻元素二分找到峰顶。随后在左侧递增区间二分目标；若未找到，再在右侧递减区间反向二分。优先搜索左侧保证返回目标出现的最小下标。

## 复杂度

三次二分共调用接口 `O(log n)` 次，额外空间 `O(1)`。

## 边界条件

峰顶包含在左侧搜索中；右侧从峰顶后一位开始；递减区间的二分移动方向与普通递增数组相反。
