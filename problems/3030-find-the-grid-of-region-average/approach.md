# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举每个 `3 x 3` 子网格，检查其中全部相邻边是否满足阈值。对有效区域求向下取整的平均强度，并累加到它覆盖的九个格子；最后每格再对覆盖它的区域平均值向下取整。

## 复杂度

每个候选区域只检查和更新常数个格子，时间 `O(mn)`，额外空间 `O(mn)`。

## 边界条件与本地验证

无有效区域覆盖的像素保留原值。区域平均值须先单独向下取整，再参与重叠区域的平均。
