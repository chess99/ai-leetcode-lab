# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

同步扫描目标串和候选词的连续字符组。字符必须相同；候选组不能更长，长度不同则目标组必须至少为 3。所有组满足即为可拉伸词。

## 复杂度

每个候选词线性扫描，时间 O(总字符数)，空间 O(1)。

## 边界条件与本地验证

组长度相等可直接匹配；目标组小于 3 不能拉伸。本地对示例和长度失配断言，并执行 py_compile。
