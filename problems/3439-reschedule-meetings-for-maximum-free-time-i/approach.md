# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

移动连续的至多 `k` 场会议后，能够合并其两端及内部的 `k+1` 个空隙。先计算所有空隙，再用长度 `k+1` 的滑动窗口取最大和。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

验证首尾空隙、移动少于 k 场、空隙全为零及最大窗口位于中间。
