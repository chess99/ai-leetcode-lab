# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

依次检查每一对相邻元素。两个正整数奇偶性不同当且仅当它们的和为奇数；若发现和为偶数的相邻对，立即返回 `False`。

## 复杂度

设数组长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

只有一个元素时没有相邻对，`all` 在空迭代上的结果为 `True`，符合题意。
