# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计频次。对每个大于 1 的起点，连续尝试平方；每个非中心层需要两个相同值，且其平方值必须存在才能继续。值 1 平方后不变，单独取不超过其频次的最大奇数。

## 复杂度

不同起点的平方链很短，时间 `O(n log log M)`，哈希表空间 `O(n)`。

## 边界条件与本地验证

平方链的最终中心只需一个元素；不能因为当前层有两个元素而在其平方不存在时虚构中心。
