# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

动态规划按两个字符串前缀计算 LCS。字符相等时取左上状态加一，否则取上方和左方的最大值。只保留上一行的左上值及一维当前行，降低空间占用。

## 复杂度

设长度为 `M`、`N`，时间为 `O(MN)`，空间为 `O(min(M, N))`。

## 边界条件与本地验证

- 相同字符串的答案为全长；无公共字符时为 0。
- 本地覆盖题目三个示例和重复字符情况。
