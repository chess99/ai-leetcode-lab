# 解题记录

## 思路

由于操作只能删除末尾字符，最终相等的字符串必须是三者的公共前缀。为了让删除次数最少，应保留最长公共前缀；若其长度为 L，操作数为三串总长度减去 `3L`。若首字符就不同，则不能把字符串删空，返回 -1。

## 复杂度

时间复杂度 O(min(|s1|, |s2|, |s3|))，额外空间复杂度 O(1)。

## 边界条件与本地验证

验证了三个字符串已相等、一个字符串是另两个的前缀、仅首字符相同和首字符不同的情况；并对小字符串与枚举所有非空公共前缀的 oracle 核对。
