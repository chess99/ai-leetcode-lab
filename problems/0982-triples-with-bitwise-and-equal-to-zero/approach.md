# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先枚举前两个数，统计每种按位与结果出现次数。再枚举第三个数和所有已出现的掩码，只要二者按位与为零，就把该掩码的有序数对数量加入答案。

## 复杂度

设数组长度为 `n`、不同按位与结果数为 `m`，时间 `O(n²+nm)`，空间 `O(m)`。

## 边界条件

三元组下标允许重复且有顺序，因此数对必须按有序对统计；值为 0 的元素与所有掩码兼容。
