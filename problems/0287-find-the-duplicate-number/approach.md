# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将下标指向 `nums[index]`，值域限制使该函数图必有环，重复数正是环入口。Floyd 快慢指针先相遇，再把慢指针重置到起点并同步前进，第二次相遇位置即入口。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`，且不修改数组。

## 边界条件与本地验证

- 重复数出现多次仍对应同一入口。
- 本地断言：三个题目示例分别返回 2、3、3，输入数组保持不变。
