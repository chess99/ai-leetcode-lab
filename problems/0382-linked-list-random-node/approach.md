# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

水塘抽样：第一个节点先入选；扫描第 `i` 个节点时以 `1/i` 概率替换当前答案。任一节点最终保留的概率均为 `1/n`，因此抽样均匀。

## 复杂度

每次抽样扫描链表，时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

- 单节点始终返回该节点值。
- 本地断言：多次结果均属于输入值集合；固定随机替身可验证替换分支。
