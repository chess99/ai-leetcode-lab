# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从左到右维护此前最大 `nums[i]`，以及此前可形成的最大差 `nums[i]-nums[j]`。当前数作为 `k` 时，最大三元组值就是该最大差乘当前数；随后更新差和左侧最大值。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

只有满足顺序的状态才参与更新；所有值为负的候选由初始 0 排除。
