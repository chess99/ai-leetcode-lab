# 解题记录

## 思路

使用输入栈接收新书，输出栈保存可借出的队首书籍。弹出时若输出栈为空，将输入栈全部转移过去，顺序便被反转，栈顶就是最早入队的元素。

## 复杂度

单次 `appendTail` 为 `O(1)`；`deleteHead` 均摊为 `O(1)`，每个元素最多转移一次。额外空间为 `O(n)`。

## 边界条件与本地验证

两个栈都为空时返回 `-1`。已断言依次入队 1、2 后两次出队返回 1、2，第三次返回 -1。
