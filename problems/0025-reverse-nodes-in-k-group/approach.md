# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

哑节点连接头部。每轮先向后探测 k 个节点，若不足一组直接返回；否则把这一组的 next 指针逐个反转，再把前驱与新组头连接，原组头成为下一轮前驱。

## 复杂度

每个节点仅被访问和改链常数次，时间 O(n)，额外空间 O(1)。

## 边界条件与本地验证

末尾不足 k 个节点不翻转；k 为一时循环仍保持链表原顺序。
