# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每次操作把当前串复制为后半段；若操作为 1，后半段字符整体加一。`k-1` 的二进制第 `i` 位表示最终位置在第 `i` 次扩展时是否落入后半段，因此累加这些位对应的操作值即可得到字符偏移。

## 复杂度

时间 `O(log k)`，空间 `O(1)`。

## 边界条件与本地验证

使用 0 基下标 `k-1`；偏移对 26 取模。2 组样例及小操作序列显式生成字符串通过。
