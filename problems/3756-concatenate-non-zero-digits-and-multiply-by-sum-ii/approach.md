# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:54Z

## 算法

抽取全串非零数字及原位置，预处理连接整数的十进制滚动前缀、数字和前缀与 10 的幂。每次查询用二分定位区间包含的非零数字段 `[a,b)`；连接值由 `prefix[b]-prefix[a]*10^(b-a)` 得到，再乘该段数字和。

## 复杂度

预处理 `O(m)`；每个查询 `O(log m)`，总空间 `O(m)`。

## 边界

区间无非零数字时两段前缀差均为 0；所有乘法与连接值按模处理，数字和可直接用整数。
