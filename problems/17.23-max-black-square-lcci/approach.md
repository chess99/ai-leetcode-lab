# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

预处理每个黑格向右连续黑格数和向下连续黑格数。边长从大到小、左上角按行列递增枚举，只需检查候选四条边的两个方向连续长度，即可确认黑色边框。

## 复杂度

预处理 O(n²)，枚举最坏 O(n³)，辅助数组空间 O(n²)，n 不超过二百。

## 边界条件与本地验证

空矩阵返回空数组；倒序边长和正序坐标使首次找到的答案满足最大边长、最小行列的优先级。
