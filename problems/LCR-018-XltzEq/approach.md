# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

使用双指针从字符串两端向中间移动，跳过非字母数字字符，再忽略大小写比较当前字符；一旦不同即不是回文。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

空串和只含标点的串均有效；本地断言覆盖了两个样例及空串。
## 思路

双指针收缩窗口并维护不重复字符集合。

## 复杂度

时间 `O(n)`，空间 `O(字符集)`。

## 边界条件与本地验证

覆盖空串。
