# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

单调栈保存当前可保留的分段最大值。新值小于栈顶时，它必须与所有更大段合并，合并后的代表值取这些段和新值的最大值；否则可独立保留一段。栈长度即最大最终长度。

## 复杂度

每个元素至多入栈出栈一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

相等值不合并，可保留更多元素；严格下降前缀会被压成一个段。覆盖 `[4,2,5,3,5]` 和已非递减数组。
