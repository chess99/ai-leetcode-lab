# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

分别按 `(值,下标)` 升序和 `(-值,下标)` 升序排列下标，再用单调栈求每个位置右侧满足奇跳、偶跳规则的最小下标。随后从右向左做布尔 DP：奇跳可达性取决于落点的偶跳可达性，反之亦然。

## 复杂度

两次排序主导时间 `O(n log n)`，数组和栈空间 `O(n)`。

## 边界条件

相同值必须按下标升序，才能选择最小落点；最后一个位置的奇、偶状态都为可达。
