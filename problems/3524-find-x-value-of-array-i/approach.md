# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

一次移除前后缀后保留的恰是一个非空子数组。动态规划统计以当前位置结尾、每种乘积模 `k` 的子数组数，加入当前值时扩展旧子数组并新建单元素子数组，再累加进答案。

## 复杂度

时间 `O(nk)`，空间 `O(k)`。

## 边界条件与本地验证

单元素子数组必须计入；按题意保存中间输入变量。覆盖给定的 `k=3` 示例。
