# 解题记录

## 思路

根化树并用显式 DFS 求进入/离开时间和子树异或。每条非根节点的父边对应切下该子树。枚举两个节点：若一个是另一个祖先，三个组件异或需从嵌套子树中相减；否则是两个独立子树与其余部分。计算三值极差取最小。

## 复杂度

预处理 `O(n)`，枚举两条边 `O(n²)`，图和数组空间 `O(n)`。

## 边界条件与本地验证

分别覆盖嵌套子树和分离子树；显式栈避免链形树递归溢出。题面样例得到 `9、0`；随机 `n<=10` 的树枚举全部两条切边并重建组件对拍。`n=1000` 约 0.31 秒。
