# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

栈保存尚未删除的前缀；读入新字符时若与栈顶字母相邻（含 a/z），弹栈，否则入栈。

## 复杂度

时间 O(n)，空间 O(n)。栈顶即当前最左可能产生的新相邻对的位置。

## 边界条件与本地验证

本地核对了无可删除字符、连续触发连锁删除以及 `a` 与 `z` 首尾相邻的情况；栈顶始终对应当前字符串最右侧尚未删除的字符。
