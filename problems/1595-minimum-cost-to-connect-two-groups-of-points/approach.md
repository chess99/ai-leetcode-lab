# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
逐个处理第一组点，位掩码记录已连接的第二组点，每个第一组点至少选择一条边。处理完后，对仍未覆盖的第二组点补上各自最便宜边。

## 复杂度
时间 `O(mn2^n)`，空间 `O(2^n)`。

## 边界条件与本地验证
一个点可连接多条边，但最优解中额外覆盖可在末尾按最小边补齐。
