# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

以第一个字符串作为候选前缀。依次和其余字符串比较；当某个字符串不以候选前缀开头时，删去候选前缀末尾字符，直到匹配或为空。

## 复杂度

时间 O(S)，S 为所有字符数；额外空间 O(1)（不计返回字符串）。

## 边界条件与本地验证

覆盖单个字符串、空字符串、首字符就不同，以及一个字符串是另一个前缀的情况。
