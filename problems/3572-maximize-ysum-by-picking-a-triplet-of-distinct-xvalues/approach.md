# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每个 x 只保留最大的 y；在这些候选中取三个最大的值，类别不足三个返回 -1。

## 复杂度

时间 O(n log n)，空间 O(n)。

## 边界条件与本地验证

本地核对了不足三个不同 `x`、同一 `x` 对应多个 `y` 以及恰好三个不同 `x` 的情况；每类只保留最大 `y` 不会损失最优解。
