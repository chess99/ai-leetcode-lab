# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

所有单词先入集合；删除每个单词的所有真后缀。保留者不可能作为其他单词后缀，长度加井号即最短编码长度。

## 复杂度

设总字符数为 L，最坏时间 `O(L^2)`，空间 `O(L)`。

## 边界条件与本地验证

- 重复词自动去重；已完成 `py_compile` 和后缀案例断言。
