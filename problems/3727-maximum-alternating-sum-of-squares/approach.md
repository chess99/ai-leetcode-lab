# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:51Z

## 算法

重排后正号位置有 `ceil(n/2)` 个、负号位置有 `floor(n/2)` 个。先计算所有平方并排序，把最小的 `floor(n/2)` 个放到负号位置，其余放到正号位置。

## 复杂度

时间 `O(n log n)`，空间 `O(n)`。

## 边界

平方消除了原数正负影响；奇数长度时正号位置恰好多一个。
