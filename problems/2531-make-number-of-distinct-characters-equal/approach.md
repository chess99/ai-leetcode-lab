# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计两个词的字符频次，枚举两边实际出现的字符作为交换对象。根据被移除字符是否只剩一次、被加入字符是否原本存在，在 `O(1)` 内计算交换后的不同字符数。

## 复杂度

字符集固定为 26，时间 `O(26^2)`，额外空间 `O(26)`。

## 边界条件与本地验证

相同字符交换不改变集合大小；出现一次的字符移出后应减少不同字符数。已验证题面样例及初始不同字符数相等的情形。
