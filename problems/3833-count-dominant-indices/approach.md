# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从右向左维护右侧元素和及个数。用乘法比较 `nums[i] * 个数` 与右侧和，避免浮点平均值。

## 复杂度

时间复杂度 O(n)，额外空间复杂度 O(1)。

## 边界条件与本地验证

最右元素不参与统计。已进行 Python 语法检查及最小断言。
## 思路

从右向左维护后缀和与长度，比较当前值和后缀平均值。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

覆盖最后一个元素不参与比较。
