# 解题记录

## 思路

不含基因值 1 的子树答案必为 1，因此只有值 1 所在节点到根的祖先链需要计算。沿该链上行，每次把当前祖先相较上一节点新增的子树节点加入全局集合，再推进最小未出现正整数。用 `prev` 跳过已经完整处理过的子树，保证每个节点只访问一次。

## 复杂度

建树和遍历总时间 `O(n)`，邻接表、集合与栈空间 `O(n)`。

## 边界条件与本地验证

若值 1 不存在，直接返回全 1；链形树也使用显式栈，避免递归溢出。题面样例通过；随机小树逐节点收集完整子树基因集合对拍。10 万节点链形性能烟测约 0.06 秒。
