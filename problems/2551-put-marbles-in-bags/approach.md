# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

首尾重量对所有方案相同。每个切口只额外贡献相邻两颗弹珠重量之和，因此最大方案选最大的 `k-1` 个边界，最小方案选最小的，二者差即答案。

## 复杂度

排序时间 `O(n log n)`，边界数组空间 `O(n)`。

## 边界条件与本地验证

`k=1` 或 `k=n` 时方案差为 0。题面样例得到 `4、0`；随机短数组枚举切口组合对拍。
