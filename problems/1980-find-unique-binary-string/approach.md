# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

构造第 i 位与第 i 个字符串第 i 位相反的新串（对角线构造）。

## 正确性

新串与每个输入字符串至少在其对应对角位不同，因此不可能与任一输入相同。

## 复杂度

时间和空间均为 `O(n)`。

## 边界条件与本地验证

只有一个字符串时仍能翻转唯一位；已用样例验证。
