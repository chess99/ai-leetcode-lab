# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
区间 DP：两端相同则沿用内部答案，否则在左端或右端补一个对应字符，取两个子区间较小值加一。用一维数组滚动保存上一层状态。

## 复杂度
时间 `O(n²)`，空间 `O(n)`。

## 边界条件
单字符和空内部区间代价为零；更新时需保存被覆盖的左上对角状态。
