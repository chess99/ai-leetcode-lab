# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

记录每个元素值的最小下标，枚举组大小的全部因数并取最小下标。

## 复杂度

时间 `O(元素数 + 组数*sqrt(V))`，空间 `O(元素数)`。

## 边界条件与本地验证

覆盖重复元素和无可整除元素。
