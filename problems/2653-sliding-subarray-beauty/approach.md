# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
值域仅 `[-50,50]`，滑窗维护频次数组，每个完整窗口从小到大找第 `x` 个数；若非负则输出 0。
## 复杂度
时间 `O(101n)`，空间 `O(101)`。
## 边界条件与本地验证
窗口中负数不足 `x` 时为 0；已验证题面样例。
