# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

位集第 `s` 位表示总奖励 `s` 可达。处理奖励值 `v` 时，只有小于 `v` 的已有总和可接上它；截取这些位后左移 `v` 位并合并。去重后按升序处理。

## 复杂度

位集长度至多为最大值两倍，时间约 `O(nM / word_size)`，空间 `O(M)`。

## 边界条件与本地验证

初始总和 0 可达；相同奖励值不能重复选择，去重可避免无效转移。
