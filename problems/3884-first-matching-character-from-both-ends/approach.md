# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按下标从小到大比较 `s[i]` 和它关于字符串中心对称的位置 `s[n-i-1]`。第一次相等的下标就是答案。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

奇数长度字符串的中心位置总会匹配；无任何匹配时返回 `-1`。本地断言覆盖了样例和这两种边界。
