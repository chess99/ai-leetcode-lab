# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

若 0、1 数量差超过 1 则无解。否则比较与可行交替模板不匹配的位置数，每次交换修正两个位置。

## 正确性

数量条件必要；固定模板下错位的 0 与错位的 1 成对交换，次数恰为错位数一半。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

数量相等时两个模板均需比较；已用样例验证。
