# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举无序房屋对，最短路是直线路径、经 `x -> y` 的新边、经反向新边三者的最小值。每个无序对对应两个有序对，因此将对应距离计数加 2。

## 复杂度

时间 `O(n^2)`，额外空间 `O(n)`；本题 `n <= 100`。

## 边界条件与本地验证

当 `x == y` 时新增边不会缩短路径，公式仍成立。结果中不可能出现距离 0 的房屋对。
