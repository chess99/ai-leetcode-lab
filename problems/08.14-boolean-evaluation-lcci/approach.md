# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用记忆化区间动态规划统计子表达式为假、为真的方案数。枚举每个运算符作为最后计算位置，组合左右区间的计数，并按真值表累加。单字符区间直接确定；缓存避免重复切分。

## 复杂度

设运算符数为 n，状态 O(n²)、每个状态枚举 O(n) 切分点，时间 O(n³)，空间 O(n²)。

## 边界条件与本地验证

表达式只有 0、1 和三种运算符；返回缓存结果中 result 对应的计数。
