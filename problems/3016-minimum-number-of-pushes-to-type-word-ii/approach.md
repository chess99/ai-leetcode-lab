# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计每个字符频次并降序排列。八个按键的第一层代价为 1、第二层代价为 2，依此类推；把频次更高的字符放入更低代价层即可由交换论证得到最优。

## 复杂度

计数为 `O(n)`，排序最多 26 个频次，额外空间 `O(26)`。

## 边界条件与本地验证

不同字符不超过 8 个时每次都只需按一次；频次为零的字符不参与映射。
