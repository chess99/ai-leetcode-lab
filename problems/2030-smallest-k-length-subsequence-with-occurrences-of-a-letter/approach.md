# 解题记录

## 思路

用单调栈构造字典序最小的长度 `k` 子序列。栈顶比当前字符大、删除后仍能凑够长度，且若栈顶是目标字母则后面仍足够补回时，才可弹栈。入栈时也要预留仍需的目标字母槽位：目标字母直接入栈，普通字符只有在剩余槽位多于需求数时才可入栈。

## 复杂度

每个字符至多进出栈一次，时间 `O(n)`，空间 `O(k)`。

## 边界条件与本地验证

覆盖 `repetition=0`、必须全部选择目标字母、目标字母后续无法补回。题面样例得到 `eet、ecde、bb`；随机长度不超过 11 的字符串枚举所有长度 `k` 的下标组合取最小合法串，对拍 700 组。
