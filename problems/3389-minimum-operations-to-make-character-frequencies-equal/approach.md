# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

枚举最终所有保留字符的共同频率 `f`。按字母顺序动态规划，状态表示当前字母是否向相邻字母转移一个可利用的字符；转移同时比较删除、补充和相邻替换的代价。取所有 `f` 的最小值即可。

## 复杂度

枚举频率和 26 个字符，时间 `O(26n)`，空间 `O(1)`。

## 边界条件与本地验证

覆盖只含一种字符、目标频率为零、相邻字符之间可转移及稀疏频率。
