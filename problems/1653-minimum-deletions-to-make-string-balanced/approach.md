# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
遇到 b 累计可删 b 数；遇到 a 时在删除当前 a 和删除此前全部 b 中取最小。
## 正确性
每个前缀仅两类最优处理，递推覆盖所有平衡结果。
## 复杂度
时间 `O(n)`，空间 `O(1)`。
## 边界条件与本地验证
- 验证了全 a、全 b 和交错字符串。
