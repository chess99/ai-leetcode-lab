# 解题记录

## 思路

枚举数组中两个元素组成的所有有序数对，筛选满足 `abs(x - y) <= min(x, y)` 的强数对，并取其中最大的异或值。同一个元素可以选择两次，因此候选集合一定非空。

## 复杂度

时间复杂度 O(n²)，额外空间复杂度 O(1)。

## 边界条件与本地验证

覆盖了只有自身配对有效、多个重复值和普通强数对；对随机小数组使用独立双重循环参考实现核对。
