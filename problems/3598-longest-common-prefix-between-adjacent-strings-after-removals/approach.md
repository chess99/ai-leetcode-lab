# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；Created：2026-08-11T22:15:56Z

## 思路

预处理每对原相邻字符串的 LCP，以及其前缀/后缀最大值。删除 `i` 只会移除相邻对 `i-1,i` 和 `i,i+1`，并新增 `i-1,i+1`；合并三部分最大值即可。

## 复杂度

时间 `O(Σ|words[i]|)`，空间 `O(n)`。

## 边界条件与本地验证

首尾删除没有新相邻对，单字符串答案为 0。已覆盖两个样例。
