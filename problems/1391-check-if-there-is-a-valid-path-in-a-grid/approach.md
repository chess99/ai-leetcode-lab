# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

为六种街道预定义它们可通行的两个方向。从左上角做深度优先搜索，只有当前街道能朝邻格延伸且邻格街道有反向接口时才入栈。能访问右下角即存在有效路径。

## 复杂度

每个格子最多访问一次，时间复杂度为 `O(mn)`，访问集合和栈空间复杂度为 `O(mn)`。

## 边界条件与本地验证

相邻格子必须双向接口匹配，单方面连通无效；`1 x 1` 网格起点即终点。已用题目五个样例验证。
