# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
加入建筑 1 高度 0 和建筑 `n` 的自然上界。排序限制后从左向右、从右向左收紧每个限制点，使相邻高度差不超过距离。两限制点之间最高峰由两端允许高度及距离共同决定。

## 复杂度
限制数为 $r$，时间 $O(r\log r)$，空间 $O(r)$。

## 边界条件与本地验证
无额外限制时答案为 `n-1`。已对小 `n` 枚举所有满足相邻差约束的高度数组对拍。
