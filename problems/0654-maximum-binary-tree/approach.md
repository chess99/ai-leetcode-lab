# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护值递减栈。新值弹出的节点成为它的左子树；弹出后栈顶若存在，新节点成为栈顶右子树。栈底最终是整个数组最大值对应根。

## 复杂度

每个节点入栈、出栈最多一次，时间 `O(n)`，栈空间 `O(n)`。

## 边界条件与本地验证

- 空数组返回空树。
- 递增与递减数组分别形成单侧树。
- 本地执行 `py_compile` 并断言题目示例结构。
