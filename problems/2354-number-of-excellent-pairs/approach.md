# 解题记录

## 思路

先对 nums 去重，因为题目按不同数值组成有序对。恒等式 `popcount(a|b)+popcount(a&b)=popcount(a)+popcount(b)`，因此只需按二进制一位数分桶，枚举两个桶的位数和是否至少为 k，并累加有序对数量。

## 复杂度

时间 `O(n+31²)`，桶空间 `O(31)`。

## 边界条件与本地验证

相同值只保留一次，但 `(x,x)` 是允许的有序对。题面样例得到 `5、0`；随机数组去重后直接枚举所有有序数值对计算 OR/AND 位数，对拍 1000 组。
