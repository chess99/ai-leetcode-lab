# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先用差分数组标记所有原有灯泡覆盖的位置。随后从左向右寻找第一个未覆盖位置 `i`，把额外灯放在 `i+1`（若越界则放末尾），可最远覆盖到 `i+2`，这是覆盖最左缺口的贪心最优选择；之后跳过三格继续。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

验证全无灯、已有灯覆盖中段、长度 1 和交叠覆盖，并与小规模安装集合枚举对拍。
