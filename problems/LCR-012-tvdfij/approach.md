# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先计算数组总和，再从左到右维护左侧和。当前位置右侧和为 `总和 - 左侧和 - 当前值`，两侧相等时立即返回该下标。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

首尾位置的空侧和视为零；本地断言覆盖了三个题目样例。
## 思路

维护总和与左侧和，比较左右剩余和。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

覆盖首尾下标。
