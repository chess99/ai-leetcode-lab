# 解题记录

## 思路

最终数组是长度 `n` 的连续整数集合，因此原数组中能保留的元素必须互异，且落在某个宽度小于 `n` 的值域窗口。排序去重后用双指针寻找包含最多不同值的窗口，其余位置都可修改成缺失值；答案为 `n-窗口大小`。

## 复杂度

排序时间 `O(n log n)`，滑窗 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

重复值只有一个可保留，已连续数组答案为 0。题面样例得到 `0、1、3`；随机小数组枚举可能的目标连续区间并计算最大保留数，对拍 500 组。
