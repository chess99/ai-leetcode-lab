# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每只猴子有顺、逆时针两种选择，共 `2^n` 种。只有全体同向的两个方案没有碰撞，故答案为 `2^n - 2`，使用快速幂取模。

## 复杂度

时间 `O(log n)`，额外空间 `O(1)`。

## 边界条件与本地验证

先取模再减 2，并再次取模防止负数。已验证 `n=3` 得 6、`n=4` 得 14。
