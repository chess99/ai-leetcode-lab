# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

用显式栈模拟后序遍历。节点第二次出栈时，左右子树和已经得到，可计算当前子树和并更新频次；最后返回频次最高的所有和。

## 复杂度

时间 `O(n)`；栈、节点到子树和的映射及频次表共 `O(n)` 空间。

## 边界条件与本地验证

空树返回空数组，多个最高频答案顺序不限。对 800 棵随机树用独立递归后序统计对拍通过。
