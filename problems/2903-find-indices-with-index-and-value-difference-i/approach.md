# 解题记录

## 思路

枚举较小下标 i，再从 `i + indexDifference` 开始枚举 j。这样下标差条件天然满足，只需检查两数绝对差；找到任意可行对便立即返回，否则返回 `[-1, -1]`。

## 复杂度

时间复杂度 O(n²)，额外空间复杂度 O(1)。

## 边界条件与本地验证

当 `indexDifference = 0` 时允许 i 与 j 相等；当 `valueDifference = 0` 时任意满足下标条件的索引对均可。使用小规模随机数组与全枚举 oracle 核对了返回结果的合法性和无解判断。
