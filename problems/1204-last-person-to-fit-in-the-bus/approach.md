# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路
- 轮次：1

## 思路

对每个排队顺序计算截至该顺序的累计体重，筛选不超过 1000 的顺序，再取其中最大的顺序并返回对应姓名。

## 边界条件与本地验证

- 使用 `MAX(turn)` 选取最后一位可上车乘客。
- 已检查查询按排队顺序累计，并使用 1000 的容量限制。
