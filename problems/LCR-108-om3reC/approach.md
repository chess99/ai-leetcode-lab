# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从起点和终点同时 BFS，每轮扩展较小的一侧。枚举每个位置替换为 26 个字母生成邻居；首次与另一侧相遇即得到最短单词数。

## 复杂度

设单词数 `N`、长度 `L`，时间 `O(NL·26)`，空间 `O(N)`。

## 边界条件与本地验证

终点不在词典时返回 0；访问过的词立即从集合删除。本地验证两个样例，并与单向 BFS 随机对拍。
