# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

排序后确定较小中位数，用双指针比较两端到中位数的距离；距离相等时取右端较大值，重复 k 次。

## 复杂度

排序主导为 `O(n log n)`，额外空间不计输出为 `O(1)`。

## 边界条件与本地验证

偶数长度使用下中位数；同强度取较大值。已用题目三个样例验证。
