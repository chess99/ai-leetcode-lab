# 2414. 最长的字母序连续子字符串的长度

## 算法
扫描字符串；当前字符比前一字符恰大 1 时延长当前连续段，否则重置为 1，维护最大长度。

## 正确性说明
连续段的充要条件是每对相邻字符字母序恰递增 1。递推精确维护每个位置结尾的最长合法段，最大值即答案。

## 复杂度分析
时间 `O(n)`，空间 `O(1)`。

## 边界情况
单字符为 1；`z` 后的 `a` 不连续。
