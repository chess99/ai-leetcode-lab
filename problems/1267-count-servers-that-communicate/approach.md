# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

预先统计每行和每列的服务器数量。某服务器所在行或列还有其他服务器时即可通信，累计这样的服务器数。

## 复杂度

时间复杂度为 `O(mn)`，额外空间为 `O(m+n)`。

## 边界条件与本地验证

- 孤立服务器不计入。
- 本地验证了题目示例和单服务器网格。
