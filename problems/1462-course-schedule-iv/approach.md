# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用邻接矩阵记录直接先修关系，并以每门课为中转点做 Floyd 传递闭包。预处理后每个查询可直接查矩阵。

## 复杂度

预处理 `O(n^3)`，查询总计 `O(q)`，空间 `O(n^2)`。

## 边界条件与本地验证

无先修关系时全部为 false；间接先修会由中转点补全。对 `n=1..13` 的随机 DAG 从每个源点独立 DFS 求可达集合，与传递闭包逐查询对拍。
