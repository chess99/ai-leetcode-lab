# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

忽略长度不足 `k` 的单词，统计其长度 `k` 前缀；每个出现至少两次的前缀唯一对应一个连接组。

## 复杂度

时间 `O(nk)`，空间 `O(nk)`。

## 边界条件与本地验证

重复单词按不同下标计数；一个单词只属于其唯一前缀组。保留隐藏变量，覆盖三组示例。
