# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

从子矩阵的最上行和最下行向中间成对遍历，只交换这两行在子矩阵列范围内的元素。这样只翻转指定正方形内部的行顺序，其他位置不变。

## 复杂度

子矩阵边长为 `k`。时间复杂度为 `O(k^2)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

`k` 为奇数时中间行无需交换；`k=1` 时矩阵保持不变。
