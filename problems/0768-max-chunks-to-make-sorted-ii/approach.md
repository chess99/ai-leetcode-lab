# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

单调栈保存当前各块的最大值。新值不小于栈顶时可以独立成块；否则它与前面所有最大值大于它的块都必须合并，同时保留这些块原来的最大值作为合并块上界。最终栈中元素个数就是最多块数。

## 复杂度

每个元素至多入栈、出栈一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

相等元素允许分块，因此比较使用 `>=`；合并时必须保留弹出的第一个栈顶作为新块最大值。
