# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把目标数组按高度分层。第一列需要启动 `target[0]` 次区间操作；之后只有当前高度高于前一位置时，新增的高度层必须在此处启动操作，因此累加所有正差值。

## 复杂度

时间复杂度 $O(n)$，额外空间复杂度 $O(1)$。

## 边界条件与本地验证

下降或持平不增加操作数。已对小数组按每个高度层统计新连通段的独立公式对拍。
