# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

数组已排序，两个相邻值差超过 `maxDiff` 时必定断开；否则可沿相邻边连通。预处理每个下标的连通块编号，查询比较编号即可。

## 复杂度

预处理 `O(n)`，每个查询 `O(1)`，额外空间 `O(n)`。

## 边界条件与本地验证

相邻差等于阈值仍连通；同一点查询恒为真。覆盖两组示例。
