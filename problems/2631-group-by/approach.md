# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

顺序遍历数组，对每个元素调用回调获得字符串键。若结果对象中没有该键就创建空数组，然后追加元素；顺序追加保证每组内保留原有顺序。

## 复杂度

时间 `O(n)`，分组结果使用 `O(n)` 空间。

## 边界条件与本地验证

空数组返回空对象。题目保证键为字符串，普通对象即可安全按键存放；相同键的元素始终进入同一个数组。
