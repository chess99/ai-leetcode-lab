# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

无需真的计算乘积。逐个数试除，将遇到的不同质因子加入集合，最后返回集合大小。

## 复杂度

时间 `O(sum(sqrt(nums[i])))`，额外空间为不同质因子数 `O(P)`。

## 边界条件与本地验证

同一质因子在同一数或多个数中重复出现都只计一次；分解结束后剩余大于 1 的数也是质因子。已验证题面样例。
