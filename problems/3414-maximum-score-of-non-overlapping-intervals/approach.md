# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

按右端点排序。对每个区间二分最后一个右端点严格小于其左端点的前驱，动态规划选择至多四个区间。状态同时存总权重和原下标元组；同权重时取字典序更小的元组，确保返回满足题意的方案。

## 复杂度

时间 `O(n log n)`，空间 `O(n)`。

## 边界条件与本地验证

端点相接视为重叠，因此前驱使用严格小于；支持选零至四个区间和同权重的字典序决胜。
