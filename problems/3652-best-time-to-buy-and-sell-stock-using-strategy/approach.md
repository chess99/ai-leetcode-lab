# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先算原策略总利润。枚举长度 `k` 的窗口，修改后前半贡献为 0，后半贡献为对应价格之和；用原贡献前缀和与价格前缀和常数时间计算每个窗口的新利润，同时保留不修改的答案。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件

修改可能降低利润，因此基准答案是原利润；`k` 为偶数，窗口可覆盖整个数组。
