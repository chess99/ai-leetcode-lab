# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1（本地预解，未发起远程测试或提交）

## 思路

以先修课指向后续课程建图，使用 Kahn 拓扑排序反复处理入度为 0 的课程。最终可处理课程数等于总课程数时无环，全部可完成。

## 复杂度

时间 `O(V+E)`，图、入度和队列空间 `O(V+E)`。

## 边界条件与本地验证

- 无先修关系时全部课程可完成。
- 环不会产生新的入度为 0 节点。

本地断言 `2, [[1,0]]` 为真、`2, [[1,0],[0,1]]` 为假，并通过 `py_compile`。
