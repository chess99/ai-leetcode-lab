# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
## 思路
将每对相邻数的比较结果转成 `-1/0/1` 序列。对 `pattern` 正确构造 KMP 前缀函数，再在线性扫描比较序列时用前缀函数失配跳转，统计所有完整匹配（包括重叠匹配）。
## 复杂度
时间 O(n+m)，空间 O(m)。
## 边界条件与本地验证
相等对应 0；匹配后也按 KMP 前缀函数回退，不能把 Z 数组当作失配函数。题面样例及随机小数组逐窗口比较通过。
