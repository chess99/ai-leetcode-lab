# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

二分稳定值。检查时先用并查集加入全部必选边并检查环和强度；可选边中优先使用无需升级的边，随后才使用需要升级的边连接分量。升级数不超过预算且图连通即为可行。

## 复杂度

时间 `O(m log m log S)`，空间 `O(n)`。

## 边界条件与本地验证

必选边成环、强度不足、无法连通均返回不可行。三个题面样例通过。
