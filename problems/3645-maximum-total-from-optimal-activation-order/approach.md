# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按 `limit` 分组。阈值为 `x` 的元素在活跃数达到 `x` 时会同时失活；借助更小阈值元素反复清空活跃数，可以安排该组中至多 `x` 个元素获得收益。因此每组独立选价值最大的 `x` 个求和。

## 复杂度

排序总时间 `O(n log n)`，空间 `O(n)`。

## 边界条件

组大小小于阈值时全部选择；价值均为正，无需舍弃可选名额；按题意保留中间输入变量。
