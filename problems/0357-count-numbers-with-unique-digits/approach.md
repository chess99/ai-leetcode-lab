# 解题记录
- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium
## 思路
按位组合计数：首位 9 种，后续依次可选 9、8…种，累加长度不超过 n 的数量。
## 复杂度
- 时间 O(min(n,10))，空间 O(1)。
## 边界条件与本地验证
- n 超过 10 不再增加。验证 `0 -> 1`、`2 -> 91`。
