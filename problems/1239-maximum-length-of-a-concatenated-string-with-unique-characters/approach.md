# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用位掩码表示已选择的字符集合。先跳过自身含重复字符的字符串，再将它与所有不冲突的已有状态合并；最终取状态中置位数的最大值。

## 复杂度

设有效组合状态数为 `S`，时间复杂度为 `O(S × L)`，空间复杂度为 `O(S)`。

## 边界条件与本地验证

- 自身含重复字符的字符串不能选择。
- 空数组答案为 0。
- 本地验证了题目示例和重复字符情况。
