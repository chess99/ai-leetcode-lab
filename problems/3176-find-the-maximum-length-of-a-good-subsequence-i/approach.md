# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

`dp[i][c]` 表示以 `nums[i]` 结尾、至多使用 `c` 次相邻变化的最长子序列。枚举此前的结尾；两值不同则消耗一次变化，否则不消耗，更新当前状态。

## 复杂度

时间 `O(n^2 k)`，空间 `O(nk)`。

## 边界条件与本地验证

单个元素始终合法。`k=0` 时只可连接相同数值。
