# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把随机范围缩为前 n-黑名单数个整数，范围内黑名单映射到尾部未被拉黑的合法值，pick 只需随机一次查映射。

## 复杂度

构造 O(B)，每次 pick O(1)，映射空间 O(B)。

## 边界条件与本地验证

仅映射落入随机前缀的黑名单；尾部黑名单不会被选到。
