# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把数组分成两半。每半从有理数 1 开始枚举乘、除、不变三种选择，用 `Fraction` 精确约分并合并相同结果的方案数。若左半结果为 `x`，右半需要等于 `k/x`；将两侧计数字典对应相乘求和。

## 复杂度

时间与空间复杂度上界 `O(3^(n/2))`。

## 边界条件与本地验证

使用精确有理数避免浮点误差，覆盖数字 1 导致三种操作结果重合及中间分数。三组官方样例均通过。
