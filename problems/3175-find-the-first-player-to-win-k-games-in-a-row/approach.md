# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

队首胜者只会被之后遇到的更高技能玩家替换。顺序扫描挑战者，维护当前擂主和连胜数；若达到 `k` 立即返回。扫描结束时的擂主是全局最高技能，之后必能连胜。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

`k` 可以远大于玩家数量，扫描结束后直接返回最终擂主而无需模拟循环队列。
