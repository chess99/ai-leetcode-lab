# 解题记录

## 思路

分别把两个数组转成集合以支持常数时间成员查询。遍历 nums1 的每个元素，统计它是否存在于 nums2 的集合；再对 nums2 做对称统计。必须遍历原数组而非集合，才能让重复元素按各自下标分别计数。

## 复杂度

设两数组长度为 n、m，期望时间复杂度 O(n + m)，额外空间复杂度 O(n + m)。

## 边界条件与本地验证

重点覆盖了重复元素、完全无交集以及一侧多个重复值的情况；并与直接双数组成员判断的 oracle 交叉核对。
