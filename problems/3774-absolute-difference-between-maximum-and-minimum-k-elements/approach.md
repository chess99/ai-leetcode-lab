# 解题记录

## 思路

排序后分别累加前 k 个与后 k 个元素。

升序排序后，前 `k` 项就是最小的 `k` 个元素，后 `k` 项就是最大的 `k` 个元素。分别求和并取绝对差即可；两组允许在 `2k > n` 时重叠，这不影响各自定义。

## 复杂度

设数组长度为 `n`。排序时间复杂度为 `O(n log n)`；当前原地排序，除排序实现使用的空间外额外空间为 `O(1)`。

## 边界条件与本地验证

`k=n` 时两者都是全数组之和，答案为 `0`；重复元素需按出现次数计入。使用题目两个样例以及 `k=n` 验证。
