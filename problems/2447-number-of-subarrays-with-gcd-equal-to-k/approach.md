# 2447. 最大公因数等于 K 的子数组数目
## 思路
枚举左端点并逐步扩展右端点，维护区间最大公因数，等于 `k` 时计数；已不可能成为 `k` 时停止。
### 正确性说明
每个连续子数组被唯一一对端点枚举。扩展时 gcd 只会保持或下降；若已小于 `k` 或不能被 `k` 整除，后续不可能回到 `k`，提前停止安全。
## 复杂度
最坏时间 `O(n² log V)`，空间 `O(1)`。
## 边界条件与本地验证
单元素等于 `k` 时计数；无元素可被 `k` 整除时答案为 0。
