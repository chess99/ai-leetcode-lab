# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
预处理每个子串变成回文串所需修改次数，再做分组 DP：枚举最后一段起点，把前缀分成少一组的代价与当前段代价相加。

## 复杂度
预处理 `O(n²)`，分组 DP `O(kn²)`，空间 `O(n²)`。

## 边界条件
每段必须非空；单字符段代价为零；`k=n` 时答案为零。
