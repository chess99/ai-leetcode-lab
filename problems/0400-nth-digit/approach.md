# 解题记录
- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium
## 思路
按一位、两位等分组跳过数字位，定位所在数字和零基字符下标。
## 复杂度
- 时间 O(log n)，空间 O(1)。
## 边界条件与本地验证
- 使用 `n-1` 避免边界偏移。验证 `3 -> 3`、`11 -> 0`、`190 -> 1`。
