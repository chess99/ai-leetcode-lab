# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

`dp[value][d]` 表示以 `value` 结尾且允许的下一相邻差不大于 `d` 的最长长度。处理当前值时枚举前值，差为 `abs(value-prev)`，从前值对应状态转移；随后对差值做后缀最大化。

## 复杂度

值域至多 300，时间 `O(n * V)`，空间 `O(V^2)`。

## 边界条件与本地验证

验证递减差、相等元素形成零差，以及仅含两个元素的数组。
