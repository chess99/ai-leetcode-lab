# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举下标 0 应有的奇偶性。奇偶性不符的元素恰须一次 ±1，故操作数固定；其可选终值为相邻的两个数。将这些候选点按值滑窗，覆盖每个待改元素一次，即得到与不变元素合并后的最小值域。

## 复杂度

时间 O(n log n)，空间 O(n)。

## 边界条件与本地验证

验证了样例与长度 1；无须修改时直接取原数组值域。
