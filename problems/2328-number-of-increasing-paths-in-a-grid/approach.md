# 解题记录

## 思路

将全部格子按值升序处理。`dp[x][y]` 初始包含单格路径；所有值更小的相邻格已完成，可把它们结尾的路径接到当前格。严格小于保证相等值之间不转移，也使处理顺序无环。

## 复杂度

设格子数为 `N`，排序时间 `O(Nlog N)`，邻接转移 `O(N)`，空间 `O(N)`。

## 边界条件与本地验证

相等相邻值不能延伸；答案逐状态取模。题面样例得到 `8、3`；随机小网格显式枚举所有递增路径对拍 800 组。本次审计将递归 DFS 改为非递归排序 DP，避免十万长度链递归溢出；十万格约 0.25 秒。
