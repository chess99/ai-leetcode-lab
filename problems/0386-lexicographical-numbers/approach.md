# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把整数看作十叉字典树的前序遍历。优先乘十进入下一位；无法深入时不断回溯越过末位 9 或超过 n 的前缀，再加一访问下一个兄弟。

## 复杂度

恰输出 n 个数，时间 `O(n)`；除返回数组外使用 `O(1)` 空间。

## 边界条件与本地验证

- 回溯处理 9 结尾和大于 n 的分支。
- 本地断言：n=13 与 n=2 得到示例顺序，n=1 返回 `[1]`。
