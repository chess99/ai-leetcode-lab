# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

队列维护 FIFO，集合去重。每个目的地保存按时间递增的时间戳数组和已移除前缀长度；由于新增时间全局非递减，区间计数可二分，转发或淘汰仅移动对应前缀指针。

## 复杂度

添加、转发均摊 `O(1)`；计数 `O(log n)`；空间 `O(n)`。

## 边界条件与本地验证

重复包不改变状态；容量满时先淘汰最早包；空队列返回空数组；查询端点包含在内。
