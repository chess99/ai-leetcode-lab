# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先用迭代 DFS 给每个岛染唯一编号并记录面积。随后枚举每个 0，收集四邻不同岛编号，翻转后的面积为 1 加这些岛面积之和，去重避免同一岛从多个方向重复计算。

## 复杂度

染色与枚举均 `O(n²)`，显式栈和面积表空间 `O(n²)`。

## 边界条件与本地验证

全 1 返回 n²，全 0 返回 1；算法会修改输入网格。本地随机小矩阵对每个 0 实际翻转并 BFS 面积对拍。
