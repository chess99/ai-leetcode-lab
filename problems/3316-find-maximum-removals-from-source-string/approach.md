# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

动态规划扫描原串。状态为已匹配模式前缀长度，值为最多删除数；每个位置可保留、若字符相同可用于匹配，若在目标集合中也可删除。

## 复杂度

时间 `O(nm)`，空间 `O(m)`。

## 边界条件与本地验证

覆盖不能删除、可删除所有无关字符和多个相同字符的匹配选择。
