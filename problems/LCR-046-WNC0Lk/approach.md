# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按层从左到右做广度优先遍历。每一层处理前队尾就是该层最右节点，记录其值后再逐个弹出当前层节点并将孩子按左、右顺序加入队列。下一轮队列正好为下一层，因此得到自顶向下的右视图；根为空时直接返回空列表。

## 复杂度

时间 O(n)，空间 O(w)。

## 边界条件与本地验证

本地检查空树、只有左链、只有右链及左右孩子不齐全的层，确保记录的是本层原队尾而非下一层节点。
