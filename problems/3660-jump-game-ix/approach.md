# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

预处理后缀最小值并扫描前缀最大值。当某位置的前缀最大值不大于下一段后缀最小值时，跨越该边界的两类严格跳跃都不可能把两侧连通，因此形成一个可达分量；分量内每个起点能达到的最大值就是该段最大值。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件

相等值不能触发严格大小跳跃，边界判定使用 `<=`；单元素自成分量。按题意保留中间输入变量。
