# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

跳过任一维超过目标的三元组；在其余三元组中分别寻找能提供目标三个坐标的元素。

## 正确性

超过目标的坐标无法通过 max 合并消除；保留三维目标值的合法三元组合并后恰得目标。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

同一三元组可满足多个坐标；已用样例验证。
