# 解题记录

## 思路

计算每项任务的开始时间加持续时间并取最小值。

每个任务彼此独立，其完成时间就是 `start + duration`。遍历所有任务并返回最小完成时间即可。

## 复杂度

设任务数为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

只有一个任务时直接返回它的开始时间与耗时之和；最早开始的任务未必最早完成。使用题目样例及这两类输入验证。
