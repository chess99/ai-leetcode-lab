# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

枚举非空点集，先筛掉节点和为奇数的集合；随后以最低位为起点在诱导子图中用位集 BFS，访问完整集合时计数。

## 复杂度

`O(2^n(n+m))` 时间，`O(n)` 空间，适用于题目的小规模节点约束。

## 边界条件与本地验证

已 importlib 加载；链图 `[1,0,1]` 返回 2。
