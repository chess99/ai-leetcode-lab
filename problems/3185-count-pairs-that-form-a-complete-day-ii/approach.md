# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护已出现时长按 24 取模后的频次。处理余数 `r` 时，与此前余数 `(24-r) % 24` 的所有时长配对，然后记录 `r`。

## 复杂度

时间 `O(n)`，空间 `O(24)`。

## 边界条件与本地验证

余数 0 只能与余数 0 配对；答案使用 Python 整数以容纳大量组合。
