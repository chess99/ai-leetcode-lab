# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

单调栈维护当前连续区域尚未处理的递增最小值层。遇到更小值就结束所有更高层；遇到未在栈顶出现的新正值，必须新增一次操作。零会清空所有正值层。

## 复杂度

每个值至多进出栈一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

零不需要操作且会切断子数组；连续相同正值只需一次。覆盖 `[0,2]`、交替值和单调序列。
