# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
把字符串看作状态图，BFS/DFS 枚举加法和轮转操作可达的所有状态，用集合去重并维护最小字典序。
## 正确性
两种操作正是图的边，搜索从初始状态遍历全部可达状态；最小值因此为所求结果。
## 复杂度
设可达状态数为 `S`、串长为 `n`，时间 `O(Sn)`，空间 `O(Sn)`。
## 边界条件与本地验证
- 覆盖重复状态、轮转周期和加法周期。
