# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按学生和科目分区，分别按日期正序、倒序编号以定位首次和最近一次成绩；聚合后筛选至少两次且最近成绩更高的分区，并按要求排序。

## 复杂度

窗口编号及分组聚合由数据库执行，时间与空间均为 `O(N)` 量级（不计排序代价）。

## 边界条件与本地验证

静态核查覆盖同日不会重复、仅一次考试、成绩下降及最终排序；SQL 使用 MySQL 8 窗口函数。
