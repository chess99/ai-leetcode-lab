# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每个数组最多保留 `n/2` 个不同值。先优先保留仅属于各自数组的值，再用两个数组共同拥有的值填满剩余容量；共同值在最终集合中只计一次。

## 复杂度

构建集合的时间和空间均为 `O(n)`。

## 边界条件与本地验证

数组内重复元素可以作为必须删除的元素。两个数组都没有独有值时，答案由共同值和总容量共同限制。
