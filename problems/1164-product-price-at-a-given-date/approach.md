# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先在目标日期（含）之前按产品分组，取每组最近一次变价记录，作为该产品当天的价格。再找出从未在该日期前变价的产品，价格固定为初始值 10。用 `UNION` 合并两部分结果。

## 复杂度

聚合和关联操作的复杂度取决于索引及执行计划；逻辑上需扫描 `Products` 表并按 `product_id` 分组，额外结果空间为产品数。

## 边界条件与本地验证

变价恰好发生在 `2019-08-16` 时应采用新价格；只有未来变价记录的产品应返回 10。已按题目样例的三类产品（历史变价、当日变价、仅未来变价）核对查询逻辑。
