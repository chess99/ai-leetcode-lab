# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
逐行枚举座位位掩码。当前掩码必须只使用完好座位且无相邻学生；再与上一行掩码检查两条对角线是否冲突，更新最大人数。

## 复杂度
列数为 `c`、行数为 `r`，保守时间 `O(r·4^c)`，空间 `O(2^c)`。

## 边界条件
左右相邻不能同时坐；正上方允许；坏座位对应位必须为零。
