# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计 a、b 数量，并让出现更多的字符作为 a。枚举 a 的候选长度，余下长度唯一确定 b；按模式切片验证每个位置是否一致，且两个映射不能相同。

## 复杂度

枚举至多 O(|value|) 种长度，每种扫描模式和切片，总体为 O(|pattern|·|value|)。

## 边界条件与本地验证

空模式仅匹配空值；某一字符不存在时其映射长度为零，仍需保证不同映射约束。
