# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先统计入度：任一节点入度超过 1 立即无效。有效树必须恰有一个入度为 0 的根；从根用栈遍历，若能且只能访问全部 `n` 个节点即为有效二叉树，否则存在环或不连通部分。

## 复杂度

时间复杂度为 `O(n)`，入度数组、访问集合和栈的空间复杂度为 `O(n)`。

## 边界条件与本地验证

多个根、共享子节点、环以及孤立子图都应返回 `false`。已用题目三个样例验证。
