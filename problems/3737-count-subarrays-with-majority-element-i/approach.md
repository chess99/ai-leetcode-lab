# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:51Z

## 算法

把等于 `target` 的元素记为 `+1`，其他元素记为 `-1`。子数组和严格大于 0 等价于目标出现次数超过一半；枚举左端点并逐步累计平衡值计数。

## 复杂度

时间 `O(n^2)`，空间 `O(1)`；`n<=1000` 可承受。

## 边界

目标不存在时所有平衡值均不为正；长度 1 且元素等于目标时合法。
