# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先统计每个值的出现频率，再统计“某频率属于多少个不同值”。按原数组从左到右扫描，首个频率只属于一个值的元素即答案。

## 复杂度

时间与空间均为 `O(n)`。

## 边界条件与本地验证

同一个合格值可能重复出现，第一次出现即返回；没有唯一频率返回 -1。保留隐藏变量，覆盖三组示例。
