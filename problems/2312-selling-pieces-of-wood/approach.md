# 解题记录

## 思路

`dp[h][w]` 表示尺寸为 `h*w` 木块的最大收益。状态先取该尺寸的最高直接报价，再枚举所有横切和竖切位置，将两块最优收益相加。小尺寸先计算，保证转移已知。

## 复杂度

时间 `O(mn(m+n))`，二维表空间 `O(mn)`。

## 边界条件与本地验证

没有报价时可继续切割或收益为零；相同尺寸多条报价取最大值。题面样例得到 `19、32`；随机小尺寸与记忆化递归切分对拍 500 组，`200*200` 上限约 1.67 秒。
