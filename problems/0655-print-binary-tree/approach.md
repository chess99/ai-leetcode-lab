# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先计算高度 `h`，建立 `h` 行、`2^h-1` 列的空矩阵。递归时把节点写到当前列范围中点，左右子树分别使用左右半区。

## 复杂度

输出矩阵大小为 `O(2^h)`，构造时间和空间均为 `O(2^h)`。

## 边界条件与本地验证

- 单节点位于唯一单元格。
- 空子树不写入字符。
- 本地执行 `py_compile` 并断言单节点与非平衡树布局。
