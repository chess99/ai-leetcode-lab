# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

记忆化搜索当前值。若当前值不大于 `y`，一直加一即为最优。否则对每个除数 5、11，既可先减去余数再整除，也可先加到下一个倍数再整除；同时保留完全用减一到达 `y` 的方案，取最小值。

## 复杂度

状态随递归值快速缩小，时间和空间均为 `O(log x)`。

## 边界条件与本地验证

当 `x <= y` 不做除法；余数为零时无需考虑向上补齐，避免无意义分支。
