# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

递归搜索左右子树。若当前节点是任一目标则返回它；若左右子树各找到一个目标，当前节点即最近公共祖先，否则返回找到目标的一侧。

## 复杂度

时间复杂度为 `O(n)`，递归栈空间复杂度为 `O(h)`。

## 边界条件与本地验证

目标节点可以是另一目标的祖先；本地断言覆盖两个题目样例。
