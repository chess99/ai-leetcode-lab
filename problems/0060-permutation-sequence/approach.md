# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

排列按首位分成大小相等的阶乘块。把 k 转为零下标后，每一步用 k 除以剩余位数的阶乘确定当前选择，并在余数块中继续，最后拼接。

## 复杂度

预计算阶乘 O(n)，列表删除导致总时间 O(n²)，额外空间 O(n)。

## 边界条件与本地验证

k 在合法范围内，首块和末块分别对应零和最大下标，不会越界。
