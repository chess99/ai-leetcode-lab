# 解题记录

## 思路

进入空格的代价为 0，进入障碍的代价为 1，因此用 0-1 BFS。距离变小时，零代价邻居放到双端队列左侧，一代价邻居放到右侧，保证按非递减距离扩展。

## 复杂度

每条网格边只松弛常数次，时间 `O(mn)`，距离表和队列空间 `O(mn)`。

## 边界条件与本地验证

起点不计删除代价，全零网格答案为 0。题面样例得到 `2、0`；随机小网格与普通 Dijkstra 对拍 1000 组，十万格烟测约 0.10 秒。
