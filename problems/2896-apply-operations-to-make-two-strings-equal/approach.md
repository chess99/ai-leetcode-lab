# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

只记录不匹配位置，数量为奇数时不可能完成。用两倍成本 DP 扫描这些位置：当前不匹配点可先留给一次任意配对（贡献 `x` 的半成本），或与前一个未配对点用相邻翻转逐步配对（贡献两倍距离）。最终再除以二。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

没有不匹配时为 0；相邻两个不匹配可用一次成本 1 操作；奇数个不匹配返回 -1。
