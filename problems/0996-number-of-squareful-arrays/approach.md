# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先对不同数值预计算哪些后继能使两数之和为完全平方数，再基于值计数做回溯。每层只枚举仍有剩余次数的不同值，因此相同数字不会因交换位置被重复计数。

## 复杂度

预处理 `O(u²)`，搜索最坏接近 `O(n!)`；计数器、兼容表和递归栈空间 `O(u²+n)`。

## 边界条件

用整数平方根判断完全平方数，避免浮点误差；单元素数组本身是一种排列。
