# 解题记录

## 思路

对每个零位比较左右元素和：相等时两个方向均有效，差一时恰有一个方向有效。

运动过程中每次遇到正数都会减一并反向，因此左右两侧的元素会被交替消耗。若左右总和相等，从任一方向开始都能全部消耗；若两侧相差 `1`，只有先走向总和较大的一侧才有效；差值更大时两个方向都无效。扫描时维护左侧和，总和减左侧和即为右侧和。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

只在值为零的位置考虑起点；左右和相等贡献两种方向，相差一贡献一种。使用题目样例，并以短数组逐起点、逐方向直接模拟作交叉验证。
