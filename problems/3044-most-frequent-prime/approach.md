# 解题记录

## 思路
从每个格子向八个固定方向延伸，逐位构造所有长度至少为 2 的数字并计数。最后对出现过的数字做试除判素，按出现次数优先、数值次优先选择答案。

## 复杂度
设矩阵为 `m × n`，单条射线最大长度为 `L`，时间复杂度 `O(mnL√V)`，其中 `V` 是生成数的上界；空间复杂度 `O(mnL)`。

## 边界条件与本地验证
单格矩阵不会生成大于 10 的数，返回 -1；同频质数取更大的数。已覆盖 `[[1,1],[9,9],[1,1]]` 与 `[[7]]`。
