# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

数组是 `1..n` 的排列，答案只依赖 `n`。当 `n<3` 时只能得到数组中的 `n` 个值；否则三个值的异或恰好覆盖从 0 到不小于 `n` 的最小二次幂减一，数量为 `2^(bit_length(n))`。

## 复杂度

时间 `O(1)`，额外空间 `O(1)`。

## 边界条件与本地验证

单元素和双元素单独处理；覆盖 `[1,2]` 与长度为 3 的排列。
