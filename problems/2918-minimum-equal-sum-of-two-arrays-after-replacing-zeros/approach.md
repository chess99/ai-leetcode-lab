# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每个零至少替换成 1，得到两个数组各自的最小可达和。较小一侧只要存在零就可把某个零增大到另一侧的最小和；否则无法追平。相等时直接返回。

## 复杂度

时间 `O(n+m)`，空间 `O(1)`。

## 边界条件与本地验证

无零数组的和固定；零可替换为任意正整数。
