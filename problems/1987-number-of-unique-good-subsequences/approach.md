# 解题记录

## 思路

分别维护以 `0` 和 `1` 结尾的不同好子序列数。遇到 `1` 时，原有任意非空好子序列都可追加它，另加单字符 `1`；遇到 `0` 时，只能给以 `1` 开头的好子序列追加它。单字符 `0` 不参与上述状态，另用布尔值记录是否存在。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

覆盖全零、全一和大量重复子序列。题面样例得到 `2、2、5`；对长度不超过 11 的随机二进制串枚举全部下标子序列并去重，与动态规划对拍。
