# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计不匹配位置中 `xy` 和 `yx` 的数量。每两个同类不匹配可用一次交换修复；若各剩一个，则需要两次交换。总不匹配数为奇数时无解。

## 复杂度

时间复杂度为 `O(n)`，额外空间为 `O(1)`。

## 边界条件与本地验证

- 字符串已相等时返回 0。
- 单个不匹配无法修复，返回 -1。
- 本地验证了同类与交叉不匹配的情况。
