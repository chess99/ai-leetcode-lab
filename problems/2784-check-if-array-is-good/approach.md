# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

长度为 n 的好数组排序后必须恰好为 `1` 到 `n - 1`，并额外包含一次 `n - 1`。

## 复杂度

时间复杂度 O(n log n)，额外空间复杂度 O(n)。

## 边界条件与本地验证

任何缺失、额外或重复错误的数字都会与目标序列不一致。已进行 Python 语法检查及样例断言。
