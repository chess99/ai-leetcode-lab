# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从起点迭代 DFS，访问过的位置标为 -1 防止循环；遇到值为 0 立即成功。

## 复杂度

时间 `O(n)`，额外空间 `O(n)`。

## 边界条件与本地验证

- 越界跳转忽略。
- 同一位置可能在首次弹栈前被不同路径重复压栈；弹栈后必须先跳过已标记位置，不能把标记值 `-1` 当作新的跳跃距离。
- 本地验证了可达、不可达示例，以及含重复入栈路径的随机图对拍。
