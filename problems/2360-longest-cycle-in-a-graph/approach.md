# 解题记录

## 思路

每个节点至多一条出边。从每个尚未全局访问的节点出发沿边走，用局部字典记录本次路径中节点的步数，同时立即标记全局访问；若走回局部路径节点，当前步数减首次步数就是环长，若走到 -1 或旧路径则无新环。

## 复杂度

每个节点全局只访问一次，时间 `O(n)`，全局与局部记录空间 `O(n)`。

## 边界条件与本地验证

无环返回 -1；进入其他起点已处理的环不能重复计算。题面样例得到 `3、-1`；随机功能图从每个起点独立跟踪局部环对拍 1500 组，十万链约 0.03 秒。
