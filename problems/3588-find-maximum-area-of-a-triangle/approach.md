# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- Created：2026-08-11T22:15:55Z

## 思路

按横坐标、纵坐标分别分组。固定一条竖直（或水平）底边时，最优第三点一定取全局另一坐标的最远端；枚举每个分组的最小和最大坐标即可。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

同一横/纵坐标不足两个点时不能作底边；面积为零时返回 `-1`。已覆盖样例和共线点。
