# 2370. 最长理想子序列

## 思路

维护每个字母结尾的最长长度。处理当前字母时，在字母表位置差不超过 `k` 的结尾中取最大长度加一，更新当前字母。

### 正确性说明

当前字母只能接在位置差不超过 `k` 的结尾后，算法检查全部合法前驱并取最长，因此得到每种结尾的最优值；所有结尾的最大值即答案。

## 复杂度

时间复杂度为 `O(26n)`，空间复杂度为 `O(26)`。

## 边界条件与本地验证

- `k=0` 时只能连接相同字母。
- 字母表不循环。
- 单字符答案为 1。
