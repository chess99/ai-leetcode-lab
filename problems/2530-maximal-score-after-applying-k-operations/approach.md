# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每次选择当前最大数才不会降低本次及后续可得分数，用最大堆维护。弹出最大值计入答案，再放回其向上取整三分之一。

## 复杂度

建堆 `O(n)`，操作 `O(k log n)`；空间 `O(n)`。

## 边界条件与本地验证

用 `(x+2)//3` 实现向上取整；数值为 1 时替换后仍为 1。已验证题面样例。
