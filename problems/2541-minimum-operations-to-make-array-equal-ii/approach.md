# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每个位置的差值必须是 `k` 的倍数。把正差和负差分别换算为单位数；总和守恒要求二者相等，所需操作数即正差单位数。`k=0` 时只能原数组已相等。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

差值不能整除 `k` 立即无解；`k=0` 单独避免除零。已验证两个题面样例。
