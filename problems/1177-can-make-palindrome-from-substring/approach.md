# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用 26 位掩码前缀异或记录每个字母的奇偶性。子串掩码的置位数是奇数频次字母数；每次替换能处理一对奇数频次字母，故只要 `odd_count // 2 <= k` 即可。

## 复杂度

预处理 `O(N)`，每个查询 `O(1)`，空间 `O(N)`。

## 边界条件与本地验证

- 单字符和至多一个奇数频次字母不需要替换。
- 本地覆盖题面示例。
