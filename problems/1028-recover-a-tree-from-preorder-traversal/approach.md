# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

顺序解析每个节点前的横线数量作为深度，再解析节点值。栈始终保存当前根到最新节点的路径；弹栈到长度等于新节点深度后，栈顶就是父节点。父节点左孩子为空时接到左侧，否则接到右侧。

## 复杂度

每个字符和节点至多处理常数次，时间 `O(n)`，路径栈空间 `O(h)`。

## 边界条件

节点值可能有多位；题目保证单子节点一定是左孩子；根节点深度为零。
