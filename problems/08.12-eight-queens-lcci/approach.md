# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

逐行放置皇后，用三个集合记录已占列、`row-column` 主对角线和 `row+column` 副对角线。合法位置加入后递归下一行，完成 N 行时生成棋盘字符串。

## 复杂度

搜索上界 `O(N!)`，递归及集合空间 `O(N)`，不计输出。

## 边界条件与本地验证

n=1 返回唯一棋盘；n=2/3 无解。官方 n=4 样例得到两种布局，并逐行检查列和对角线约束。
