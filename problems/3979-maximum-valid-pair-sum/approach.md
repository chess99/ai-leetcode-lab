# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举右端点 `j`。此时合法左端点范围为 `[0,j-k]`，滚动维护该范围内的最大数组值，与 `nums[j]` 相加更新答案。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

`k=n-1` 时只有首尾一对；验证两组样例及二次枚举对拍。
