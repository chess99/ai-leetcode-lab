# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

不发生长按时，原字符串就是 `word`，贡献一种方案。至多发生一次长按时，每一对相邻且相等的字符都对应一种删去该次重复输入后的不同原字符串，因此答案为 `1` 加上相邻相等对的数量。

## 复杂度

设字符串长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

没有相邻重复字符时答案为 `1`；连续重复字符会为每一个可缩短的位置各贡献一种方案。
