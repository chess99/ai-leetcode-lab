# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

排序后，`nums[i]` 作为最大值时可从左侧选至多 `k-1` 个，作为最小值时可从右侧选至多 `k-1` 个。预处理组合数 `C(t,r)` 的前缀和，便可在 `O(1)` 得到两类选择数并累加贡献。

## 复杂度

时间 `O(n log n + n)`，空间 `O(n)`。

## 边界条件与本地验证

验证 `k=1`、`k=n`、重复值和结果取模。
