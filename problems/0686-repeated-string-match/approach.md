# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先重复 `a` 直到长度不小于 `b`，检查是否包含；若跨越最后一次拼接边界，最多再多拼一次即可。两次都不包含则不可匹配。

## 复杂度

拼接和包含检查与构造字符串长度成正比，额外空间为重复串长度。

## 边界条件与本地验证

- 覆盖边界跨接匹配、完全匹配和不可匹配。
- 本地执行 `py_compile` 和最小断言。
