# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

固定移动后的横纵坐标可直接计数。每个通配符都能选择沿当前某个坐标的绝对值增大方向移动，使曼哈顿距离恰好增加 1，因此答案为固定终点距离加通配符数量。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

原点、单一方向和固定移动互相抵消均适用；验证两组样例并枚举通配符替换对拍。
