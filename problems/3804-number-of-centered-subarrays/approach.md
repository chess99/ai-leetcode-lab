# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举左端点并向右扩展，维护当前子数组元素和及出现值集合；若当前和存在于集合中就计数。`n≤500`，平方枚举足够。

## 复杂度

时间 `O(n²)`，空间 `O(n)`。

## 边界条件与本地验证

单元素子数组恒满足；负数和零均由整数集合直接处理。按题意保留中间变量，覆盖两组示例。
