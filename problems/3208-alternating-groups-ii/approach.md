# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将环形数组按需延长到 `n + k - 1` 个位置，维护以当前位置结尾的最长交替连续段。长度达到 `k` 时对应一个合法起点，累计所有 `n` 个环形起点。

## 复杂度

时间 `O(n+k)`，额外空间 `O(1)`。

## 边界条件与本地验证

相邻颜色相同会将当前交替长度重置为 1；通过取模处理跨越数组末尾的组。
