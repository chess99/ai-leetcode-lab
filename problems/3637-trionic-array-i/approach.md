# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

从左到右依次吞掉严格递增、严格递减、严格递增的三个连续段。每一段都必须至少包含一次相邻比较，且最终必须正好到达数组末尾。

## 复杂度

设数组长度为 `n`。每个元素最多参与一次扫描，时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

缺少任一上升或下降段都会返回 `False`；相等元素不属于严格单调段。
