# 解题记录

## 思路

尾随零由因子对 `2*5` 产生，而阶乘中 2 的数量更多，因此只统计因子 5。`n/5` 统计至少含一个 5 的数，`n/25` 补充额外一个 5，依次累加即可。

## 复杂度

循环次数为 `O(log₅ n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

当 `n<5` 时结果为 0；25 会贡献两个因子 5。已对 3 断言为 0、5 断言为 1、25 断言为 6。
