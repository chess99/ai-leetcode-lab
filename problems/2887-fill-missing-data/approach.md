# 解题记录

## 思路

对 `quantity` 列调用 `fillna(0)`，将其中的缺失值统一替换为 0，再返回处理后的表格。其他列和原有行顺序均保持不变。

## 复杂度

设表格有 n 行，时间复杂度为 O(n)，除 Pandas 生成中间列所需空间外，额外空间复杂度为 O(n)。

## 边界条件与本地验证

覆盖了没有缺失值、多个缺失值以及自定义索引的情况；本地使用 Pandas 2.2.3 独立加载并断言列顺序、索引和填充值。
