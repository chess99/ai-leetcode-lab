# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
记忆化搜索每个位置出发能访问的最大数量。向左右各检查至多 `d` 格；遇到第一个不低于当前位置的值就停止该方向，否则可跳过去并递归。

## 复杂度
每个位置计算一次且扫描 `2d`，时间 `O(nd)`，缓存和递归栈空间 `O(n)`。

## 边界条件
相等高度也会阻挡；不能越过阻挡点；至少能停留在起点，因此答案最小为 1。
