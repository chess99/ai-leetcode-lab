# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按替换位置从右向左处理。每次先验证 source 是否从指定下标开始，匹配才替换；从右向左不会改变尚未处理的左侧下标。

## 复杂度

排序和字符串构造时间 O(r log r + 总文本长度)，额外空间为结果字符串。

## 边界条件与本地验证

source 不匹配时保持原文；替换长度可变化；位置无序也正确。本地对示例和失配替换断言，并执行 py_compile。
