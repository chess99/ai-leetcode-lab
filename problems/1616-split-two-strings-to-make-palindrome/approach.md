# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
双指针比较一个串的前缀和另一个串的后缀，首个不匹配后只需检查任一剩余中段是否回文；交换两串重复一次。
## 正确性
匹配部分固定，任意合法切分的未匹配部分必须完整来自其中一个串，故中段回文是充要条件。
## 复杂度
时间 `O(n)`，空间 `O(1)`。
## 边界条件与本地验证
- 覆盖原串回文、需交换方向和单字符串。
