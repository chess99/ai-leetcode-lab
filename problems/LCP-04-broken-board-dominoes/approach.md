# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

棋盘按坐标奇偶染成二分图，每块骨牌对应一条相邻黑白格边，不重叠摆放等价于匹配。对所有完好黑格运行增广路算法求最大二分匹配。

## 复杂度

顶点不超过 64，时间 `O(VE)`，空间 `O(V+E)`。

## 边界条件与本地验证

坏格不加入图；单格棋盘返回 0。本地验证两个样例，并对小棋盘枚举骨牌集合对拍。
