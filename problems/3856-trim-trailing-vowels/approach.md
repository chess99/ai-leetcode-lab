# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

使用字符串尾部裁剪删除所有连续元音字符。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

覆盖全元音字符串。
## 思路

从字符串末尾向前跳过元音后截断。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

覆盖全元音字符串。
