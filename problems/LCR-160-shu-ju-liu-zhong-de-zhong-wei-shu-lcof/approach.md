# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护最大堆 `lower` 和最小堆 `upper`，保证下半部元素数等于上半部或多一。插入后重平衡；奇数个元素取下半部堆顶，偶数取两堆顶平均。

## 复杂度

单次插入 `O(log n)`，查询 `O(1)`，空间 `O(n)`。

## 边界条件与本地验证

负数通过取反存入最大堆；调用查询前题目保证已有元素。本地验证两个操作样例，并与排序数组中位数随机对拍。
