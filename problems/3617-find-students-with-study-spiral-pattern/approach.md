# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

按学生和日期排序，用窗口函数把相邻日期相隔超过两天的会话分段。每段计算不同学科数作为候选周期；按位置对周期取模，同一槽位必须始终对应同一学科，且记录数至少为两个周期，便构成合格螺旋学习模式。

## 复杂度

窗口排序主导，时间 `O(N log N)`，中间结果空间 `O(N)`。

## 边界条件与本地验证

不足六次、少于三科、断档超过两天和周期槽位不一致的记录都会被筛除。
