# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

并查集将可经由交换互相到达的下标归为连通分量。每个分量内的字符可任意重排，因此把下标和字符分别排序后依次匹配，即获得全局最小字典序字符串。

## 复杂度

时间复杂度为 `O(n log n + pα(n))`，空间复杂度为 `O(n)`。

## 边界条件与本地验证

- 不在任何交换对中的下标保持原字符。
- 间接连通的下标也必须合并。
- 本地验证了题目示例和无交换对情况。
