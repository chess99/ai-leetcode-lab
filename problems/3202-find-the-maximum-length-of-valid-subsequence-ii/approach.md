# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举相邻和对 `k` 的余数 `s`。`dp[s][r]` 表示末项余数为 `r` 的最长有效子序列；加入余数 `r` 的当前数时，前一项余数必须是 `(s-r) mod k`，据此转移。

## 复杂度

时间 `O(nk)`，空间 `O(k^2)`。

## 边界条件与本地验证

长度为 1 的子序列天然有效，作为状态起点；余数计算使用模运算避免负数。
