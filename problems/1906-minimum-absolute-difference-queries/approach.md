# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

构造值域 1 到 100 的频次前缀和。每个查询扫描存在的数值，比较相邻存在值的差。

## 正确性

任意最小绝对差必发生在排序后相邻不同值；前缀差精确判断区间内是否存在该值。

## 复杂度

预处理 `O(100n)`，每次查询 `O(100)`，空间 `O(100n)`。

## 边界条件与本地验证

只有一种值时返回 -1；已用样例验证。
