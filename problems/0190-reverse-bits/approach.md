# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

固定循环 32 次。每次取出 `n` 的最低位并接到结果的末尾：结果左移一位后与该最低位相或，再将 `n` 右移一位。

## 复杂度

循环次数固定为 32，时间 O(1)，额外空间 O(1)。

## 边界条件与本地验证

验证全零、仅最高位为 1，以及题目给出的两个示例；固定次数保证输入前导零也会被反转。
