# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
用显式栈保存数组及下一个访问下标，遇到子数组压栈，遇到整数立即 `yield`，避免深递归。
## 复杂度
时间 `O(N)`，栈空间 `O(D)`。
## 边界条件与本地验证
空数组直接结束；支持最大嵌套深度。已验证嵌套样例。
