# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

逐格检查两个题设条件：若下方格子存在，二者必须相等；若右方格子存在，二者必须不同。任一条件不满足即可返回 `False`，全部通过则返回 `True`。

## 复杂度

设网格大小为 `m x n`。时间复杂度为 `O(mn)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

单行只需检查水平方向，单列只需检查竖直方向；单个格子会直接通过。
