# 解题记录

## 思路

用两个栈保存光标左右字符；右栈顶是紧邻光标的字符。添加和删除只操作左栈，移动光标时在两栈间转移字符，返回左栈末尾至多十个字符。

## 复杂度

添加文本 `O(len(text))`，删除和移动 `O(k)`，总空间与当前文本长度成正比；每个字符移动一次只产生常数操作。

## 边界条件与本地验证

删除或移动超过边界时只处理已有字符，空左侧返回空串。题面完整操作序列通过；另以普通字符串和光标下标随机模拟 500 轮、每轮 200 次操作对拍。
