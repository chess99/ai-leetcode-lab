# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每个位置取每个可选数字的次数相同。设可选数字数为 `c`、数字和为 `S`，任一位置总贡献为 `S * c^(k-1)`，所有十进制位权之和是 `(10^k-1)/9`。用模快速幂计算三者乘积。

## 复杂度

时间复杂度 `O(log k)`，空间复杂度 `O(1)`。

## 边界条件与本地验证

覆盖允许前导零、只有一个可选数字及 `k` 很大。三组官方样例均通过。
