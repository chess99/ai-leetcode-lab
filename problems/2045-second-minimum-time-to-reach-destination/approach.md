# 解题记录

## 思路

所有边行驶时间相同、信号灯同步，因此走相同边数的路径到达时间相同。先用 BFS 为每个节点维护两个不同的最短边数，取得终点第二短边数；再逐边模拟，若出发时处于红灯就等到下一次变绿，然后增加固定行驶时间。

## 复杂度

每个节点只记录两种边数，每条边被处理常数次，时间 `O(V+E)`，空间 `O(V+E)`。

## 边界条件与本地验证

第二条路径允许重复顶点和边，但到达时间必须严格不同。题面样例得到 `13、11`；随机连通小图用按绝对时间的优先队列独立求每个点前两个到达时刻，对拍完成。
