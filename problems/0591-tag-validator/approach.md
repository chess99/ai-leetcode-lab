# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

扫描字符串并用栈维护嵌套标签；普通闭标签必须与栈顶一致，CDATA 仅允许位于已打开标签内且整体跳过。

## 复杂度

每个字符至多扫描常数次，时间 O(n)，栈空间 O(n)。

## 边界条件与本地验证

根标签关闭后不可再有内容；标签名限定 1 至 9 个大写字母。
