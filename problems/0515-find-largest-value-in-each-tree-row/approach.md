# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

广度优先逐层遍历，在处理一层的固定队列长度时累计最大值，再把左右孩子加入下一层队列。

## 复杂度

时间 `O(n)`，队列最坏空间 `O(n)`。

## 边界条件与本地验证

空树返回空数组，负数层也能正确求最大值。对 800 棵随机树与独立层序分组后逐层取最大值对拍通过。
