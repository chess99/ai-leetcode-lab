# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
动态规划按点从左到右构造线段集合，状态区分是否在当前点结束一条线段；通过前缀累加优化转移并对模数取模。
## 正确性
每组线段按其最后端点唯一归入状态，转移穷尽“延续/开始/结束”合法情形且不产生重叠内部。
## 复杂度
时间 `O(nk)`，空间 `O(k)` 或实现对应的 `O(nk)`。
## 边界条件与本地验证
- 覆盖 `k=1`、共享端点和模数取模。
