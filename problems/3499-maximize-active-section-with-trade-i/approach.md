# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

游程编码字符串。一次交易只能选择被两个零段夹住的一段一；将它清零后两个零段合并并翻成一。中间一最终也会恢复为一，所以净增量正好是左右零段长度之和。取最大增量即可。

## 复杂度

时间 `O(n)`，游程数组空间 `O(n)`。

## 边界条件与本地验证

没有可选的一段时保持原有一的数量；首尾零段不会误选。覆盖 `"01"`、`"0100"`、`"1000100"`。
