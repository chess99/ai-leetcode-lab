# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

共享当前数字与条件变量。每个线程只在当前数字符合自身规则时输出并递增，否则等待；递增后唤醒所有线程重新判断。结束时也唤醒所有等待线程。

## 复杂度

输出 `n` 次，时间复杂度为 `O(n)`，额外空间为 `O(1)`。

## 边界条件与本地验证

- 15 的倍数优先由 fizzbuzz 线程处理。
- 所有线程结束时均能被唤醒。
- 本地以四个线程验证了 `n=15` 的完整有序输出。
