# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:51Z

## 算法

另外两个原元素应取绝对值最大的两个数，再把被替换元素设为 `100000` 或 `-100000` 使总乘积为正；被替换位置可选择这两个下标之外的任一下标。

## 复杂度

时间 `O(n log n)`，空间 `O(n)`。

## 边界

数组恰有三个元素时仍有一个未选入数对的位置可替换；全零或只有一个非零值时答案可以为 0。
