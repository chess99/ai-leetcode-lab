# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用前缀和获得任意段的 1 数量。递归处理当前段：先计算不拆分费用；若长度为偶数，则还可以递归求两个等长半段费用之和并取较小值。允许的拆分结构固定，因此整棵递归树只有线性数量的节点。

## 复杂度

时间复杂度 `O(n)`，递归栈空间复杂度 `O(log n)`。

## 边界条件与本地验证

覆盖全零段、奇数长度不可拆、连续多层二分和不拆更便宜。三组官方样例均通过。
