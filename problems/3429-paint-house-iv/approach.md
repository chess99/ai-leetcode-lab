# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从两端向中间同时给一对对称房屋染色。状态记录上一对的左、右颜色；下一对选择两种不同颜色，并分别不同于同侧上一间颜色，从而同时满足相邻和对称限制。

## 复杂度

状态数固定为 9，时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

验证 `n=2`、中心相邻的一对、相邻约束与对称约束同时生效。
