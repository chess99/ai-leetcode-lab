# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

二分时若中点为空字符串，就在当前区间内向两侧寻找最近的非空字符串，再据其与目标的比较缩小区间。

## 复杂度

平均时间复杂度为 `O(log n)`，空字符串密集时最坏为 `O(n)`；额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

区间全为空时返回 `-1`；本地断言覆盖存在与不存在目标的样例。
