# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

深度优先遍历数组。遇到子数组且剩余可展开深度大于零时递归访问其元素，并将深度减一；否则把当前项直接加入结果，因而恰好展开不超过 `n` 层。

## 复杂度

访问每个可见元素一次，时间 `O(m)`，结果与递归栈合计空间 `O(m + n)`。

## 边界条件与本地验证

`n = 0` 时不会展开任何子数组。嵌套深度超过 `n` 的数组保留为数组，且遍历顺序与原数组一致。
