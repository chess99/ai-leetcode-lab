# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

扫描每个下标作为当前子数组末端。若当前元素等于前两个元素之和，当前斐波那契段长度加一；否则最长合法后缀重置为 2，持续更新全局最大值。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

题目保证长度至少 3，任意长度 2 的子数组都合法，所以初值为 2。验证三个题面样例；按题意保留中间输入变量。
