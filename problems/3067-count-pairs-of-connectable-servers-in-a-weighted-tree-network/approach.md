# 解题记录

## 思路
依次把每个节点当作中心。对中心的每个相邻分支 DFS，统计与中心距离能被 `signalSpeed` 整除的节点数；不同分支中的两点路径必经中心，因此将当前分支计数乘以前序分支累计值即可贡献配对数。

## 复杂度
每个中心遍历整棵树，时间复杂度 `O(n^2)`，递归栈空间 `O(n)`。

## 边界条件与本地验证
同一分支不能配对；中心自身不计入端点。已覆盖单链与星形树的手工断言。
