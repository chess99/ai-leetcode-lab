# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
把矩阵编码成位掩码，并预计算点击每格产生的异或掩码。从初始状态 BFS，第一次到达零状态即为最少翻转次数。

## 复杂度
最多 `2^(mn)` 个状态，每个状态尝试 `mn` 次翻转，空间 `O(2^(mn))`。

## 边界条件
初始全零返回 0；搜索耗尽仍未到达零则返回 -1；边缘格只影响存在的邻居。
