# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；Created：2026-08-11T22:15:56Z

## 思路

同一横线任选两点形成一条水平边。依次处理每个 `y` 的点数及其可选边数，与此前所有横线的可选边数相乘并累加，即枚举出每个水平梯形一次。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

同一横线少于两点没有贡献；按要求取模。已覆盖两个样例。
