# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
排序节点度数，对每个查询用双指针统计度数和大于阈值的点对。两点间的重复边会被度数和计算两次，再遍历实际相邻点对，扣除减去共享边后不再达标的误计。

## 复杂度
排序 $O(n\log n)$；每个查询 $O(n+e)$，空间 $O(n+e)$。

## 边界条件与本地验证
共享边按重数统计。已与随机多重图逐点对直接计算相连边并集对拍。
