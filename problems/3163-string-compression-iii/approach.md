# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从左向右扫描同字符连续段，每次最多取 9 个字符，将长度和字符追加到答案，然后继续处理剩余部分。

## 复杂度

时间 `O(n)`，空间 `O(n)`（输出）。

## 边界条件与本地验证

长度超过 9 的连续段必须拆分；单字符段编码为 `1` 加字符。
