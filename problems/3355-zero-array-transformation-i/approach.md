# 3355. 零数组变换 I

## 思路

## 算法

每个查询可对区间内每个下标独立选择减 1 或不减，因此位置 `i` 只需被至少 `nums[i]` 个查询覆盖。用差分数组统计全部查询的覆盖次数并逐位置比较。

## 复杂度

时间复杂度 `O(n+q)`，空间复杂度 `O(n)`。

## 边界

初始为 0 的位置不需要覆盖；区间右端用差分数组的 `right + 1` 位置撤销贡献。
