# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:52Z

## 算法

动态规划记录到每格、恰好使用某一花费时的最大分数，从上方和左方转移。值 0 的花费为 0，值 1/2 的花费为 1。到终点后取不超过预算的最大分数。

## 复杂度

有效花费最多为路径格数，时间 `O(mn*min(k,m+n))`，滚动行空间 `O(n*min(k,m+n))`。

## 边界

不可达状态用 `-1`；若终点所有预算状态都不可达则返回 `-1`。起点按题面值 0 初始化。
