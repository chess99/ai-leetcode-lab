# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

前 `n` 个正奇数之和为 `n²`，前 `n` 个正偶数之和为 `n(n+1)`。两者的最大公约数为 `n * gcd(n, n+1) = n`。

## 复杂度

时间复杂度和额外空间复杂度均为 `O(1)`。

## 边界条件与本地验证

`n=1` 时两和分别为 1 和 2，结果仍为 1。
