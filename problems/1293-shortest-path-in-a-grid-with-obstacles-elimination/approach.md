# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
BFS 状态包含位置和剩余消除次数。对同一格只保留历史最大的剩余次数，较差状态不可能产生更优后续路径。

## 复杂度
状态上界 `O(mnk)`，时间和空间均为该量级。

## 边界条件
若 `k` 足以覆盖曼哈顿最短路可直接返回；到达障碍格先消耗一次额度；无路返回 -1。
