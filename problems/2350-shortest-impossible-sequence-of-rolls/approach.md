# 解题记录

## 思路

从左到右贪心收集一整套 `1..k`。每收齐一套，就保证任意序列的下一个指定点数都能在此前选择之后找到，因此所有长度不超过完整套数的序列都是子序列；未完整的下一套中必缺某点数，构造重复该过程的序列即可证明下一长度存在不可能项。答案是完整套数加一。

## 复杂度

时间 `O(n)`，集合空间 `O(k)`。

## 边界条件与本地验证

末尾未收齐不增加完整套数；`k=1` 同样适用。题面样例得到 `3、2、1`；随机短 rolls 枚举所有点数序列并逐个检查子序列，对拍 500 组。
