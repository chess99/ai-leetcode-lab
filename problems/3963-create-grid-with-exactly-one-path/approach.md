# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将第一行和最后一列设为空格，其余位置设为障碍物。路径只能先沿第一行向右，再沿最后一列向下，因此恰好只有一条有效路径。

## 复杂度

构造时间复杂度为 `O(mn)`，输出空间复杂度为 `O(mn)`。

## 边界条件与本地验证

单行或单列时整条线均为唯一路径；本地断言覆盖了普通网格、单行和单列。
