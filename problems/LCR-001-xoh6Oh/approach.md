# 解题记录

## 思路

先记录结果符号并将两数转为非负数。每次找到不超过当前被除数的最大“除数的二倍幂倍数”，从被除数中减去它并累加对应商。重复直到不足一个除数。最后恢复符号，并单独处理 32 位最小值除以 -1 的溢出。

## 复杂度

设商的二进制位数为 `k`。每轮倍增和减法总计时间复杂度为 `O(k^2)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

结果向零截断，符号在最后统一处理；`-2^31 / -1` 返回 `2^31-1`。已对 `15/2`、`7/-3` 和溢出用例做最小断言。
