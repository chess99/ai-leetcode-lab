# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举矩形的上、下边界，把其间每列之和压缩成一维数组。对该数组用前缀和频次统计和为目标值的连续子数组数量，每个这样的子数组对应一个目标子矩阵。

## 复杂度

设矩阵为 `m×n`，时间 `O(m²n)`，列和与哈希表空间 `O(n)`。

## 边界条件

前缀和 0 初始出现一次；矩阵元素和目标值都可为负数；相同前缀和需要累计出现次数。
