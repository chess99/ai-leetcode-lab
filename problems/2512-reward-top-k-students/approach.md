# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将正、负反馈词分别置入集合。逐词计算报告分数，保存 `(-score, id)` 并排序，前 `k` 个的 ID 即为答案。

## 复杂度

设报告总词数为 `W`、学生数为 `n`，时间 `O(W + n log n)`，额外空间 `O(n + 词表大小)`。

## 边界条件与本地验证

无反馈词得 0 分；同分时元组排序保证较小 ID 在前。已验证两组题面样例。
