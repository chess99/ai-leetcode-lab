# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

重复执行 `n &= n - 1`，它会清除当前最低位的一个 1。每执行一次计数加一，直到 n 变为零。

## 复杂度

设 n 的二进制表示中有 k 个 1：时间 O(k)，额外空间 O(1)。

## 边界条件与本地验证

验证单个 1、多个分散的 1，以及 32 位范围内接近全为 1 的输入。
