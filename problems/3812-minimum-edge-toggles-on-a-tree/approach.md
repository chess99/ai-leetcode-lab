# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每条边翻转次数只需考虑奇偶。以 0 为根，从叶到根处理：若当前节点与目标不同，则其父边必须翻转，这是唯一选择，同时切换父节点需求。最后根仍有需求则无解；否则被选择边集合也是唯一且最短的答案，排序返回。

## 复杂度

时间复杂度 `O(n log n)`（排序），空间复杂度 `O(n)`。

## 边界条件与本地验证

覆盖无解、单条边和深树，使用迭代遍历避免递归栈。三组官方样例均通过。
