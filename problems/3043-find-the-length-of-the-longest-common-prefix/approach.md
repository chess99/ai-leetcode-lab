# 解题记录

## 思路
将 `arr1` 每个整数转成字符串，并把它的所有非空前缀放入集合。随后枚举 `arr2` 的前缀，命中集合时更新最大长度。

## 复杂度
设所有数字的十进制位数总和为 `D`，时间复杂度 `O(D^2)`（本题每个数字最多 9 位），空间复杂度 `O(D^2)`。

## 边界条件与本地验证
两数组首位均不相同返回 0；一个数字完全是另一个数字前缀时取较短长度。已覆盖 `[1,10,100]` 与 `[1000]`。
