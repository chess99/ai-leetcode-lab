# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

经过 `t` 秒后原串保留的后缀从位置 `t*k` 开始。若该后缀正好等于原串同长度前缀，新增字符可以补齐原串；枚举最小的正整数 `t`。移除长度达到串长时可直接重建原串。

## 复杂度

时间 `O(n^2 / k)`，额外空间 `O(1)`；本题 `n <= 50`。

## 边界条件与本地验证

只枚举正秒数。`k` 大于等于串长时第一秒即可完全重建。
