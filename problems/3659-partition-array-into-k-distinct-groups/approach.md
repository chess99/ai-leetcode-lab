# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

首先总元素数必须能被 `k` 整除，组数为 `n/k`。同一个值每组最多出现一次，所以其频次不能超过组数；反之若所有频次满足该上界，可将各值轮转分配到各组，条件也充分。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件

不可整除时立即失败；`k=1` 时总能分组。按题意保留中间输入变量。
