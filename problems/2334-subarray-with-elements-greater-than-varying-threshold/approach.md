# 解题记录

## 思路

单调递增栈确定每个元素作为区间最小值时能覆盖的最大范围。遇到更小元素时弹栈，当前下标是其右边界，弹栈后的新栈顶是左边界；若 `minimum*length>threshold`，该区间长度就是合法答案。末尾加入零哨兵清空栈。

## 复杂度

每个下标进出栈一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

判断必须严格大于阈值；任意一个合法长度都可返回。题面样例得到 `3、1`；随机数组枚举所有子数组建立合法长度集合，对拍 2000 组。
