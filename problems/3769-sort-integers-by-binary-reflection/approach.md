# 解题记录

## 思路

将二进制字符串反转后转回整数，按“反射值、原值”作为排序键。

二进制表示由 `bin(value)[2:]` 得到，反转后转回整数即为反射值。以二元组 `(反射值, 原值)` 排序，先满足反射值升序，并在反射值相同时让较小原值靠前。

## 复杂度

设位数为 `d`，时间 `O(n log n * d)`，空间 `O(n)`。

## 边界条件与本地验证

反转产生的前导零在转成整数时自然忽略；重复元素保持相同排序键。使用题目两个样例及二进制为 `100...0` 的数验证。
