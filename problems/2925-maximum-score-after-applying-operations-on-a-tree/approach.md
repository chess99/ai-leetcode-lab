# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

最大得分等于总和减去为保持健康必须保留的最小节点值和。对每个子树，要么保留当前节点值，要么清零当前节点并让每个子树各自保留一条健康路径，取二者较小；叶子必须保留自身。

## 复杂度

时间 `O(n)`，空间 `O(n)`（邻接表和递归栈）。

## 边界条件与本地验证

根可清零但每条根到叶路径仍须保留正值；叶子不能清零。
