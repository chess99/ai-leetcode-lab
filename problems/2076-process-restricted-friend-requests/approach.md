# 解题记录

## 思路

并查集维护当前好友群。处理请求 `(a,b)` 时取得两群的根；若已同群直接允许，否则扫描所有限制对，若某限制的两端当前根恰好分属这两个候选群，则合并会违反限制，拒绝请求；没有冲突才合并。

## 复杂度

设请求数 `q`、限制数 `r`，时间 `O(q*r*α(n))`，并查集空间 `O(n)`，符合 `q、r<=1000` 的限制。

## 边界条件与本地验证

限制约束会随群组合并传递；同群请求永远允许且无需修改。题面样例得到 `[真,假]` 和 `[真,假,真,假]`；随机小规模用显式集合维护群组逐请求对拍。
