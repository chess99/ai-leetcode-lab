# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每把叉子使用独立互斥锁，并额外限制最多四位哲学家同时尝试拿叉子，破除五人各持一把叉子的死锁环。

## 复杂度

每次进餐执行常数次同步，时间和额外空间均为 `O(1)`。

## 边界条件与本地验证

- 先取得两把叉子才调用 `eat`；本地以 5 个线程验证均可完成且回调顺序正确。
