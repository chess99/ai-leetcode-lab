# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

对候选第 `t` 个正偶数 `2t` 二分。通过 `nums` 二分位置和奇数前缀和，计算查询区间删除了多少个不超过它的偶数；剩余数量单调，故可二分。

## 复杂度

每个查询 `O(log n log(k+n))` 时间，预处理 `O(n)`。

## 边界条件与本地验证

题面样例输出 `[2,6,6]`，另与 1000 组小规模直接枚举对拍通过。
