# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

二维动态规划处理两个数组前缀。当前可跳过任一侧末项，也可把两项配对：单独用当前乘积开始非空子序列，或接在此前对角状态的正收益后。初值使用负无穷，确保全为负乘积时不会错误选择空子序列。

## 复杂度

时间和空间复杂度均为 $O(mn)$。

## 边界条件与本地验证

两侧子序列必须非空且等长；全负结果仍需返回最大负值。实际加载后验证样例，并枚举短数组的所有等长子序列下标组合对拍。
