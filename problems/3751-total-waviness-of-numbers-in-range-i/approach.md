# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:53Z

## 算法

题目范围上限为 `10^5`，直接枚举闭区间内每个整数。转成十进制字符串后，检查每个非首尾位是否严格大于两邻位（峰）或严格小于两邻位（谷）。

## 复杂度

设区间长度为 `R`、数字位数为 `D`，时间 `O(RD)`，空间 `O(D)`。

## 边界

不足三位的数字循环为空，波动值自然为 0；相邻数字相等不构成严格峰谷。
