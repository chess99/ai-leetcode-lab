# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

检查 n 的三进制表示。每个三的幂至多使用一次，等价于所有三进制位只能为 0 或 1。

## 正确性

三进制展开唯一，因此出现数字 2 时必需重复某个幂；反之所有位为 0/1 就直接构成合法和。

## 复杂度

时间 `O(log n)`，空间 `O(1)`。

## 边界条件与本地验证

`n=1` 合法，含三进制位 2 的数不合法。已用样例验证。
