# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

以最后一个值分组维护好子序列的数量和元素和。读到 `x` 时，只有以 `x-1` 或 `x+1` 结尾的旧序列能接上；将它们各自延长并附加单元素序列，再并入以 `x` 结尾的状态。状态始终只保留每个值的计数和总和。

## 复杂度

时间 `O(n)`，空间 `O(U)`，`U` 为不同数值数。

## 边界条件与本地验证

处理相邻值、重复值和负数。已对 100 组小数组枚举所有子序列对拍。
