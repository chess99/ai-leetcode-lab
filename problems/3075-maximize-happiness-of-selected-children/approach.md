# 解题记录

## 思路
把幸福值降序排序。第 `i` 个被选中的孩子会被前面 `i` 次选择各减 1，贡献为 `max(happiness[i]-i, 0)`，累加前 `k` 项。

## 复杂度
时间复杂度 `O(n log n)`，空间复杂度 `O(1)`（不计排序实现）。

## 边界条件与本地验证
负贡献按 0 截断；`k` 等于数组长度也可处理。已覆盖 `[1,2,3]，k=2`。
