# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将所有 `y` 放在开头、所有 `x` 放在结尾，其他字符保持原有相对顺序放在中间。这样每个 `y` 都位于每个 `x` 之前，同时字符计数不变。

## 复杂度

时间复杂度为 `O(n)`，输出空间复杂度为 `O(n)`。

## 边界条件与本地验证

当 `x` 或 `y` 未出现时条件自动满足；本地断言验证了排列一致性以及 `y` 全部位于 `x` 前的要求。
