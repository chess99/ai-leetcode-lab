# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先求全数组异或：非零时全选最优；为零时，若存在非零元素，删除任意一个非零元素后异或就等于该元素且非零，得到长度 `n-1`；若元素全为零，任何子序列异或都为零。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

单个非零元素返回 1，全部为零返回 0。验证两个题面样例以及全零数组。
