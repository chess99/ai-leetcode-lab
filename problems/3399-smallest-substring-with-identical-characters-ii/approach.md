# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

与第一题相同地二分最大连续段长度。长度为一时单独比较两种交替串的翻转数；更大长度时，连续段每隔 `L+1` 个字符改一次即可，累加 `run // (L+1)` 判断是否在操作预算内。

## 复杂度

时间 `O(n log n)`，空间 `O(1)`。

## 边界条件与本地验证

处理超长连续段、可通过交替串得到一的情况、零操作和字符串长度为一。
