# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:50Z

## 算法

原有位置转换的基础代价为对应元素差的绝对值之和。新增元素必须从某个原元素复制；该元素在变到自身目标值的路径区间内可免费经过新增目标值，否则需额外走到该目标值的最近端点。取所有位置的最小额外代价，再加一次追加操作。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界

新增目标落在某条转换区间内时额外修改代价为 0；`n=1` 同样适用。
