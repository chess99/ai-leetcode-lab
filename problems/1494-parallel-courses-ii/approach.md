# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用位掩码表示已经修完的课程。对每个状态找出所有先修课均已完成的可选课程，再枚举其中人数不超过 `k` 的非空子集作为本学期课程，更新新状态的最小学期数。

## 复杂度

最坏时间复杂度 $O(3^n)$，空间复杂度 $O(2^n)$。

## 边界条件与本地验证

一次可少于 `k` 门；题目保证依赖图可完成。已与小规模状态图 BFS 的最短层数对拍。
