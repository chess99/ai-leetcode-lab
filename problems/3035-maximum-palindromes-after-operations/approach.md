# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

所有字符可任意交换，回文串长度为 `L` 只需要 `L // 2` 对相同字符。汇总全局可用字符对，按字符串长度升序贪心分配，优先满足所需对数最少的字符串。

## 复杂度

统计总字符数为 `T`，时间 `O(T + n log n)`，额外空间 `O(26)`。

## 边界条件与本地验证

奇数长度的中心可使用任意剩余字符，不额外消耗字符对；资源不够时后续更长字符串也不可能满足。
