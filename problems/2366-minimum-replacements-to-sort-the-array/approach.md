# 解题记录

## 思路

从右向左维护当前元素各拆分块允许的最大值 limit。若值 x 超限，至少需 `ceil(x/limit)` 份；均匀拆分能使最小块尽量大，从而给更左元素最宽松的上界，新 limit 为 `floor(x/parts)`，操作数增加 `parts-1`。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

本就非递减时每步 parts 为 1；整除与非整除都用上取整处理。题面样例得到 `2、0`；随机小值数组枚举每个数的可能拆分份数做 DP 对拍 1000 组。
