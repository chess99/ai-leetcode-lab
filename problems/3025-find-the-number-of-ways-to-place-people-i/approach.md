# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举候选左上点 A 和右下点 B。坐标满足方向关系后，再扫描所有点；若矩形（含边界）中存在除端点外的点，则该对无效，否则计数。

## 复杂度

时间 `O(n^3)`，额外空间 `O(1)`；本题 `n <= 50`。

## 边界条件与本地验证

同一横线或竖线构成的退化矩形也需检查其线段上的点；端点本身不视为障碍。
