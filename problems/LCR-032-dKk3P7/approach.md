# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先排除两个字符串完全相同的情况（本题定义要求顺序不完全相同），再比较排序后的字符序列是否相同。

## 复杂度

时间复杂度为 `O(n log n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

相同字符串应返回 `false`，不同长度也不会匹配；本地断言覆盖了三个题目样例。
