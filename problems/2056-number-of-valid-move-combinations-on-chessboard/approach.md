# 解题记录

## 思路

为每个棋子枚举原地不动，或沿合法方向移动 1 到棋盘边界距离的所有方案。笛卡尔积枚举组合后，在整数时刻 0 到 7 计算各棋子位置：移动步数取 `min(time,distance)`，到终点后保持不动；任一时刻位置重合则组合非法。

## 复杂度

设第 `i` 个棋子方案数为 `c_i`，最多 4 个棋子；时间 `O(8*n*乘积(c_i))`，枚举器之外额外空间 `O(n)`。

## 边界条件与本地验证

包含原地不动、途中相遇、先到终点后被撞三种情况。单车和单后样例得到 `15、22`，两车对角起点得到 223；四皇后上限形态性能烟测约 4.3 秒。
