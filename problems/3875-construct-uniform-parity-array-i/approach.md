# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

答案始终为 `true`。若原数组全为偶数，直接保留所有元素即可。否则存在至少一个奇数：奇数元素保留自身；每个偶数元素减去这个奇数后变为奇数，从而可构造全奇数组。

## 复杂度

时间复杂度为 `O(1)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

全偶、混合奇偶和单元素数组均可构造；本地断言覆盖了这些情况。
