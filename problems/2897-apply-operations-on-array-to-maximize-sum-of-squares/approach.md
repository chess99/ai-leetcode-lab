# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

操作可以在数字间自由重新分配每一位的 1，总位频次不变。平方和偏好集中，因此连续构造 `k` 个数，每次尽量取走所有仍有剩余的位，得到按位不增的最大数序列。

## 复杂度

31 位逐数统计和构造，时间 `O(31(n+k))`，空间 `O(31)`。

## 边界条件与本地验证

高位可同时集中到同一个数，结果按模取值。题面样例得到 `261、90`；随机小位宽枚举合法位分配对拍。
