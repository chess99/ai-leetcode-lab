# 解题记录

## 思路

自底向上动态规划，`dp[i][j]` 表示以该格为正金字塔顶点的最大高度。若当前格为 1 且不在底边或左右边界，高度为下方左、中、右三格高度最小值加一；高度为 `h` 时贡献 `h-1` 个高度至少为 2 的金字塔。把网格上下翻转后再算一次得到倒金字塔。

## 复杂度

两次扫描均为 `O(mn)`，动态规划空间 `O(mn)`。

## 边界条件与本地验证

边界格最大高度为 1，不计入答案；全零及单行网格均为 0。题面样例均得到 2；随机最多 `7*7` 网格逐顶点、方向和高度直接检查所有必需格，对拍 800 组。
