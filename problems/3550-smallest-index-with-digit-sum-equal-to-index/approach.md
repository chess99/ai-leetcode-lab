# 解题记录

## 思路

按下标从小到大遍历数组，将当前整数转为十进制字符并求数位和。遇到数位和等于下标的位置立即返回，因此天然得到最小下标；遍历结束仍未找到则返回 -1。

## 复杂度

设数组长度为 n、每个数最多 d 位，时间复杂度 O(nd)，额外空间复杂度 O(d)。

## 边界条件与本地验证

下标 0 只有在元素数位和为 0 时才满足；验证了首项命中、中间命中、多项命中取最小以及无解情况。
