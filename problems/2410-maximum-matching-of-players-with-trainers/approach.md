# 2410. 运动员和训练师的最大匹配数

## 思路
排序后双指针扫描。当前训练师足够时匹配最弱未匹配运动员，否则丢弃该训练师。

### 正确性说明
能匹配时给最弱运动员使用当前最弱可用训练师不会损失更强运动员的机会；不能匹配时该训练师也无法匹配任何剩余运动员。故匹配数最大。

## 复杂度
时间 `O(n log n+m log m)`，额外空间 `O(1)`（不计排序）。

## 边界条件与本地验证
能力相等可以匹配；训练师不足时只匹配可行部分。
