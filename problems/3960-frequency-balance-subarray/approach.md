# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举左端点并向右扩展，维护“值的出现次数”和“每种出现次数对应多少个值”。只有一个不同值时合法；否则恰好需要两种频率，且较大者为较小者的两倍。每次加入元素只会改变两个频率桶，可常数时间判断。

## 复杂度

时间 `O(n^2)`，空间 `O(n)`。

## 边界条件与本地验证

特别区分“只有一种元素”和“多个元素但频率都相同”；验证三个样例及随机数组朴素对拍。
