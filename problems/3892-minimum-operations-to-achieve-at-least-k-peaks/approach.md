# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

峰值不能相邻，选中位置的独立代价是补到两侧最大值加一。枚举首位置选或不选，做“已选数量、前一位是否选”的链式 DP，处理环首尾冲突。

## 复杂度

`O(nk)` 时间，`O(k)` 空间。

## 边界条件与本地验证

处理 `k=0` 和不可能的 `k`；题面样例及随机不相邻集合枚举对拍通过。
