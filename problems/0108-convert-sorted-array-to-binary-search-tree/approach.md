# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

递归选择当前区间的中点作为根节点。左半区间构成左子树，右半区间构成右子树；原数组有序，因此保持二叉搜索树的大小关系，且两个子树规模至多相差一。

## 复杂度

时间 O(n)，递归栈空间 O(log n)。

## 边界条件与本地验证

单元素区间直接成为叶子；空区间返回 `None`。本地用奇数、偶数长度数组检查中序遍历有序且各节点左右高度差不超过一。
