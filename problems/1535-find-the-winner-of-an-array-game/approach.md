# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
顺序维护当前胜者及连胜次数；最大元素最终必胜。
## 正确性
每轮当前胜者只需与下一个尚未挑战的元素比较，较小者会移到队尾且在最大元素出现前不可能改变前端胜者。顺序扫描精确模拟这一过程；若无人提前达到 `k` 胜，全局最大值出现后永不失败，最终必为答案。
## 复杂度
时间 `O(N)`，空间 `O(1)`。
## 边界条件与本地验证
覆盖 k 很大时返回全局最大值。
