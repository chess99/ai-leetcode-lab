# 解题记录

## 思路

将 2 到 30 中不含平方质因子的数编码为十个质数的位掩码。用背包统计已选质因数集合；只有与当前掩码不相交的状态才能加入当前数，而同一数有其出现次数种下标选择。数值 1 可任意加入，先将空状态乘以 `2^count(1)`，最后排除只含 1 的子集。

## 复杂度

状态数为 `2^10`，时间 `O(30*2^10)`，空间 `O(2^10)`。

## 边界条件与本地验证

跳过含平方质因子的数，重复的合法非 1 数最多选一个。题面样例得到 `6、5`；随机小数组枚举全部下标子集并直接判断乘积是否无平方质因子，对拍 500 组。
