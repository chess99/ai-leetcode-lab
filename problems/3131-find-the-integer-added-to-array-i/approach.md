# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

两个数组每个元素都加上相同整数，故最小元素之间的差值就是该整数。

## 复杂度

时间复杂度 O(n)，额外空间复杂度 O(1)。

## 边界条件与本地验证

该整数可以为负数。已进行 Python 语法检查及样例断言。
## 思路

两数组排序后的最小元素差即为固定加数。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

覆盖负加数。
