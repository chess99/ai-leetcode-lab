# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
统计数字频次和总和模 3。余数为 1 时优先删除一个最小的余 1 数字，否则删除两个最小余 2 数字；余数为 2 对称处理。剩余数字降序拼接即最大值。

## 复杂度
数字种类固定为 10，时间 `O(n)`，空间 `O(1)`。

## 边界条件
无法组成时返回空串；剩余全是零时规范化为单个 `0`；删除必须优先选择较小数字。
