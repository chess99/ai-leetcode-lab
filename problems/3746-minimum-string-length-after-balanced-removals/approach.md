# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:52Z

## 算法

每次合法删除都等量减少 `a` 和 `b`，因此两者数量差的绝对值是不变量。通过不断删除相邻的不同字符，可消去 `min(count(a),count(b))` 对，最终恰好剩下该差值。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界

两类字符数相等时可全部删除；只有一种字符时无法删除任何字符。
