# 解题记录

## 思路

先计数所有位数短于 n 的无重复数字正整数。再沿 n 的十进制前缀逐位枚举更小且未使用的数字，后缀用剩余数字排列数填充；若 n 当前位已出现，后续不可能与 n 共前缀，直接结束，否则记录该位。完整走完时把 n 自身计入。

## 复杂度

十进制位数最多 10，时间 `O(d²)`，已用数字集合空间 `O(10)`。

## 边界条件与本地验证

首位不能为零，后续零可正常使用；n 自身有重复数字时不计。题面样例得到 `19、5、110`；对 1..499 及 500 个随机上界逐个检查所有整数数字是否互异，对拍一致。
