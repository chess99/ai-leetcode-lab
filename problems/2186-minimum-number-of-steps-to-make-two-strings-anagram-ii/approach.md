# 解题记录

## 算法、正确性与复杂度

统计每个字符在 `s` 与 `t` 中频次之差并求绝对值和。某字符若在一边多出 `d` 个，必须向另一边追加至少 `d` 个，且追加这些字符即可消除该差异；各字符独立，所以绝对差之和既是下界也是可达值。时间复杂度 `O(|s|+|t|)`，额外空间 `O(1)`（小写字母表）。

## 边界与验证

- 已互为异位词时所有差为 0。
- 一边完全缺少某字符时需补齐其全部频次。
- 用题面样例及完全相同、无公共字符的字符串断言验证。
