# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

逐个处理目标字符：先在当前前缀后添加 `a`，再依次递增最后一个字符直到目标字符，并记录每一步。

## 复杂度

时间和输出空间均为 `O(输出总长度)`。

## 边界条件与本地验证

覆盖单字符、`a` 和 `z`。
