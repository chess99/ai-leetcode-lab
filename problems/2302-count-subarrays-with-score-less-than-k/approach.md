# 解题记录

## 思路

数组全为正数，固定右端时分数随左端右移单调下降。滑动窗口维护元素和，分数不小于 `k` 时持续移出左端；恢复合法后，以当前右端结束的所有 `right-left+1` 个后缀窗口都合法。

## 复杂度

每个元素进入和离开窗口至多一次，时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

条件是严格小于；当单元素也超限时窗口可缩为空。题面样例得到 `6、5`；随机正数数组枚举全部子数组对拍 1500 组，十万规模约 0.02 秒。
