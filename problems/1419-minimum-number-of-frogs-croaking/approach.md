# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护处于 `c/r/o/a/k` 各阶段的青蛙数量。每个字符必须接在正确的前一阶段之后；完成 `k` 后释放该青蛙。过程中的最大活跃数量就是答案。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

- 缺少前序阶段或结束时仍有活跃青蛙均返回 -1。
- 验证了交错叫声、串行叫声和不完整叫声。
