# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

按绝对值从小到大处理计数，使每个值优先与其两倍配对；配对后扣减两倍值的剩余数量。零只能两两配对，因此单独要求出现次数为偶数。

## 复杂度

- 时间复杂度：O(n log n)。
- 空间复杂度：O(n)。

## 边界条件与本地验证

- 负数同样按绝对值排序，避免先消耗较大绝对值。
- 专门验证 `[0,0] -> true`、单个零为 false，并与小规模回溯配对 oracle 对拍。
