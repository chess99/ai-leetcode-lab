# 解题记录

## 思路

以对角线平方为主键、面积为次键取最大矩形。

矩形的对角线长度为 `sqrt(length² + width²)`。平方根保持大小关系，因此比较对角线平方即可避免浮点运算。Python 元组按字典序比较，将 `(对角线平方, 面积)` 取最大值，天然满足对角线相同时选择面积最大的规则。

## 复杂度

设矩形数量为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

只有一个矩形时直接返回其面积；对角线平方相同但面积不同的矩形会按面积正确决胜。使用题目样例和相同对角线的矩形组验证。
