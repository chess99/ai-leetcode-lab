# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

乘客和司机分别用 FIFO 队列。取消乘客时从活跃集合删除；匹配前惰性弹出队首已取消乘客，然后取两个队首配对。

## 复杂度

各操作摊还 `O(1)`，空间 `O(q)`。

## 边界条件与本地验证

取消已匹配乘客无影响；任一队列无有效对象则返回 `[-1,-1]`。添加乘客时保留隐藏变量，覆盖完整示例。
