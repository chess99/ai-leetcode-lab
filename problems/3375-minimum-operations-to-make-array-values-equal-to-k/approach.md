# 解题记录

## 思路

小于 k 的值无法增大而无解；每种大于 k 的不同值需要一次操作。

操作只能把当前最大的一种值整体降低，不能增加任何元素。因此若存在小于 `k` 的元素，必然无解。否则可以把大于 `k` 的不同值按降序逐层降到下一个值，最后降到 `k`；每个不同值恰对应一次操作。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

所有元素已经等于 `k` 时返回 `0`；包含小于 `k` 的值时返回 `-1`；重复的同一较大值只需一次操作。使用题目三个样例和这些边界验证。
