# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按位看所有三元组表达式，异或会消去成对出现的贡献，最终每一位只保留数组所有元素的异或。因此线性累积异或即可。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

单元素时直接返回该元素；重复元素依异或规则抵消。已验证题面样例和单元素情形。
