# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

递归地先将上方 `n-1` 个盘移到辅助柱，再移动最大盘到目标柱，最后将辅助柱的 `n-1` 个盘移到目标柱。

## 复杂度

时间复杂度为 `O(2^n)`，递归栈空间复杂度为 `O(n)`。

## 边界条件与本地验证

空柱无需移动；本地断言覆盖两个题目样例。
