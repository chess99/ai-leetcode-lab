# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用并查集逐边合并端点。若一条边的两个端点已在同一集合，加入它会形成环，它就是答案。

## 复杂度

路径压缩后时间近似 `O(n)`，并查集空间 `O(n)`。

## 边界条件与本地验证

- 首次检测到的成环边即题目要求的最后冗余边。
- 本地执行 `py_compile` 并断言两个典型环案例。
