# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先计算两字符串后缀的最长公共子序列长度。随后从左上角重建：相同字符只加入一次；不同时沿 LCS 较长的方向选择一个字符加入；某串耗尽后追加另一串剩余部分，所得即为一个最短公共超序列。

## 复杂度

DP 时间和空间均为 `O(mn)`，重建时间 `O(m+n)`。

## 边界条件

答案不要求唯一；任一输入为空时结果就是另一字符串；相同字符必须同步推进两个下标。
