# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1（本地预解，未发起远程测试或提交）

## 思路

数组元素为正数，双指针窗口右扩累加；一旦和达到目标就不断收缩左边界并更新最短长度。

## 复杂度

两个指针各单调前进，时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

- 无满足窗口返回 0。
- 达标后必须持续收缩以获得最短窗口。

本地断言示例、单元素和无解情况，并通过 `py_compile`。
