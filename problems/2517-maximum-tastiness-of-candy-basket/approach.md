# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

排序后对最小间隔二分。判定某个间隔时贪心选择最早能放入的价格，若能选满 `k` 个则该间隔可行。

## 复杂度

排序 `O(n log n)`，二分判定总计 `O(n log R)`，其中 `R` 为价格范围；额外空间 `O(1)`（不计排序）。

## 边界条件与本地验证

允许最小间隔为 0；`k=2` 也适用同一贪心。已验证题面样例。
