# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1
## 思路
展开所有配对异或时，`nums1` 每个元素恰好出现 `len(nums2)` 次，`nums2` 同理。偶数次异或抵消，故仅当对方数组长度为奇数时才将本数组全部元素异或进答案。
## 复杂度
时间 `O(n+m)`，额外空间 `O(1)`。
## 边界条件与本地验证
- 对方长度为偶数时整组贡献为 0。
- 已验证样例返回 13、0；两个数组均单元素时返回两者异或值。
