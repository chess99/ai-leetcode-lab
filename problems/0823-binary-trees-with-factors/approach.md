# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按值升序动态规划。每个值自身构成一棵树；枚举更小因子 a，若 b=value/a 也在数组中，则以 a、b 为左右孩子的组合数为 dp[a]*dp[b]，累加到当前值。

## 复杂度

时间 O(n²)，空间 O(n)。

## 边界条件与本地验证

有序处理确保因子状态已就绪；左右孩子顺序不同分别计数；结果取模。本地对示例和单元素断言，并执行 py_compile。
