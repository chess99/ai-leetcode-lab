# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:53Z

## 算法

所有结果恰好是十进制表示不含 0、且不大于 `n` 的正整数：任意原数删零后满足这一条件，反之该整数本身就在 `1..n`。先统计位数更短的 `9^len` 个数，再按位做不含 0 数字的字典序计数。

## 复杂度

时间 `O(log n)`，空间 `O(log n)`（十进制字符串）。

## 边界

遇到 `n` 的某位为 0 时，等长且同前缀的合法数无法继续，立即返回；若 `n` 本身无零则最后计入它。
