# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

闭包保存上一次计时器。每次调用先取消旧计时器，再创建一个在 `t` 毫秒后以本次参数调用原函数的新计时器；因此只有静默窗口内最后一次调用会执行。

## 复杂度

每次包装函数调用为 `O(1)` 时间和 `O(1)` 额外空间。

## 边界条件与本地验证

连续调用会持续推迟执行并保留最后一组参数。`t = 0` 时仍通过计时器异步调度，符合防抖接口语义。
