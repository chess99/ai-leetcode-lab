# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

递归携带“上一步向左/向右时的路径长度”。走向左孩子时只能延续此前向右路径，反之亦然，同时更新全局最大值。

## 复杂度

时间 `O(n)`，递归空间 `O(h)`。

## 边界条件与本地验证

- 单节点路径长度为 0。
- 本地验证了交替路径和单边链。
