# 解题记录

## 思路

至少两个偶数的按位或最低位才会为零。

按位或结果存在尾随零，等价于结果最低位为 `0`。只有参与运算的每个数最低位都为 `0`，结果最低位才为 `0`；又因为必须选择至少两个元素，所以只需判断数组中是否至少有两个偶数。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

只有一个偶数时不能单独选择它；两个或更多偶数时任选两个即可。使用全奇数、恰有一个偶数、恰有两个偶数以及题目样例验证。
