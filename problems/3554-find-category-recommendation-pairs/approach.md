# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

先将购买记录映射为“用户—类别”并去重。对同一用户的两条类别记录自连接，只保留字典序递增的类别对；分组统计用户数，筛出至少三人的组合并按要求排序。

## 复杂度

设用户类别去重记录为 `R`，自连接规模取决于单用户类别数，总体为 `O(R^2)` 的最坏情况。

## 边界条件与本地验证

同一用户购买同类别多个商品只计一次；严格比较类别名避免重复对与自身配对。
