# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

最小堆存储苹果批次的过期日和数量。每天加入新批次、清除过期批次，并优先吃最早过期的一颗。

## 正确性

选择最早过期苹果不会减少未来可吃数量；延后它可能直接腐烂，而较晚过期批次仍可留给之后。

## 复杂度

时间 `O(d log d)`，空间 `O(d)`，d 为处理天数。

## 边界条件与本地验证

苹果供应结束后仍需继续处理堆；过期日等于当天的苹果不可吃。已用题目样例验证。
