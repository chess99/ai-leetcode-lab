# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先全部放置 `a`，将剩余数值从右向左尽量填入，每位最多增加 25。

## 正确性

将较大的字符放在更靠右位置不会增大更早的字典序位，因此每步右侧贪心填满得到最小字符串。

## 复杂度

时间和空间均为 `O(n)`。

## 边界条件与本地验证

最小值 `k=n` 时全为 `a`；已用 `n=3,k=27` 得到 `aay` 验证。
