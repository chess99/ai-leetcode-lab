# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

最大堆每次取剩余最多且不同于前一字符的字符；前一字符暂存到下一轮再放回。最大频次超过 `(n+1)//2` 时无解。

## 复杂度

时间 `O(n log a)`，空间 `O(a)`，其中 `a` 为字符种类数。

## 边界条件与本地验证

- 不可重排时返回空串；本地 `py_compile` 和有效性断言通过。
