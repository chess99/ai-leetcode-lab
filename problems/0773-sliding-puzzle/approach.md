# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把 2×3 棋盘编码为长度 6 的字符串，预先列出空格在每个下标可交换的邻居。对状态图做 BFS，第一次到达 `123450` 的层数就是最少移动次数；搜索耗尽仍未到达则无解。

## 复杂度

最多 6! 个排列，每个状态分支数不超过 3，时间和空间均为 `O(6!)`。

## 边界条件与本地验证

初始即目标返回 0；访问集合避免来回交换；不可解排列返回 -1。
