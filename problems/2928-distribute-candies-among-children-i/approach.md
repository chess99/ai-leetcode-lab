# 解题记录

## 思路

枚举前两名孩子各自获得的糖果数，第三名的数量由 `n - first - second` 唯一确定。只要第三名的数量不超过 limit，就计入一种方案，避免无须必要的第三层枚举。

## 复杂度

时间复杂度 O(min(n, limit)²)，额外空间复杂度 O(1)。

## 边界条件与本地验证

循环范围保证三人的数量均非负；另行检查第三人的上限。验证了无可行分配、上限足够大以及题目两个样例，并与三重枚举 oracle 核对。
