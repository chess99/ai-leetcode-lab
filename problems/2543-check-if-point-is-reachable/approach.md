# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

四种操作不会给两坐标最大公约数引入除 2 以外的新质因子；反向利用辗转相减与除 2，可以到达 `(1,1)` 当且仅当 `gcd(x,y)` 是 2 的幂。

## 复杂度

时间 `O(log min(x,y))`，空间 `O(1)`。

## 边界条件与本地验证

最大公约数为 1 也属于 2 的零次幂。题面样例为假、真；小坐标正向 BFS 可达集合对拍。
