# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

因横坐标递增，式子可写成 $(y_i-x_i)+(x_j+y_j)$。扫描右端点时，双端队列维护距离范围内最大的 `y_i-x_i`，并按该值单调递减；队首就是当前最优左端点。

## 复杂度

每个点至多进出队一次，时间复杂度 $O(n)$，空间复杂度 $O(n)$。

## 边界条件与本地验证

先移除横坐标差超过 `k` 的点，再计算答案。已与随机有序点集的全部合法点对枚举对拍。
