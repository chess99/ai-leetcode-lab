# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

令 `dp[l][r]` 为把 `s[l..r]` 变成回文串所需的最少删除数。两端相等时继承内部状态，否则删除左端或右端之一。按左端倒序用一维数组滚动，找最少删除数不超过 1 的最长区间。

## 复杂度

时间 `O(n^2)`，空间 `O(n)`。

## 边界条件与本地验证

长度为 2 的子串可删除一个字符；题意要求恰好删除一个字符，任意回文子串也可删去一端后仍为回文。
