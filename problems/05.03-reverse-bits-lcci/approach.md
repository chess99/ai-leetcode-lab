# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

扫描 32 个比特，记录当前连续 `1` 长度和上一个连续 `1` 长度。遇到零时，只有其下一位为一才可将两段通过翻转该零连接。

## 复杂度

时间复杂度为 `O(1)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

全一时答案为 32；本地断言覆盖两个题目样例和全一输入。
