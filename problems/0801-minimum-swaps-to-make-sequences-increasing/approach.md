# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护处理到当前位置且当前位置“不交换/交换”时的最少交换数。若两条序列各自与前一位递增，可从相同交换状态转移；若交叉比较也递增，可从相反交换状态转移。每步只依赖上一位置，用两个变量滚动。

## 复杂度

单次扫描，时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

首位置不交换代价为 0、交换代价为 1；两类递增条件可能同时成立，需要对转移结果取最小值。
