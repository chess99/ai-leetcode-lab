# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

原地从左向右更新数组；更新到位置 `i` 时，左侧已经是本秒前缀和，执行 `a[i] += a[i-1]` 正好得到新值。重复 `k` 次。

## 复杂度

时间 `O(nk)`，额外空间 `O(n)`。

## 边界条件与本地验证

首项始终为 1；每次加法立即取模以避免数值膨胀。
