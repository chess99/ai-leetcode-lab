# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护当前前缀除以 `m` 的余数。读入新数字后，新前缀余数为 `(旧余数 * 10 + 当前数字) % m`；余数为零时对应位置填 `1`，否则填 `0`。全程无需构造大整数。

## 复杂度

时间复杂度 `O(n)`，除答案数组外空间 `O(1)`。

## 边界条件与本地验证

可处理前缀为零以及很长的字符串；每一步立即取模，避免数值溢出。
