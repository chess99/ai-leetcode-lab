# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；Created：2026-08-11T22:15:56Z

## 思路

从小到大加入边并维护连通分量数。阈值为 `w` 时，只保留权值不超过 `w` 的边能得到的分量数不大于 `k`，便可再删边切成恰好 `k` 个分量；首次达到 `k` 的边权即答案。

## 复杂度

排序后并查集扫描，时间 `O(m log m)`，空间 `O(n)`。

## 边界条件与本地验证

`k=n` 时答案为 0；已覆盖两个样例。
