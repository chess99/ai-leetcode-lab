# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

除朋友数为 1 外，任意分段的最大长度固定为 `n - numFriends + 1`；枚举每个起点的该长度子串，取字典序最大者即可。

## 复杂度

时间 `O(n^2)`，额外空间 `O(n)`；题目 I 的长度上限允许直接枚举。

## 边界条件与本地验证

验证单个朋友、长度为一的片段，以及最大候选位于串首或串尾。
