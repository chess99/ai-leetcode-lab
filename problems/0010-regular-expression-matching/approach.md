# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

dp[i][j] 表示 s 前 i 个字符能否与 p 前 j 个字符完全匹配。普通字符和点号只看左上状态；星号可匹配零个前元素，或在当前字符相符时消耗一个字符并保持模式位置。

## 复杂度

时间 O(|s||p|)，空间 O(|s||p|)。

## 边界条件与本地验证

空串初始化允许 a*b*c* 这类模式匹配；星号前始终有有效字符由题目保证。
