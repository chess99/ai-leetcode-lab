# 解题记录

## 思路

答案是与 n 位数相同长度的全一二进制数。

设 `n` 的二进制长度为 `b`。所有少于 `b` 位的全一数都小于 `n`，而 `b` 位全一数 `2^b - 1` 一定不小于 `n`，并且是满足条件的最小值。

## 复杂度

使用整数位长运算，时间复杂度和额外空间复杂度均为 `O(1)`。

## 边界条件与本地验证

当 `n` 本身已经全为置位位时，公式仍返回 `n`；跨越二进制位数边界时会返回下一位数的全一值。使用 `n=1、3、5、10` 验证。
