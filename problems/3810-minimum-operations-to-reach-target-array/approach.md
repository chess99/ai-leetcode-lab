# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

一次选择值 `x` 会同时更新当前所有值为 `x` 的段；已经等于目标的位置即使被包含也保持不变。因此每种在错位位置出现的当前值恰需选择一次，且选择不同值彼此不替代，答案为这些值的不同数量。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

完全相等返回 0；同一当前值分散在多个段仍由一次操作处理。按题意保留中间变量，覆盖三组示例。
