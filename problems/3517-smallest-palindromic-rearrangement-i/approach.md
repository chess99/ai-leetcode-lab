# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计字符频次，将每个字符的一半按字典序放在左侧；唯一的奇数频字符放在中心，再镜像左侧。最小的左半部分保证整体字典序最小。

## 复杂度

时间 `O(n)`，空间 `O(n)`（返回字符串）。

## 边界条件与本地验证

长度为一时左右半为空；输入保证为回文，因此至多一个奇数频字符。覆盖 `"z"`、奇数和偶数长度。
