# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护当前元素在某段中取正号或负号时的最大总成本。取正号可新开一段，或接在任意已完成状态后；取负号只能接在前一元素取正号的同一段后。常数状态滚动更新。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

单元素数组直接返回该值；使用负无穷初始化非法的负号状态。
