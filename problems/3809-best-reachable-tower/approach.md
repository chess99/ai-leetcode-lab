# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

线性枚举塔并检查曼哈顿距离。对可达塔使用 `(-质量,x,y)` 作为比较键，最小键即质量最大且坐标字典序最小。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

距离等于半径仍可达；没有候选返回 `[-1,-1]`。覆盖质量并列和无可达塔。
