# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

同一条对角线上的坐标满足 `row + column` 相等。逐条从左下方向右上方收集元素，偶数编号保持该方向，奇数编号反转，从而得到题目要求的交替遍历顺序。

## 复杂度

每个元素访问一次，时间 `O(mn)`；对角线临时数组最坏 `O(min(m,n))`，不计返回数组。

## 边界条件与本地验证

覆盖单行、单列和非方阵。对 1000 个随机矩阵用独立坐标分组规则生成期望顺序，对拍通过。
