# 2375. 根据模式串构造最小数字

## 算法

将数字 `1` 到 `n+1` 依次压栈。遇到 `I` 或扫描结束时清空栈加入答案，连续 `D` 段会因此以降序输出。

## 正确性说明

每段连续 `D` 必须递减，反转最小的一批连续可用数字满足条件且字典序最小；在 `I` 处立即输出较小数字同样最优。故构造结果满足模式且字典序最小。

## 复杂度分析

每个数字进出栈一次，时间复杂度为 `O(n)`，空间复杂度为 `O(n)`。

## 边界情况

- 全 `D` 时输出降序。
- 全 `I` 时输出升序。
- 数字不会超过 9。
