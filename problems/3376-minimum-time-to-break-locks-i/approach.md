# 3376. 破解锁的最少时间 I

## 算法

用位掩码表示已打开的锁。状态中已开锁数唯一决定当前增长因子 `1 + 已开锁数*k`；枚举下一把锁，等待时间为 `ceil(strength / 因子)`，做最小化转移。

## 复杂度

时间复杂度 `O(n*2^n)`，空间复杂度 `O(2^n)`。

## 边界

初始增长因子为 1；每次开锁后能量重置，故不同锁之间的等待时间直接相加。
