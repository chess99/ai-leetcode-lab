# 解题记录

## 思路

累积每一步坐标并统计回到零的位置。

蚂蚁从边界位置 `0` 出发，数组中的正负数分别表示向右和向左移动。依次把每一步加到当前位置；一次移动结束后位置等于 `0`，就记录一次返回边界。

## 复杂度

设移动次数为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

初始时刻不属于“移动后返回”，不能预先计数；连续多次回到零时每次都应计入。使用题目样例、从不返回以及多次返回的序列验证。
