# 解题记录

## 思路

调用 `pivot`，指定 `month` 为行索引、`city` 为新列、`temperature` 为单元格值，从而把长表转换为题目要求的宽表。

## 复杂度

设输入有 n 行，构造透视表的时间复杂度为 O(n)，输出与辅助空间复杂度为 O(n)。

## 边界条件与本地验证

题目保证每个“月份—城市”组合唯一，因此不会因重复键而失败。本地使用 Pandas 2.2.3 验证了官方样例等价数据及单城市数据，并检查索引名和列名。
