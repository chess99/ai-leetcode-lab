# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

每行从右向左模拟重力，石头落到障碍物前的最右空位；完成后顺时针旋转矩阵。

## 正确性

扫描时写指针始终是当前障碍物分段内最右空位，因此每块石头落到唯一正确位置；旋转后即为竖直重力效果。

## 复杂度

时间 `O(mn)`，输出空间 `O(mn)`。

## 边界条件与本地验证

障碍物重置写指针；空格与无石头行均适用。已用样例验证。
