# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

区间状态记录当前玩家相对另一玩家可取得的最大分差。选择左端或右端后，下一状态的分差要从当前所得分数中扣除；按区间长度递增计算，并压缩成一维数组。最终分差非负表示先手至少平局。

## 复杂度

时间 `O(n²)`，空间 `O(n)`。

## 边界条件与本地验证

单元素必胜，平局也返回真。对 1000 组随机短数组与独立记忆化 minimax 对拍通过。
