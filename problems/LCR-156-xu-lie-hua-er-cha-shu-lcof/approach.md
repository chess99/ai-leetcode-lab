# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

采用层序格式，空孩子记作 `#`，序列化末尾多余空标记可删除。反序列化按队列依次给每个父节点填充左右孩子。

## 复杂度

时间和空间均为 `O(n)`。

## 边界条件与本地验证

空树编码为 `#`；支持负数和单侧深链。本地用随机树验证序列化—反序列化往返一致。
