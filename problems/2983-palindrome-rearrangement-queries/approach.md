# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
## 思路
将右半串反转，与左半串按回文对应位置对齐。失配前缀和先判断两个可重排区间并集之外是否仍有不相等位置；字符前缀计数再扣除只被单侧区间覆盖时必须匹配的固定镜像字符，最后比较两侧重叠区域剩余字符多重集。
## 复杂度
预处理 O(n·26)，每次查询 O(26)。
## 边界条件与本地验证
覆盖区间相离时必须检查中间固定区域，包含或交叠时需避免重复扣除交集。题面样例及长度至 8 的小串全排列 oracle 通过。
