# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

只有当前值不等于目标下标的位置需要参与交换。可行的最大 `k` 等于这些错位值的按位与：每个需移动值都必须包含 `k` 的所有位，而该按位与也能通过交换连通错位部分。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件

已排序时题目规定返回 0；错位值含 0 时答案自然为 0。覆盖三组示例。
