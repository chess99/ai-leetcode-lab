# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把列名视为没有零位的 26 进制数。按从左到右扫描，每次先将已有结果乘以 26，再加上当前字母对应的值（`A` 为 1，`Z` 为 26）。

## 复杂度

设列名长度为 n：时间 O(n)，额外空间 O(1)。

## 边界条件与本地验证

单字符 `A` 和 `Z` 分别得到 1、26；多字符 `AA` 和 `ZY` 验证进位与累积顺序。
