# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
先求原数组价值。反转内部区间只改变两条边，其最大增益为所有相邻对较小值的最大值与较大值的最小值之差的两倍；另行枚举反转触及首端或末端时的单边增益。

## 复杂度
数次线性扫描，时间 `O(n)`，空间 `O(1)`。

## 边界条件
长度小于 2 时价值为零；增益至少为零，可选择长度 1 的反转。
