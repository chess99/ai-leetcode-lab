# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

滑动窗口用两个单调队列维护最大、最小值。固定右端点时，左端点右移会使极差和长度均不增，因此合法性单调；收缩到成本不超过 `k` 后，以当前右端结束的全部后缀窗口都合法。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

单元素成本恒为 0；重复值通过非严格弹栈保留最新下标。保留隐藏变量，并与平方暴力随机对拍。
