# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

用二维计数表记录每位玩家获得的每种颜色球数。遍历每位玩家，只要任一颜色的数量严格大于玩家编号，就将该玩家计入答案。

## 复杂度

设领取记录数为 `p`、玩家数为 `n`。颜色范围固定为 11，时间复杂度为 `O(p + n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

玩家 0 获得任意一个球就满足条件；计数必须严格大于编号，等于编号不算获胜。
