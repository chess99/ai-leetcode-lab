# 解题记录

## 思路

先统计字符串中每个字符的出现次数，再分别在元音集合和辅音集合中取最大频次。题目要求的是两个最大频次之和，某一类不存在时其贡献记为 0。

## 复杂度

设字符串长度为 n，时间复杂度 O(n)，额外空间复杂度 O(1)，因为仅有 26 种小写字母。

## 边界条件与本地验证

覆盖了只有元音、只有辅音、两类都有以及同类多字符并列最高频的情况；并与直接对每个字母调用计数的参考实现核对。
