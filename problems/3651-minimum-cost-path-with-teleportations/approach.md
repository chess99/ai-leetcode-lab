# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
## 思路
按已用传送次数分层做右下路径 DP。每层完成普通转移后，按格子值降序扫描，维护可传送来源的最小代价并免费写入下一层。
## 复杂度
时间 `O(kmn log(mn))`，空间 `O(kmn)`。
## 边界条件与本地验证
传送仅能到不大于当前值的格；两个题面样例通过。
