# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

初始化时建立“元素值到坐标”的映射。查询时先定位该值，再按四个正交方向或四个对角方向枚举相邻格，只累加仍在边界内的格子。

## 复杂度

设网格边长为 `n`。初始化时间和空间复杂度均为 `O(n^2)`；每次查询只检查四个位置，时间复杂度为 `O(1)`。

## 边界条件与本地验证

边缘与角落缺少的相邻位置会被边界判断忽略。元素唯一性保证坐标映射没有冲突。
