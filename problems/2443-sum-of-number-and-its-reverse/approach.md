# 2443. 反转之后的数字和
## 思路
枚举 `0` 到 `num` 的每个 `k`，判断 `k + reverse(k)` 是否等于目标。
### 正确性说明
非负解不可能大于 `num`，算法枚举全部可能值，因此存在解时必找到，不存在时返回假。
## 复杂度
时间 `O(num log num)`，空间 `O(1)`。
## 边界条件与本地验证
`num=0` 时 `k=0` 可行；反转后的前导零不影响整数值。
