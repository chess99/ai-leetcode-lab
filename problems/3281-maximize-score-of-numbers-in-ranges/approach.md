# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

排序左端点后二分最小间距。对候选间距从左到右贪心选择每段中可行的最小整数，若越过右端点则失败。

## 复杂度

时间 `O(n log n + n log R)`，空间 `O(1)`（不计排序）。

## 边界条件与本地验证

覆盖区间重叠、`d=0` 和示例 `[6,0,3], 2`，结果为 `4`。
