# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

从坐标 `(0, 0)` 开始模拟命令：上下改变行号，左右改变列号。最后根据题目编号规则计算 `row * n + col`。

## 复杂度

设命令数为 `k`。时间复杂度为 `O(k)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

题目保证蛇始终不会越界；空移动后的初始位置编号为 `0`。
