# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

线性扫描连续相同字符，记录每段字符及长度并拼接压缩结果。仅当压缩后字符串更短时返回它。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

空串直接返回，压缩不变短时保留原串；本地断言覆盖两个样例。
