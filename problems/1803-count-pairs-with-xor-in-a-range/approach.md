# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
用二进制字典树计算此前数字中与当前值异或小于 `limit` 的数量。逐位根据 `limit` 的位决定可直接计入的同位分支与需继续比较的异位分支。答案是小于 `high+1` 减去小于 `low`。

## 复杂度
固定 16 位，时间 $O(16n)$，空间 $O(16n)$。

## 边界条件与本地验证
先查询再插入，避免自配对及重复计数。已与随机数组全部下标对直接异或对拍。
