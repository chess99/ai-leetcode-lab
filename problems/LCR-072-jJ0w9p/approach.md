# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

在 `0` 到 `x` 之间二分。若中点平方不超过 `x`，记录其为当前可行值并继续向右搜索；最终返回最大可行值。

## 复杂度

时间复杂度为 `O(log x)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

`x = 0` 和非完全平方数均可正确处理；本地断言覆盖了两个样例及零。
