# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先计算旋转 0 的得分。对每个元素推导它随 `K` 增加时从得分变为失分、再因环绕恢复得分的两个边界，在环形差分数组中记录分数变化。顺序累加即可得到每个旋转的分数，并只在严格更高时更新答案以保留最小下标。

## 复杂度

每个元素贡献常数次差分，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

变化区间跨越下标 0 时需补记初始差分；并列最高分不更新答案。
