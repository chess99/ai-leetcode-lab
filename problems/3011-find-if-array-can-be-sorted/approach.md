# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

相邻且置位数相同的连续段内部可任意重排，不同段无法跨越。依次扫描每段，若当前段最小值小于前面所有段的最大值，则无法整体有序；否则继续。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

单元素段也要检查。数组元素均为正数，因此初始前缀最大值设为 0 安全。
