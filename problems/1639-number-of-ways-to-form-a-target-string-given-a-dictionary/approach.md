# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
统计词典每一列各字符出现次数。顺序扫描列，用倒序一维 DP 决定跳过该列或用该列匹配目标的下一个字符。

## 复杂度
统计 `O(WL)`，DP `O(LT)`，空间 `O(L字母表+T)`。

## 边界条件与本地验证
列只能从左到右使用；倒序更新避免同一列被重复选择；结果取模。
