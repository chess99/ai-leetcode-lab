# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
逐位构造答案。以 `H` 开头的剩余路径数由组合数得到；若 `k` 落在这部分就选 `H`，否则减去该数量并选 `V`。

## 复杂度
构造 `O(H+V)` 次组合数运算，空间 `O(H+V)`。

## 边界条件与本地验证
字典序中 `H<V`；某类步数耗尽后只能选择另一类；`k` 从 1 开始。
