# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

区间 DP 判断子串能否完全删除；随后从右向左计算每个后缀可得到的最小字符串。当前首字母可保留，或与某个可在删除中相邻的字符配对删去，取所有结果字典序最小者。

## 复杂度

时间 `O(n^3)`，空间 `O(n^2)`；`n<=250`。

## 边界条件与本地验证

支持字母环形相邻的 `a-z`。5000 组短串与所有删除顺序枚举一致。
