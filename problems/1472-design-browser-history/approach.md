# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

数组保存历史并记录当前位置。访问新页时截断当前位置后的前进历史；后退和前进将位置限制在数组边界内。

## 复杂度

后退和前进为 `O(1)`；访问因截断为 `O(n)`，空间为历史长度。

## 边界条件与本地验证

访问新页必须删除前进记录；步数超过边界时停在首尾。对 500 个实例各执行 3000 次随机操作，与列表加当前位置的模型逐次对照。
