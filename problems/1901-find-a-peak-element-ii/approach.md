# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

二分行，取中间行最大元素。若上或下同行元素更大，峰必在对应半区，否则该位置为峰。

## 正确性

行内最大值已大于左右邻居；若上下都不更大即满足四邻峰条件，向更大邻行移动不会丢失峰。

## 复杂度

时间 `O(n log m)`，空间 `O(1)`。

## 边界条件与本地验证

首尾行只有一个纵向邻居；已用样例验证。
