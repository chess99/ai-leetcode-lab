# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用两个变量滚动维护连续两个斐波那契数，每次更新为它们之和并取模，迭代 `n` 次后得到答案。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

`n = 0` 时返回 `0`；本地断言覆盖了三个题目样例和取模范围。
