# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按行动态规划。到达当前格的最小路径和等于当前值加上一行中左上、正上、右上（存在者）的最小路径和。只保留上一行的 DP 结果，最后取末行最小值。

## 复杂度

矩阵边长为 `n`。时间复杂度 `O(n^2)`，滚动数组额外空间 `O(n)`。

## 边界条件与本地验证

- 首尾列只有两个可选前驱。
- 矩阵可含负数，不能用非负哨兵。
- 验证了题目两个示例、单元素矩阵和含负路径用例。
