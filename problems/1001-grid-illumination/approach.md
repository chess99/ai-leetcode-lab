# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用集合保存去重后的亮灯坐标，并分别统计每行、每列、两类对角线上的灯数。查询点只需检查四个计数是否任一非零；随后枚举该点周围九宫格，把仍开启的灯从集合和四类计数中删除。

## 复杂度

初始化 `O(L)`，每次查询固定检查和删除至多 9 个位置，时间 `O(L+Q)`，空间 `O(L)`。

## 边界条件

重复灯坐标只计一次；九宫格枚举需裁剪到网格边界；查询结果在关灯前计算。
