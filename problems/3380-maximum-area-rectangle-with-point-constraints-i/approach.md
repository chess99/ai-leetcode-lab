# 3380. 用点构造面积最大的矩形 I

## 思路

## 算法

数据规模很小。枚举两条不同的横坐标和两条不同的纵坐标，检查四个角是否存在；再扫描全部点，若有非角点落在矩形内部或边界上则淘汰，剩余矩形更新最大面积。

## 复杂度

点数为 `n`，时间复杂度 `O(n^5)`，空间复杂度 `O(n)`；本题 `n<=10`。

## 边界

边界上的额外点同样不合法；没有合法矩形时保持返回 `-1`。
