# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

两个等长字符串互为旋转，当且仅当第二个字符串是第一个字符串自拼接后的子串。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

长度不同直接返回假，两个空串互为旋转；本地断言覆盖两个样例。
