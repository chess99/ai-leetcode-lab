# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

提取所有非负元素，按其数量对 `k` 取模并左旋，再依次写回原先的非负位置；负数原位保留。

## 复杂度

时间与空间均为 `O(n)`。

## 边界条件与本地验证

没有非负元素时避免模零并原样返回；单个非负元素旋转不变。保留隐藏变量，覆盖三组示例。
