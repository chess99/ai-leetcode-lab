# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

只需判断能否一步吃后：车与后同行或同列，且象不在两者之间；象与后同一对角线，且车不在两者之间。任一成立返回 1，否则总能先移动一枚棋子创造攻击线，返回 2。

## 复杂度

时间 `O(1)`，额外空间 `O(1)`。

## 边界条件与本地验证

阻挡只在阻挡棋子严格位于攻击者和皇后之间时生效；端点不可能重合。
