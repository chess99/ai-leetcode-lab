# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

使用 Boyer-Moore 投票法。不同元素两两抵消，出现次数超过一半的元素无法被完全抵消，最终候选值即为答案。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

题目保证存在多数元素；本地断言覆盖了题目样例和单元素数组。
