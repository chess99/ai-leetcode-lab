# 解题记录

## 思路

忽略大小写后统计相邻字符变化次数。

按键是否变化只由相邻两个字符对应的字母决定。逐对比较相邻字符的小写形式，不相等就说明发生了一次按键变化；首字符只是初始按键，不计为变化。

## 复杂度

设字符串长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

同一字母的大小写切换不算更换按键；长度为 `1` 时没有相邻字符，答案为 `0`。使用题目样例以及全为同一字母但大小写交替的字符串验证。
