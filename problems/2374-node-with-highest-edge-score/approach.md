# 2374. 边积分最高的节点

## 算法

遍历每个源节点 `i`，将 `i` 加到其目标节点的积分中。最后从小到大选择积分最大的节点。

## 正确性说明

每条边恰将其源编号加入目标一次，积分数组完全符合定义。选择最大值且保留最先出现的下标，满足同分取最小编号的要求。

## 复杂度分析

时间复杂度为 `O(n)`，空间复杂度为 `O(n)`。

## 边界情况

- 无入边节点积分为 0。
- 同分时返回较小编号。
- 编号 0 的贡献为 0。
