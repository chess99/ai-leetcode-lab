# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每轮每种仍存在的字符恰删除一次，因此最后一轮恰包含出现频次最大的所有字符。它们在最后一轮的相对顺序等于各自最后一次出现位置的顺序，扫描原串取这些最后出现即可。

## 复杂度

时间 `O(n)`，额外空间 `O(26)`。

## 边界条件与本地验证

频次相同的字符都要保留；结果顺序按最后出现位置，而非字母表顺序。
