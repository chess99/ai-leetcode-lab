# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

每个安检室的两种模式对编号偏移的贡献分别为 0 和 `capacity-1`。问题化为从所有贡献中任选，使总和恰为 k 的方案数；用 0/1 背包计数。使用新数组转移，正确处理 capacity=1 时两种不同模式都贡献 0。

## 复杂度

时间 `O(N*k)`，空间 `O(k)`。

## 边界条件与本地验证

k=0 和零贡献均已覆盖。三个官方样例通过；随机小实例与枚举全部模式子集对拍通过。
