# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

枚举分界字符，分别计算 `a<b`、`b<a` 所需修改数；第三种条件是把两串都变成各自最高频字符。

## 正确性

任意严格大小关系均存在唯一分界字符，枚举覆盖全部情况；第三种条件逐串保留最多字符最优。

## 复杂度

时间 `O(n+26^2)`，空间 `O(26)`。

## 边界条件与本地验证

分界不能为 z；已用题目样例验证。
