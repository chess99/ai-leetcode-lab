# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把每条非空链表的头节点放入最小堆。每次弹出最小节点接到结果尾部，再把它的后继放入堆；附加序号用于避免节点对象值相等时被比较。

## 复杂度

总节点数 `N`、链表数 `k`，时间 `O(N log k)`，空间 `O(k)`。

## 边界条件与本地验证

空数组和全空链表返回空；直接复用原节点。本地验证三个样例，并对随机有序列表与排序后的扁平数组核对。
