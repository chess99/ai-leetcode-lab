# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
## 思路
扫描数字和操作符，用栈延迟加减、立即计算乘除；末尾追加 `+` 触发最后一个数入栈。
## 复杂度
- 时间 O(n)，空间 O(n)。
## 边界条件与本地验证
- `int(a / b)` 向零截断。验证 `3+2*2 -> 7`、`3/2 -> 1`、`3+5/2 -> 5`。
