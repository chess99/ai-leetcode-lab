# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

角度在 `[0,2π)` 均匀取样。为使面积均匀，半径不能直接均匀取样，而应取 `R√U`，其中 `U` 在 `[0,1)` 均匀；再用极坐标转换并平移到圆心。

## 复杂度

每次调用时间和额外空间均为 `O(1)`。

## 边界条件与本地验证

用可控随机源验证圆心和圆周边界。固定种子抽样 160000 次，所有点都在圆内，四象限频次通过五倍标准差检查，平均半径平方接近理论值 `R²/2`。
