# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

必须保证任意连续三个位置至少有一个被提升到 k。令状态表示最近一次被提升位置的最小成本；把当前位置提升的成本是 `max(0,k-nums[i])`，其前驱只能是最近三种状态中的最小者。滚动保存三项即可。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

原值已不小于 k 时提升成本为零；结尾可由任意最近状态结束。
