# 3346. 执行操作后元素的最高频率 I

## 算法

每个原值 `x` 能被改成闭区间 `[x-k,x+k]` 中的任意整数。对这些区间做扫描线，得到每个目标值可覆盖的元素数；其中已经等于目标值的元素不消耗操作，其余至多使用 `numOperations` 个，因此频率为 `min(覆盖数, 原频率 + numOperations)`。

## 复杂度

排序扫描事件，时间复杂度 `O(n log n)`，空间复杂度 `O(n)`。

## 边界

目标值不一定在原数组中，扫描线的区间起点同样会被检查；`k=0` 时所有区间退化为单点。
