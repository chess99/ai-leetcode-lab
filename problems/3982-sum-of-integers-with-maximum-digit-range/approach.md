# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

对每个整数取各位数字的最大值减最小值，得到数字范围。先找全部范围中的最大值，再累加范围等于该值的原整数。

## 复杂度

设单个数的位数为 `d`，时间复杂度为 `O(nd)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

相同最大范围的所有数都要计入；本地断言覆盖了题目样例和多元素并列最大范围。
