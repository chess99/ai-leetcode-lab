# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

广度优先搜索 `(行,列,剩余生命)`，非正生命状态直接丢弃，每格只扩展剩余生命更高的状态；以正生命到达终点则成功。

## 复杂度

时间上界 `O(mn * health)`，空间 `O(mn)`。

## 边界条件与本地验证

覆盖起点或终点危险、生命恰好归零和题面示例。某条路径以零生命先到终点时不能立即判失败，因为队列中可能仍有另一条正生命路径。
