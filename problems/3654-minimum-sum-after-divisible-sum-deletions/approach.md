# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按原数组前缀和模 `k` 做 DP。处理当前元素时，可以保留它，使剩余和增加该值；也可把上一个相同前缀模之后的整段删除，此时剩余和回到该模状态曾达到的最小值。维护每种前缀模的最小剩余和。

## 复杂度

时间 `O(n)`，空间 `O(k)`。

## 边界条件

前缀模 0 初值为 0，允许直接删除可整除前缀；所有元素为正，最小化剩余和等价于最大化删除和。按题意保留中间输入变量。
