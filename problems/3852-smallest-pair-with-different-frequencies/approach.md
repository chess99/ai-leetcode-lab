# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

统计频次并按数值升序枚举数对，首个频次不同的数对即为答案。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

覆盖不存在答案。
## 思路

统计频次后寻找满足频次差条件的最小数对。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

覆盖不存在答案。
