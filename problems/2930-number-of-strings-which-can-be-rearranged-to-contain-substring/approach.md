# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

重排后能含 `leet` 当且仅当至少有一个 l、两个 e、一个 t。DP 状态记录是否已有 l、e 的数量（截断为 2）以及是否已有 t；每次按 l/e/t/其他 23 字母转移。

## 复杂度

状态数常数，时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

长度小于 4 的结果为 0；e 计数超过 2 无需继续区分。
