# 解题记录

## 算法、正确性与复杂度

枚举左数的切点和右数的切点，括号外前后缀分别视为乘数（空串为 1），计算 `left * (middleLeft + middleRight) * right` 并保留最小值。全部合法括号位置均被枚举，故最小者正确。时间 `O(|a||b|)`，空间 `O(1)`（不计答案）。

## 边界与验证

- 括号可贴在两端，空乘数按 1。
- 已用题面三组样例断言验证。
