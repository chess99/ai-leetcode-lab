# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

一维 DP：`dp[j]` 表示已扫描的源前缀形成目标前 `j` 个字符的方案数。扫描源字符时倒序更新，匹配则把 `dp[j]` 加到 `dp[j+1]`。

## 复杂度

时间 `O(|s||t|)`，空间 `O(|t|)`。

## 边界条件与本地验证

空目标方案数为 1；倒序更新防止同一个源字符重复使用。本地验证两个样例及随机短串组合枚举。
