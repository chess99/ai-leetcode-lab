# 解题记录

## 思路

若头节点就是目标，直接返回其后继。否则遍历到目标节点的前驱，将前驱的 `next` 指向目标的后继，即可跳过目标节点。

## 复杂度

设链表长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

目标可能是头节点或尾节点；节点值唯一。已用 `4->5->1->9` 删除 5 的最小断言，结果为 `4->1->9`。
