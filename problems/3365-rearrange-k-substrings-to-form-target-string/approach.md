# 3365. 重排 K 个子字符串形成目标字符串

## 思路

## 算法

将两个字符串都按长度 `n/k` 切成恰好 `k` 段。允许的操作只是重排这些完整片段，因此分别排序后比较两个片段多重集合是否相同。

## 复杂度

时间复杂度 `O(n log k)`（比较片段总长度为 `n`），空间复杂度 `O(n)`。

## 边界

重复片段通过排序保留其出现次数；题目保证字符串长度可被 `k` 整除。
