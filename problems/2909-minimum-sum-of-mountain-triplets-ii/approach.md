# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

预处理每个位置右侧的最小值，扫描中维护左侧最小值。每个中点只需检查两侧最小值是否都小于它，若是便得到该中点的最小山形和。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

严格小于才是山形；不存在合法中点返回 -1。
