# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

建立二维位移前缀和。删除窗口 `[l,l+k)` 后的终点等于完整路径终点减去该窗口的净位移，把所有窗口得到的坐标放入集合去重。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

必须删除且 `1≤k≤n`；`k=n` 时只得到原点。验证三个题面样例。
