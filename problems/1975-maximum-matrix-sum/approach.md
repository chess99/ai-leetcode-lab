# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

尽量把所有元素变为正数。负数个数为偶数或含 0 时可全部取绝对值；否则必须保留绝对值最小的一个为负。

## 正确性

一次操作改变两个符号，负数奇偶性不变；在必须保留负数时选择最小绝对值损失最小。

## 复杂度

时间 `O(mn)`，空间 `O(mn)`（当前实现展平矩阵）。

## 边界条件与本地验证

零可消除奇偶限制；已用样例验证。
