# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

最大堆每次优先取剩余最多的字符；若会形成三个连续相同字符，改取次多字符。无法替换时结束。

## 复杂度

时间 `O(a+b+c)`，空间 `O(1)`。

## 边界条件与本地验证

- 不产生 `aaa`、`bbb`、`ccc`。
- 本地验证了题目示例和单一字符情况。
