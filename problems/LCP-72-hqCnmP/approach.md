# 解题记录

## 思路

直接按题意模拟：每轮扫描全部相邻车厢，选择物资和最小的相邻对；`min` 在并列时保留最左位置。将二者替换为和，直到车厢数降至原长度的一半。

## 复杂度

设初始长度为 `n`。执行 `ceil(n/2)` 次合并，每轮扫描和列表替换为 `O(n)`，总时间复杂度为 `O(n^2)`，额外空间复杂度为 `O(1)`（修改输入列表）。

## 边界条件与本地验证

长度为奇数时目标为向下取整；并列最小和必须选择最左相邻对。已对 `[7,3,6,1,8]` 断言得到 `[10,15]`。
