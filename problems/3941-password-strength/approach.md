# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先对密码字符去重，再按小写、大写、数字、特殊字符四类累加对应分值。

## 复杂度

时间 `O(n)`，空间 `O(1)`（字符集大小固定）。

## 边界条件与本地验证

重复字符只计一次；验证两组样例和单一字符类别。
