# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按身高降序、k 升序排序，再把每人插入结果的 k 下标。处理时已有人员都不矮于当前人，插入位置之前恰好就是所需的 k 个较高或等高人员。

## 复杂度

排序 `O(n log n)`，列表插入最坏 `O(n^2)`，空间 `O(n)`。

## 边界条件与本地验证

- 同身高按 k 升序，避免先插入较大 k 破坏较小 k。
- 本地断言：两个示例结果中每人前方的合格人数均等于其 k。
