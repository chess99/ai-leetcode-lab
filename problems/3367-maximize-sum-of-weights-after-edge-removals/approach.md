# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

树形 DP。对每个节点先取所有子树在不连父边时的最优值；保留一条子边带来的增益为“连该边后的子状态加权值”减去“不连该边”的子状态。将正增益排序，节点可连接父边时取前 `k-1` 个，否则取前 `k` 个。

## 复杂度

所有节点的排序总代价 `O(n log n)`，空间 `O(n)`。

## 边界条件与本地验证

覆盖 `k=0`、叶子节点、增益为负以及根节点没有父边。
