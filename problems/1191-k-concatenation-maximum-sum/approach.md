# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

Kadane 算法计算一份或两份数组的最大子数组和。若总和为正，跨越两端的最优段会额外包含中间完整的 `k-2` 份数组；否则两份已经足够。

## 复杂度

时间 `O(N)`，空间 `O(1)`。

## 边界条件与本地验证

- 允许空子数组，全部负数返回 0。
- 本地覆盖三个示例。
