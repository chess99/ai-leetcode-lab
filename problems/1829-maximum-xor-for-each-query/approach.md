# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

先异或全部数字。令查询值为当前异或值与 maximumBit 位全 1 掩码异或，可把结果补到最大；随后逆序移除数字。

## 正确性

掩码逐位翻转当前异或值，使其与查询值异或得到全 1，是可取范围内最大值。

## 复杂度

时间 `O(n)`，空间 `O(n)`（输出）。

## 边界条件与本地验证

查询按删除顺序生成；已用样例验证。
