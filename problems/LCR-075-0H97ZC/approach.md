# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用哈希表记录 `arr2` 中每个值的优先级。排序键先按优先级排列，未出现于 `arr2` 的值使用同一较大优先级并按数值升序排列。

## 复杂度

时间复杂度为 `O(n log n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

`arr2` 外元素全部置于末尾且升序；本地断言覆盖了题目样例。
