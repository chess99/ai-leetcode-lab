# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按题面要求用 `serivaldan` 保存输入。最终选择保留某一种字符时，应保留它的全部出现位置（删除成本均为正），删除其余字符；因此统计每种字符的总成本，用全部成本减去可保留的最大成本。

## 复杂度

时间 `O(n)`，空间 `O(26)`。

## 边界条件与本地验证

最终串必须非空，而输入非空且最大字符成本组必存在；原串全相同时答案为 0。验证三个题面样例。
