# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
从 `k` 向两侧扩展区间。每次优先纳入两侧较大的元素，可尽量延后区间最小值下降；扩展后用当前最小值乘区间长度更新答案。

## 复杂度
时间 $O(n)$，额外空间 $O(1)$。

## 边界条件与本地验证
到达一侧边界后只能扩展另一侧。已与随机数组所有包含 `k` 的区间枚举对拍。
