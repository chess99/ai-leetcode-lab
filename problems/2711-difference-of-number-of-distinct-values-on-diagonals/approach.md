# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

逐格沿左上方向和右下方向分别扫描，将遇到的值放入集合。两个集合大小就是题目所需的不同值数量，取其绝对差填入答案。

## 复杂度

每个格子最多扫描一条对角线，时间复杂度 `O(mn * min(m, n))`，临时集合空间 `O(min(m, n))`。

## 边界条件与本地验证

单行、单列或 `1 x 1` 矩阵的一侧对角线可能为空；重复值由集合自动去重；当前格子本身不计入任一侧。
