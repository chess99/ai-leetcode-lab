# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

逐行把矩阵转成柱状图：当前位置为 1 就累加高度，为 0 就清零。每行用单调递增栈计算柱状图最大矩形，并更新全局答案。

## 复杂度

时间 `O(rows·cols)`，空间 `O(cols)`。

## 边界条件与本地验证

空矩阵直接返回 0；行末追加高度 0 的哨兵弹出剩余柱子。本地验证五个样例，并对小矩阵枚举所有矩形对拍。
