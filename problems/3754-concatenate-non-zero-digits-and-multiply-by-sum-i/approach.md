# 解题记录

## 思路

保留原顺序的非零数字，构造连接值并乘以这些数字之和。

将整数转为字符串，过滤字符 `0`。剩余字符按原顺序连接后转成 `x`，并累加这些字符对应的数字得到数字和，二者相乘即为答案。

## 复杂度

时间和空间均为 `O(d)`。

## 边界条件与本地验证

`n=0` 时没有非零数字，按题意 `x=0` 且答案为 `0`；只有一个非零数字时答案为该数字的平方。使用题目两个样例和 `n=0` 验证。
