# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

二分阈值。固定阈值后，权重大于阈值的边记为 1，其余记为 0，用 0-1 BFS 求最少重边数；不超过 `k` 即可行。

## 复杂度

`O((n+m) log m)` 时间，`O(n+m)` 空间。

## 边界条件与本地验证

源点等于终点直接返回 0；三个题面样例输出 4、-1、0。
