# 2408. 设计 SQL

## 算法
用哈希表按表名保存列数、下一个自增 id 和“id 到行”的映射。插入校验列数后写入，删除移除映射，查询按 id 与列号访问，导出按插入顺序拼接 csv。

## 正确性说明
每张表独立维护自增 id，删除不回退计数。行映射始终记录当前存在行，因此插入、删除、查询和导出均与定义一致。

## 复杂度分析
插入、删除、查询平均 `O(1)`；导出为 `O(r*c)`，其中 `r` 为行数、`c` 为列数。

## 边界情况
非法表、行、列返回题目规定的失败值；删除缺失行无影响。
