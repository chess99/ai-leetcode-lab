# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

模拟等候室当前人数。遇到 `E` 加一，遇到 `L` 减一；整个过程中人数的最大值就是同时需要的最少椅子数。

## 复杂度

设事件串长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

题目保证进出序列有效，因此人数不会减为负数。全是进入事件时，答案等于字符串长度。
