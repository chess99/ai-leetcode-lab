# 解题记录

## 算法、正确性与复杂度

为每个数字逐位应用 `mapping`，拼成其映射值，再按该值排序并返回原数字。Python 排序稳定，因此映射值相同的元素保留输入相对顺序。映射函数准确替换每个十进制位，得到题意比较键，稳定排序即满足全部要求。设最大位数为 `d`，时间 `O(n log n * d)`，额外空间为排序所需 `O(n)`。

## 边界与验证

- 数字 0 单独映射为 `mapping[0]`。
- 映射产生前导零时转换为整数自然忽略。
- 用题面样例、恒等映射和映射值相同的稳定顺序断言验证。
