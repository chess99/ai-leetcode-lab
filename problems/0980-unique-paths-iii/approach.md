# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先统计所有非障碍格，再从起点做回溯。进入普通格后临时标成障碍，枚举四邻格，返回时恢复。只有到达终点且剩余未走格数恰好为 1，才构成覆盖全部可走格的合法路径。

## 复杂度

设可走格数为 `n`，最坏时间 `O(4^n)`，递归栈 `O(n)`；原地标记不另建访问集合。

## 边界条件

终点不能提前经过；障碍不计入待访问数量；起点也计作必须覆盖的格子。
