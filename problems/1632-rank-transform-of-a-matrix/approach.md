# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
按数值升序分批处理。同值且共享行或列的格子用临时并查集合成组件；组件排名为其涉及行列既有最大排名加一。整批算完后再统一更新行列排名，避免同值互相抬高。

## 复杂度
排序 `O(mn log(mn))`，并查集近线性，空间 `O(mn)`。

## 边界条件与本地验证
同值组件必须同排名；同批更新延后；负数与重复值均不影响排序逻辑。
