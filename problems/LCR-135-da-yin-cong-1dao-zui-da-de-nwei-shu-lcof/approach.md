# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

`cnt` 位十进制数的最大值为 `10^cnt - 1`，直接生成从 `1` 到该值的连续整数序列。

## 复杂度

时间复杂度与输出规模相同，为 `O(10^cnt)`；输出空间复杂度为 `O(10^cnt)`。

## 边界条件与本地验证

`cnt = 1` 时返回 `1` 到 `9`；本地断言覆盖了 `cnt = 1` 与题目样例的范围和端点。
