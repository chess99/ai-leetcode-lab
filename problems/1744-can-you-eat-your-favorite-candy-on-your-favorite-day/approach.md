# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

构造糖果数量前缀和。第 d 天最多吃 `(d+1)*cap` 颗、最少吃 d+1 颗，检查这一区间与目标类型糖果的累计区间是否相交。

## 正确性

区间相交当且仅当存在一种每日吃法能在该日吃到目标类型。

## 复杂度

预处理 `O(n)`，查询 `O(1)`，空间 `O(n)`。

## 边界条件与本地验证

累计边界使用严格/非严格不等式避免吃到前一类型；已用样例验证。
