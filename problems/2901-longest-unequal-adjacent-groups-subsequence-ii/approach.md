# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

`dp[i]` 表示以第 i 个单词结尾的最长合法子序列。枚举此前单词，检查组号不同、长度相等且汉明距离为 1 时进行转移，并记录前驱以重建答案。

## 复杂度

时间 `O(n^2 * L)`，空间 `O(n)`。

## 边界条件与本地验证

字符串长度不同不能转移；任一单词本身都是长度 1 的合法解。
