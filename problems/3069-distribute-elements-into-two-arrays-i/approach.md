# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

按题意模拟两个数组。先分别放入前两个元素；之后每个元素只需比较两个数组末尾元素，决定追加到哪一个数组。最后连接两个数组即可。

## 复杂度

设数组长度为 `n`。时间复杂度为 `O(n)`，除返回数组外的额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

`n >= 3`，初始化两个数组后仍有定义良好的后续遍历。题目保证元素互异，比较末尾元素时不会相等。
