# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

分别扫描所有水平、垂直切线并维护两侧元素和及频次。两侧和相等直接成功；否则较大一侧必须恰好存在值等于差额的可删格。二维矩形删除任一格仍连通，只有宽或高为一的条带需要限制为端点删除。

## 复杂度

两次扫描均为 `O(mn)` 时间和 `O(mn)` 频次空间。

## 边界条件与本地验证

处理一行、一列、单行部分及端点删除。1000 组小网格与枚举切线和连通性的 oracle 一致。
