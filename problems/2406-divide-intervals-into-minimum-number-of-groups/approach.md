# 2406. 将区间分成最少组数

## 算法
按左端点排序，用最小堆保存各组最后区间的右端点。当前区间仅在最早结束时间严格小于当前开始时间时可复用该组；否则新开组。

## 正确性说明
堆顶是所有组中最早结束的区间。若它仍与当前区间相交，其他组也不能复用；若不相交，复用它不影响合法性。故贪心使用的组数最少。

## 复杂度分析
时间 `O(n log n)`，空间 `O(n)`。

## 边界情况
端点相等也相交，因此使用严格小于比较。
