# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

滑动窗口计数。当新加入字符次数达到 `k` 时，当前及所有更长右扩子串均有效，累加后不断移动左端直到该次数低于 `k`。

## 复杂度

时间 `O(n)`，空间 `O(字符集大小)`。

## 边界条件与本地验证

覆盖 `k=1`、字符重复和没有字符达到 `k` 次。
