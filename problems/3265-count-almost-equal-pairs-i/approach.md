# 解题记录

## 思路
对每个数枚举不交换及一次交换两位后的所有值。检查一对数时，同时判断当前数能否通过交换变成此前数，以及此前数能否通过交换变成当前数；任一方向成立即可计数。

## 复杂度
时间 `O(n²L²)`；缓存每个数的交换结果，空间 `O(nL²)`。

## 边界条件与本地验证
交换后允许出现前导零，所以关系不能只按输入顺序单向检查。例如 `[30, 3]` 中应交换 `30` 得到 `03`，该数对仍然有效。
