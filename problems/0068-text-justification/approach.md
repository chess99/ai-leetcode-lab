# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

贪心装入能容纳的最多单词。非末行且多词时，将剩余空格均分到间隙，余数从左到右各多一个；末行或单词行仅单空格连接并在右侧补齐。

## 复杂度

每个单词只处理一次，时间 O(总字符数)，除结果外空间 O(1)。

## 边界条件与本地验证

单词长度不超过宽度；单词行避免除零，所有输出行都补齐到精确宽度。
