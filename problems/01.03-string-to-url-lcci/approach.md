# 解题记录

## 思路

只保留真实长度内的字符，再将其中每个空格替换为 `%20`。尾部预留空间不属于真实字符串，必须先截断。

## 复杂度

设真实长度为 `n`。时间复杂度为 `O(n)`；构造结果字符串需要 `O(n)` 额外空间。

## 边界条件与本地验证

真实内容可全部为空格，长度为 0 时返回空串。已对 `"Mr John Smith    ", 13` 断言得到 `"Mr%20John%20Smith"`。
