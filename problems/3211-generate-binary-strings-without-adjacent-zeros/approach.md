# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

深度优先构造前缀。字符 `1` 总能追加；字符 `0` 只在前缀为空或前一位为 `1` 时追加，到长度 `n` 后收集答案。

## 复杂度

设输出数量为 `A`，时间 `O(nA)`，额外空间 `O(n)`（不含输出）。

## 边界条件与本地验证

`n=1` 时两个二进制字符都合法；递归始终禁止生成相邻的 `00`。
