# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

扫描右端点并维护每个数值作为左端点之前的最小前缀和。当前值为 `v` 时，只需查询左端值 `v-k`、`v+k`；用当前前缀和减去对应最小前缀和即可得到以当前位置结束的最大好子数组和。

## 复杂度

时间 `O(n)`，哈希表空间 `O(n)`。

## 边界条件与本地验证

答案可以为负数，不能把初值设为零；若不存在满足端点差值的子数组才返回 0。
