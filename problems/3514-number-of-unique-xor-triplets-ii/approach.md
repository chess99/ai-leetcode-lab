# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用指示数组表示出现过的值。XOR 快速沃尔什变换把 XOR 卷积变为逐点乘法，变换值立方后逆变换即可得到每个三元异或值的组合数，统计正数位置。

## 复杂度

令 `U` 为严格大于最大值的最小二次幂，时间 `O(U log U)`，空间 `O(U)`。

## 边界条件与本地验证

重复使用同一索引合法，因此只需值是否存在；已保存中间输入变量。覆盖 `[1,3]` 和多值输入。
