# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

建立字母到数字的固定映射。逐词把所有字母转换成数字串，与 num 完全相同便保留，输出顺序沿用输入顺序。

## 复杂度

设所有单词字符数为 S，时间 O(S)，额外空间 O(1)（不计结果）。

## 边界条件与本地验证

题目保证长度一致且 num 不含 0、1，空数字串也能匹配空单词。补齐 `List` 类型导入，并验证
模块可在无预注入环境中独立加载。
