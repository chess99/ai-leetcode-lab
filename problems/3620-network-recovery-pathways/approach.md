# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

二分路径允许的最小边权。给定阈值后，只保留边权不少于阈值且中间节点在线的边；在 DAG 拓扑序上动态规划从起点到各点的最小成本，终点成本不超过预算即为可行。

## 复杂度

拓扑排序 `O(n+m)`；二分不同边权，合计 `O((n+m) log m)` 时间，空间 `O(n+m)`。

## 边界条件与本地验证

离线中间点完全跳过；无有效路径返回 `-1`，边权为零也可二分。
