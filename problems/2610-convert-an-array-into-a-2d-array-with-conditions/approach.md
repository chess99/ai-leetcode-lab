# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

记录每个数已经放入过多少行。遇到一个数时，将它放入对应计数下标的行，再递增计数；若该行尚不存在则创建。这样同一数的每次出现都落在不同的行，且最大出现次数正好决定最少行数。

## 复杂度

时间 `O(n)`，哈希表和输出使用 `O(n)` 空间。

## 边界条件与本地验证

全不重复时只创建一行；某个数重复 `c` 次时必然创建至少 `c` 行，本构造恰好达到该下界。
