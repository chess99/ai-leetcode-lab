# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

回溯逐位构造。字符 0 总可选择；只有上一位不是 1 且加入当前下标后成本不超过 `k` 时，才选择字符 1。到达长度 `n` 时收集字符串。

## 复杂度

输出规模记为 `A`，时间 `O(nA)`，递归栈 `O(n)`（不计输出）。

## 边界条件与本地验证

下标 0 的字符 1 成本为 0；验证 `n=1,k=0` 和题目样例，并与全二进制串枚举比较。
