# 解题记录

## 思路

把每个模式字符允许替换成的字符存成集合。枚举原串中所有等长窗口，逐位检查两字符本来相同，或模式字符可以按映射替换为原串字符；存在完整匹配窗口即返回真。

## 复杂度

设原串长 `n`、模式长 `m`，时间 `O((n-m+1)m)`，映射空间 `O(r)`。

## 边界条件与本地验证

映射有方向且不能链式传递，原字符相同无需映射。三个题面样例通过；随机字符串、模式和映射表直接逐窗口核对 1000 组。
