# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

坐标 `(row, col)` 所在对角线由 `row + col` 唯一确定。按行从上到下收集元素后，同一对角线的元素顺序与要求相反，因此按对角线编号递增输出每组的逆序即可。

## 复杂度

设全部整数数量为 `N`，时间复杂度和额外空间复杂度均为 `O(N)`。

## 边界条件与本地验证

各行长度可不同；单行直接保持原顺序。已用题目四个样例验证。
