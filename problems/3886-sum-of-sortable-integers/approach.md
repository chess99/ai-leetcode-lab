# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

枚举 `n` 的每个因数 `k`。排序后的对应区块是目标；原区块可独立循环移位，故只需用 KMP 判断它是否为目标块的旋转。所有区块均成立即累加 `k`。

## 复杂度

设 `tau(n)` 为因数个数，时间为 `O(n * tau(n))`，额外空间为 `O(n)`。

## 边界条件与本地验证

题面三个样例分别得到 3、0、3。
