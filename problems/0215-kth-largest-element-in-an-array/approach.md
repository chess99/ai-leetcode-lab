# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将第 k 大转换为升序下标 `n-k`，用 Quickselect 分区。分区后枢轴位于其最终有序位置，只继续搜索包含目标下标的一侧。

## 复杂度

平均时间 `O(n)`，原地分区空间 `O(1)`；固定末尾枢轴的最坏时间为 `O(n^2)`。

## 边界条件与本地验证

- 重复元素按位置而非不同值计数。
- 本地断言：两个题目示例分别为 5 和 4，单元素为自身。
