# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1（本地预解，未发起远程测试或提交）

## 思路

与课程表相同地做 Kahn 拓扑排序，同时记录每次出队课程。若记录数不足课程数说明有环，返回空列表。

## 复杂度

时间 `O(V+E)`，空间 `O(V+E)`。

## 边界条件与本地验证

- 多个可选课程的顺序任意但必须满足全部边。
- 有环返回空列表。

本地验证输出顺序满足每条先修边，覆盖有环和零先修关系；通过 `py_compile`。
