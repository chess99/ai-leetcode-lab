# 解题记录

## 思路

课程关系构成 DAG。拓扑排序中，`dp[v]` 表示完成课程 `v` 的最早时间；每条先修边 `u->v` 转移为 `dp[v]=max(dp[v],dp[u]+time[v])`。入度归零后课程才可进入队列，最终所有课程完成时间的最大值就是总工期。

## 复杂度

时间 `O(V+E)`，图、入度、动态规划与队列空间 `O(V+E)`。

## 边界条件与本地验证

独立课程可从零时刻并行开始，多条先修链取最晚者。题面样例得到 `8、12`；随机 DAG 用按拓扑下标计算的最长先修路径独立对拍。
