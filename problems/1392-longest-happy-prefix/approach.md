# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

构造 KMP 前缀函数，`prefix[i]` 表示以 `i` 结尾的前缀中，最长的真前后缀长度。失配时沿此前的前缀函数回退，匹配时长度加一。整个字符串末位的前缀函数值就是最长快乐前缀长度。

## 复杂度

时间和空间复杂度均为 $O(n)$。

## 边界条件与本地验证

单字符或不存在匹配前后缀时返回空串；重叠前后缀允许存在。实际加载后验证样例，并对随机字符串枚举所有真前缀对拍。
