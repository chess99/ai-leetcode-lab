# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
反向恢复目标数组。每次最大值必是最后被替换的元素，其前值等于最大值减去其余元素和；用取模批量执行多次相减，并用最大堆持续处理。

## 复杂度
每轮堆操作 `O(log n)`，数值通过取模快速下降；空间 `O(n)`。

## 边界条件
其余和为 1 时一定可还原；最大值不大于其余和或取模为零时失败；单元素只有 `[1]` 可行。
