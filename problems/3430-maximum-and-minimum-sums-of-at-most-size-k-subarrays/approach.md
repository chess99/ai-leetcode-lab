# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

分别统计每个元素作为子数组最小值、最大值的贡献。用严格/非严格单调栈确定唯一负责区间；设左右可扩展长度为 L、R，再用分段等差数列公式计算满足左右长度之和不超过 k+1 的起止点组合数。

## 复杂度

时间复杂度 O(n)，空间复杂度 O(n)。

## 边界条件与本地验证

相等元素通过一侧严格、一侧非严格避免重复。随机短数组枚举所有长度不超过 k 的子数组对拍通过。
