# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先统计每个字符的出现次数，再按原字符串顺序寻找第一个次数为一的字符。

## 复杂度

时间复杂度为 `O(n)`，空间复杂度为 `O(n)`。

## 边界条件与本地验证

不存在唯一字符时返回空格；本地断言覆盖了两个样例。
