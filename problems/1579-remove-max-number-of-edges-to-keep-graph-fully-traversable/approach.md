# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
为 Alice、Bob 各维护并查集，优先加入可共同使用的类型 3 边，再分别加入专属边。成功合并的边必须保留，其余均可删除。

## 复杂度
时间 `O(E α(n))`，空间 `O(n)`。

## 边界条件与本地验证
最终任一并查集不连通则返回 -1；公共边只给保留数加一次。
