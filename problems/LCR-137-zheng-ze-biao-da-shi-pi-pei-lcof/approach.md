# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

动态规划匹配完整文本与模式。普通字符或点从左上状态转移；星号可让前一元素出现零次（跳过两位），或在当前字符匹配时继续消费文本而停留在同一模式位置。

## 复杂度

时间 `O(|s||p|)`，空间 `O(|p|)`。

## 边界条件与本地验证

预处理能匹配空串的 `x*` 前缀；题目保证星号前元素有效。本地验证三个样例，并与递归穷举匹配器随机对拍。
