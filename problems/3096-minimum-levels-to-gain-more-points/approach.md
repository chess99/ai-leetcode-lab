# 解题记录

## 思路
把可通过关卡映射为 `+1`，不可通过映射为 `-1`。先求总分，再从左到右维护 Alice 前缀分数，首个满足 `prefix > total-prefix` 且 Bob 仍有关卡的位置即为答案。

## 复杂度
时间复杂度 `O(n)`，空间复杂度 `O(1)`。

## 边界条件与本地验证
不能把全部关卡交给 Alice；不存在严格大于时返回 -1。已覆盖三个题目示例。
