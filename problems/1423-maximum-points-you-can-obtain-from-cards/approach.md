# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先取前 `k` 张作为初始方案。之后每一步少取一张左侧牌，并多取一张右侧牌，枚举从右侧取 0 到 `k` 张的所有组合，维护最大分数。

## 复杂度

时间复杂度为 `O(k)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

`k` 等于数组长度时初始方案就是答案；只能从两端取牌，无法取中间牌。已用题目五个样例验证。
