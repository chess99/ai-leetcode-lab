# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

可以把两个被修改的元素设成相同值，使最小得分为零。于是只需让未修改元素的值域最小。排序后，至多改两个元素只有三类有效选择：改掉两个最大值、两端各改一个、或改掉两个最小值；分别对应三个剩余值域，取最小值。

## 复杂度

时间复杂度 `O(n log n)`，排序外额外空间 `O(1)`。

## 边界条件与本地验证

当长度为 3 时，三种表达式均为 `0`；重复数值自然得到零值域。
