# 3342. 到达最后一个房间的最少时间 II

## 思路

## 算法

仍以 Dijkstra 求最短到达时间。第 1、3、5… 次移动耗时 1，第 2、4、6… 次耗时 2；网格是二分图，到达坐标 `(r,c)` 的移动次数奇偶性固定为 `(r+c)` 的奇偶性，因此可由目标坐标直接确定本次移动耗时。

## 复杂度

设网格有 `N` 个房间，时间复杂度 `O(N log N)`，空间复杂度 `O(N)`。

## 边界

进入相邻房间前可等待到其开放；不同路径的移动次数奇偶性相同，故不需要把奇偶性再作为状态。
