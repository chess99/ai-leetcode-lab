# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
记录源串每个数字出现下标的队列。依次构造目标字符时取该数字最早未用位置；若其前面仍有更小数字，则排序操作无法让它越过该小数字。

## 复杂度
仅有十种数字，时间 `O(10n)`，空间 `O(n)`。

## 边界条件与本地验证
目标需要的数字耗尽立即失败；相同数字必须按源串顺序使用。
