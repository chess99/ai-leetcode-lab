# 解题记录

## 思路

一个数能整除 numsDivide 的所有元素，当且仅当它整除这些元素的最大公约数。先求该 GCD，再排序 nums；第一个能整除 GCD 的值之前的元素全部删除即可最优，若不存在返回 -1。

## 复杂度

时间 `O(nlog n+m log V)`，排序额外空间 `O(n)`。

## 边界条件与本地验证

重复的较小值必须全部计入删除数；没有候选返回 -1。题面样例得到 `2、-1`；随机数组直接检查排序后每个候选是否整除所有 numsDivide，对拍 1200 组。
