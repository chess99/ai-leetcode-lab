# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；Created：2026-08-11T22:15:56Z

## 思路

使用前缀异或快速得到任意子数组异或。`dp[g][i]` 为前 `i` 个数分为 `g` 段时，各段异或最大值的最小可能值；枚举最后一段起点转移。

## 复杂度

时间 `O(k*n^2)`，空间 `O(n)`。

## 边界条件与本地验证

每段非空，因此最后一段起点至少为 `g-1`。已覆盖三个样例。
