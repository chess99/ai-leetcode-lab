# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

节点 `x` 将树分成左子树、右子树和其余父侧三块。二号玩家若选择其中最大的一块，就能占据该连通区域；当该块节点数超过 `n // 2` 时必胜。一次 DFS 同时统计子树大小并记录 `x` 的左右大小。

## 复杂度

设节点数为 `N`。DFS 时间 `O(N)`，递归栈最坏为 `O(N)`。

## 边界条件与本地验证

- `x` 为根时父侧大小为 0。
- 只有任一分块严格超过一半才获胜。
- 本地覆盖题目两个示例及选择叶节点的情况。
