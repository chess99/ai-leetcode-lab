# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

以邮箱为并查集节点，将同一账户中的邮箱合并。按最终根分组并排序邮箱，根邮箱对应账户名构成结果。

## 复杂度

设邮箱数为 `n`，并查集近似 `O(n)`，排序各组邮箱总计 `O(n log n)`。

## 边界条件与本地验证

- 通过共享邮箱可传递合并多个账户。
- 每组邮箱排序，结果组顺序不影响答案。
- 本地执行 `py_compile` 和题目示例断言。
