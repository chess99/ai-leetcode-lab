# 解题记录

## 思路
统计已有字母频次，用 `(频次, 字母)` 最小堆逐个为问号选择当前频次最小的字母，以最小化频次平方和。选出的字母排序后按问号从左到右填回，保证相同最小值下字典序最小。

## 复杂度
时间复杂度 `O((n+26)log 26)`，空间复杂度 `O(26+n)`。

## 边界条件与本地验证
没有问号时原样返回；频次并列时先选较小字母。已覆盖 `"???"` 和 `"a?a?"`。
