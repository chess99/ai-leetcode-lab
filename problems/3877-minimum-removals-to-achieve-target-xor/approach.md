# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

改为最大化保留元素数，其异或必须等于 target。将数组折半，分别枚举子集，记录每个异或值最多可保留几个元素；匹配两半异或即可得到最优。

## 复杂度

时间 O(2^(n/2))，空间 O(2^(n/2))。

## 边界条件与本地验证

验证三个样例和空保留集合（target 为 0）。
