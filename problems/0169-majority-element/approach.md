# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

使用 Boyer-Moore 投票。维护候选值与票数：票数为零时选当前值为候选；相同值加票，不同值减票。多数元素出现次数超过一半，成对抵消后仍会留下它。

## 复杂度

时间复杂度 O(n)，额外空间 O(1)。

## 边界条件与本地验证

数组非空，单元素直接成为候选；正负数都只按相等关系处理；多数元素的存在由题目保证。
