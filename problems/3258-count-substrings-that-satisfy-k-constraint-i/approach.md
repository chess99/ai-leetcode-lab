# 解题记录

## 思路

枚举子串左端点，再逐步向右扩展并维护 0 和 1 的出现次数。只要两种字符中至少一种的计数不超过 k，当前子串就满足 K 约束并计入答案。

## 复杂度

设字符串长度为 n，时间复杂度 O(n²)，额外空间复杂度 O(1)。

## 边界条件与本地验证

全 0 或全 1 的任意子串必然满足条件；只有 0 和 1 的计数都超过 k 时才不合法。验证了题目样例，并对小型二进制字符串与直接切片计数的 oracle 核对。
