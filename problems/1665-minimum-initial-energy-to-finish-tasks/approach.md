# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
按“最低要求减实际消耗”降序完成任务。扫描时维护此前已消耗能量，初始能量至少要覆盖 `此前消耗+当前最低要求`，取所有任务的最大值。

## 复杂度
排序时间 `O(n log n)`，额外空间取决于排序实现。

## 边界条件与本地验证
实际消耗不超过最低要求；只有一个任务时答案就是其最低要求。
