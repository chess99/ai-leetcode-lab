# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

两字符串相似当且仅当不同位置数为 0 或 2（题目保证互为字母异位词）。枚举字符串对，相似就用并查集合并，最终返回连通分量数。

## 复杂度

字符串数 `n`、长度 `m`，时间 `O(n²m)`，空间 `O(n)`。

## 边界条件与本地验证

完全相同字符串也会合并；相似关系通过并查集取传递闭包。本地验证两个样例并与显式图 DFS 对拍。
