# 3371. 识别数组中的最大异常值

## 思路

## 算法

设异常值为 `x`，特殊值为 `y`，其余普通元素之和也是 `y`，所以总和满足 `total = x + 2y`。枚举 `x`，检查 `total-x` 是否为偶数以及对应 `y` 是否仍有可用出现次数，取最大的合法 `x`。

## 复杂度

时间复杂度 `O(n)`，空间复杂度 `O(n)`。

## 边界

异常值与特殊值可相等，必须确认计数至少为两次；负数总和同样适用整除判断。
