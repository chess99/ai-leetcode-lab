# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

相邻和的奇偶性只有两种：若为偶数，子序列所有元素必须同奇偶；若为奇数，元素必须奇偶交替。统计同奇偶的数量；同时扫描并维护以奇、偶结尾的最长交替子序列，更新后取最大值。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

交替选择必须保留原数组顺序，不能只根据奇偶总数量计算；首个元素可作为长度 1 的交替子序列。
