# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按层维护当前节点列表，先记录这一层的值，再依次收集其左右孩子作为下一层，直到没有节点。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

空树返回空列表；本地断言覆盖了题目示例与空树。
