# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

DFS 序把每个成员的管理子树映射为连续区间。用两个树状数组支持区间加和区间和：类型 1 是单点区间加，类型 2 是子树区间加，类型 3 查询子树区间和。

## 复杂度

预处理 `O(n)`，每次操作 `O(log n)`，空间 `O(n)`。

## 边界条件与本地验证

迭代 DFS 避免深链递归；查询结果最后取模。本地验证题面样例，并对随机小树用显式逐节点加币对拍。
