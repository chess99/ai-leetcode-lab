# 解题记录

## 思路
先喝掉全部初始瓶子，空瓶数相同。只要空瓶数不少于当前交换门槛，就交换一瓶、喝掉它并得到一个空瓶，然后门槛加一；累计喝掉的瓶数。

## 复杂度
交换次数不超过初始瓶数，时间复杂度 `O(numBottles)`，空间复杂度 `O(1)`。

## 边界条件与本地验证
门槛为 1 也每次只能交换一瓶后立刻提高；空瓶不足门槛时终止。已覆盖 `(13,6)` 与 `(10,3)`。
