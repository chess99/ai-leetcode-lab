# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

对二叉搜索树进行右根左的迭代中序遍历，访问顺序正好从大到小；访问到第 `cnt` 个节点时返回其值。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(h)`。

## 边界条件与本地验证

`cnt` 在有效节点数范围内；本地断言覆盖两个题目样例。
