# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

每次最多从一堆中取两枚硬币，因此数量为 `c` 的一堆至少需要 `ceil(c / 2)` 次，逐堆累加即可。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

奇数堆需额外取一次；本地断言覆盖了两个样例及单枚硬币。
