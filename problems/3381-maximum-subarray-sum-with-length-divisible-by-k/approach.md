# 3381. 长度可被 K 整除的子数组的最大元素和

## 思路

## 算法

子数组 `(j, i]` 的长度能被 `k` 整除，当且仅当两个前缀下标对 `k` 取模相同。扫描前缀和，并为每个余数维护历史最小前缀和；用当前前缀和减该最小值即可得到以当前位置结尾的最佳答案。

## 复杂度

时间复杂度 `O(n)`，空间复杂度 `O(k)`。

## 边界

初始空前缀属于余数 0；所有数都为负时最小前缀策略仍会得到非空、合法长度的最大和。
