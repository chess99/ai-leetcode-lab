# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举子数组左端点，向右累加得到每个子数组和；检查其个位是否为 `x`，并将和转成十进制字符串读取首位。

## 复杂度

时间 `O(n^2 log S)`（首位转换），空间 `O(log S)`，`S` 为总和。

## 边界条件与本地验证

所有元素为正，和始终为正，不涉及负号或前导零；验证两组样例和随机前缀和朴素比较。
