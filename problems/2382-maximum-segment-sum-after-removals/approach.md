# 解题记录

## 思路

正向删除会拆分区间，逆序恢复只会合并，适合并查集。从全部关闭开始逆序开启查询位置，记录开启前的当前最大段和作为对应答案；开启后与左右已开启邻居合并，并在根上维护组件和，更新全局最大值。

## 复杂度

时间 `O(nα(n))`，并查集、组件和、开启标记与答案空间 `O(n)`。

## 边界条件与本地验证

恢复位置可能同时连接左右两段；最后一次删除后的答案为 0。题面样例得到 `[14,7,2,2,0]` 和 `[16,5,3,0]`；随机正数数组逐次删除并线性扫描段和对拍 1000 组，十万规模约 0.12 秒。
