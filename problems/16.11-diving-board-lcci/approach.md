# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

枚举长木板的数量，从零到 `k`。每种数量对应一个总长度；若两种木板等长，所有组合只有一个结果。

## 复杂度

时间复杂度为 `O(k)`，输出空间复杂度为 `O(k)`。

## 边界条件与本地验证

`k = 0` 返回空数组；本地断言覆盖样例及等长木板。
