# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

第一次操作只能选首两项、末两项或首末项，分别枚举其分数。固定分数后，记忆化搜索剩余区间；若三种可删除端点组合的和等于目标分数，转移到对应缩短后的区间并取最大值。

## 复杂度

每个目标分数至多有 `O(n^2)` 个区间状态，时间和空间均为 `O(n^2)`。

## 边界条件与本地验证

剩余元素少于两个时不能继续操作。三种初始选择即使分数相同也可安全重复计算。
