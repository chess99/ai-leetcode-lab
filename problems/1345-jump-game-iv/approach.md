# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
建立值到所有下标的映射，从下标 0 做 BFS，每步扩展相邻下标和所有同值下标。同值列表第一次使用后立即删除，避免后续反复扫描造成平方复杂度。

## 复杂度
每个下标及每个同值关系只处理一次，时间和空间均为 `O(n)`。

## 边界条件
单元素数组返回 0；越界相邻位置忽略；访问集合避免重复入队。
