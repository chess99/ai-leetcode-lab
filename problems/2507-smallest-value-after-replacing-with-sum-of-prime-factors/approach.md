# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

反复试除分解当前数，并按重数累加质因子。若所得和等于当前数，当前数必为稳定值（包括质数），否则继续替换。

## 复杂度

单轮试除为 `O(sqrt(n))`，替换后的数不会增大；额外空间 `O(1)`。

## 边界条件与本地验证

质数会直接稳定；幂次质因子会重复计入。已验证 `15 -> 5` 与质数 `3 -> 3`。
