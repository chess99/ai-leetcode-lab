# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

直接模拟指令指针。集合记录已执行下标：遇到 `add` 累加并后移，遇到 `jump` 按值跳转；越界或再次访问即停止。

## 复杂度

每条指令最多执行一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

跳转可为负、零或越界；重复访问的指令不重复计分。覆盖自环和越界跳转。
