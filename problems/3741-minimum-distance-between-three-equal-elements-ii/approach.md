# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:52Z

## 算法

将三个下标排序为 `i<j<k`，距离恒为 `2*(k-i)`，与中间位置无关。对每个值记录出现位置；最优三元组一定是连续三次出现，扫描到第三次及以后时用当前下标与倒数第三个下标更新答案。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界

任何值出现不足三次时返回 `-1`；相邻三次出现也按同一公式处理。
