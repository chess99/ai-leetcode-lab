# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

全 0 串无法通过操作产生 1；只要串中有一个 1，它可与任意位置配合传播或调整 1。因此可达性只取决于两个串是否都含有 1，或都不含 1。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

相同全 0 串可达；一方全 0、另一方含 1 不可达。已验证两个题面样例。
