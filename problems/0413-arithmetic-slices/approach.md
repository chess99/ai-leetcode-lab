# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

`current` 表示以当前位置结尾的新增等差子数组数量。若最后三个数差相等，原有每个等差切片都可向右扩展，另加新长度三切片，因此加一；否则归零。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

- 长度不足三不会进入循环。
- 本地断言：`[1,2,3,4]` 为 3，单元素为 0，常值四元素为 3。
