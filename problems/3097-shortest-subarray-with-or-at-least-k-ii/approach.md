# 解题记录

## 思路
滑动窗口维护每一位在窗口内出现的次数和当前按位或。右端加入时增加位计数；当或值达到 `k`，不断移动左端并按位清除计数归零的位，更新最短长度。

## 复杂度
每个元素加入和移除各一次，每次检查 31 位，时间复杂度 `O(31n)`，空间复杂度 `O(31)`。

## 边界条件与本地验证
`k=0` 时任一单元素窗口有效，直接返回 1，避免空窗口的按位或仍满足阈值而继续收缩；整体或值不足 `k` 时返回 -1。已覆盖三个题目示例。
