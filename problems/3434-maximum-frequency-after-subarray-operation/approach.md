# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

原有 `k` 的数量是基数。若把某个值 `v` 变为 `k`，所选子数组中每个 `v` 收益 +1、每个原 `k` 损失 -1、其余为 0；对每个候选 `v` 运行 Kadane 算法取最大增益。

## 复杂度

值域至多 50，时间 `O(n * V)`，空间 `O(1)`（不计输入）。

## 边界条件与本地验证

验证不操作最优、子数组中混有 k、候选值多次出现和数组全为 k。
