# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

数组长度为奇数，直接取得中间下标元素，再统计它在数组中的出现次数；次数恰为一时返回 `true`。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

长度为一的数组中中间元素必唯一；本地断言覆盖了唯一与重复的中间元素。
