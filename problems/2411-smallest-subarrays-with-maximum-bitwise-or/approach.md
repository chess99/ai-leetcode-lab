# 2411. 按位或最大的最小子数组长度

## 思路
从右向左记录每个二进制位最近出现的位置。位置 `i` 的最大后缀或包含的每一位都必须覆盖到对应最近位置，最远位置决定最短子数组右端点。

### 正确性说明
后缀最大或的每一位必须在子数组中出现；最近出现位置之前无法获得该位，覆盖所有最近位置又足以获得全部位，因此取最远者得到最短长度。

## 复杂度
时间 `O(32n)`，空间 `O(32)`。

## 边界条件与本地验证
数字为 0 时不更新任何位，仍可由后缀位确定长度。
