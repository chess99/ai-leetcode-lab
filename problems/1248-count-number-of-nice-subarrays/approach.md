# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

前缀和记录截至当前位置的奇数个数。当前前缀有 `odds` 个奇数时，所有此前奇数计数为 `odds-k` 的前缀都能与它组成恰有 `k` 个奇数的子数组。

## 复杂度

时间复杂度为 `O(n)`，空间复杂度为 `O(n)`。

## 边界条件与本地验证

- 前缀奇数数为 0 的初始次数为 1。
- 连续偶数会形成多个不同起点的有效子数组。
- 本地验证了题目示例和全偶数情况。
