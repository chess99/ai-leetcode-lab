# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

数组长度为 N-2，所以完整范围是 1..len(nums)+2。完整范围与数组全部异或后得到两个缺失数的异或值；取其最低位 1，把完整范围和数组按这一位分组异或，即分别还原两个缺失数。

## 复杂度

时间 `O(N)`，额外空间 `O(1)`。

## 边界条件与本地验证

覆盖两个缺失数分别在范围首尾、数组为空及乱序输入。两个官方样例通过；N<50 随机缺失组合与集合 oracle 对拍通过。
