# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

DP 表示以当前位置为右下角的全 1 正方形最大边长。当前位置是 1 时，边长为上、左、左上三个边长最小值加一；为 0 时置零。使用一维数组和额外变量保存左上角旧值。

## 复杂度

时间 `O(mn)`，滚动数组空间 `O(n)`。

## 边界条件与本地验证

- 补零列免去首行首列特判。
- 本地断言：三个题目示例面积为 4、1、0；全 1 的 `2x2` 面积为 4。
