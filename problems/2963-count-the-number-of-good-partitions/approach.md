# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

记录每个值最后出现位置，扫描时维护当前不可切断区间的最远右端。下标到达该右端时形成一个独立块；相邻块之间可切或不切，每个边界贡献两种选择。

## 复杂度

时间 `O(n)`，最后位置表空间 `O(n)`。

## 边界条件与本地验证

全不同有 `2^(n-1)` 种，全相同只有 1 种。题面样例得到 `8、1、2`；随机短数组枚举所有分割验证跨组值集合不交对拍。
