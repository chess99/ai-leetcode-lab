# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

滑动窗口维护其中相等元素对数。右端加入一个值会新增该值当前频次对；窗口对数达到 `k` 后，固定左端的所有更长右扩窗口都有效，累计其数量并收缩左端。

## 复杂度

时间 `O(n)`，空间 `O(n)`（频次表）。

## 边界条件与本地验证

收缩时先减少频次再减去剩余配对数；结果可能很大，使用 Python 整数。已验证题面样例和无有效子数组情形。
