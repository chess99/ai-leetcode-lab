# 解题记录

## 思路

选择两个最大元素作加数、最小元素作减数。

表达式中 `a` 和 `b` 的系数为正，应选择数组中两个最大的元素；`c` 的系数为负，应选择最小元素。数组至少有三个元素，所以三者可来自不同下标。

## 复杂度

排序时间 `O(n log n)`，空间 `O(n)`。

## 边界条件与本地验证

负数同样遵循该贪心：减去最小的负数能增大结果；重复值可以从不同下标选择。使用题目两个样例和全负数数组验证。
