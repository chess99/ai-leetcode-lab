# 3341. 到达最后一个房间的最少时间 I

## 算法

将房间视为图节点，进入相邻房间的到达时间为 `max(当前时间, moveTime[邻居]) + 1`。边的等待代价非负，使用 Dijkstra 求从左上到右下的最短时间。

## 复杂度

设网格有 `N` 个房间，时间复杂度 `O(N log N)`，空间复杂度 `O(N)`。

## 边界

允许在当前房间等待；目标房间的开放时间也必须满足，开放时间为 0 时退化为普通网格最短路。
