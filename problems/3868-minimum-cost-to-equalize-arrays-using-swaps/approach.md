# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

数组内可任意重排，所以两数组最终必须拥有相同多重集合。每个值的总计数需为偶数；满足后，第一数组中多出的元素数正好是需要跨数组交换的次数。

## 复杂度

时间 O(n)，空间 O(n)。

## 边界条件与本地验证

验证交换费用为 0、1 和不可行样例。
