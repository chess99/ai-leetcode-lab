# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

总在较短数组上二分切分位置，使两数组左侧合计包含一半元素。切分正确当左侧最大值不大于右侧最小值；此时奇数总长取左侧最大，偶数总长取两侧边界平均。

## 复杂度

时间 O(log min(m,n))，额外空间 O(1)。

## 边界条件与本地验证

任一数组可为空，切分边界使用正负无穷哨兵；总元素数题目保证至少为一。
