# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护删除位置左侧的奇偶和与右侧的奇偶和。删除一个元素后，右侧元素下标奇偶翻转，据此比较新偶数位和与奇数位和。

## 正确性

左侧下标不变，右侧下标全部翻转，公式完整覆盖删除后的每个元素，因此相等时且仅当数组公平。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

删除首尾元素同样适用；已用 `[2,1,6,4]` 验证结果为 1。
