# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
题目操作序列对应二进制反射格雷码。把给定格雷码解码为普通二进制，其数值就是到零所需步数；解码可通过不断右移并异或前缀完成。

## 复杂度
时间 `O(log n)`，空间 `O(1)`。

## 边界条件与本地验证
`n=0` 返回 0；按位运算使用非负整数。
