# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

两个指针分别从两条链表出发。走到末尾后切换到另一条链表的头部，因此它们都恰好走过两条链表的总长度；若有交点会同时到达，若无交点会同时为 None。

## 复杂度

时间复杂度 O(m+n)，额外空间 O(1)。

## 边界条件与本地验证

任一头节点为空时返回 None；相同头节点会立即返回；没有交点时两个指针最终都为 None。
