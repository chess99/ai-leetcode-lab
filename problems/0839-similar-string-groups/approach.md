# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

两字符串是异位词，若不同位置数为 0 或 2 就直接相似。枚举所有字符串对，用并查集合并相似边，最终集合数量即组数。

## 复杂度

N 个长度 L 字符串，时间 `O(N²L)`，并查集空间 `O(N)`。

## 边界条件与本地验证

相同字符串差异 0 需合并；题面保证互为异位词，差异 2 自动对应一次交换。本地验证题面样例和随机排列集合的图连通分量 oracle。
