# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

长度确定后，每个允许的下一字符都对应 `2^remaining` 个合法后缀。按 `a,b,c` 枚举并跳过整块候选，定位第 k 个字符串。

## 复杂度

时间和空间均为 `O(n)`。

## 边界条件与本地验证

- 总数为 `3×2^(n-1)`，超出返回空串。
- 验证了最小、最大和越界 k。
