# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

将目标拆成完整数组轮次和余数。完整轮次固定贡献 `rounds * n` 个元素；余数部分至多跨越一次数组边界，在 `nums` 的两倍数组上用正数滑动窗口寻找和为余数的最短段。

## 复杂度

时间 `O(n)`，空间 `O(n)`（两倍数组）。

## 边界条件与本地验证

余数为 0 时无需额外段；不存在余数窗口则返回 -1；窗口允许跨越数组末尾。
