# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

BFS 求最短步数。向右只有一个目标；向左虽可到所有更小下标，但用全局指针记录尚未批量访问的最小下标，每个位置至多因“向左边”入队一次，因此不会退化为平方复杂度。

## 复杂度

每个弹簧至多入队一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

右跳越界时立即返回当前距离加一；下标 0 不产生左跳。本地验证样例，并对随机短数组显式建全图 BFS 对拍。
