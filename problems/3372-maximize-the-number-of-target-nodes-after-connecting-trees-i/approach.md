# 3372. 连接两棵树后最大目标节点数 I

## 算法

连接边对每个第一棵树节点的选择独立。第一棵树中节点 `i` 可达数固定为距离不超过 `k` 的节点数；第二棵树的连接点应使距离不超过 `k-1` 的节点数最大，因为连接边已占用一步。分别 BFS 统计并相加。

## 复杂度

每个起点 BFS 的时间为 `O(n)` 或 `O(m)`，总时间复杂度 `O(n^2+m^2)`，空间复杂度 `O(n+m)`。

## 边界

`k=0` 时不能通过连接边到达第二棵树；每次查询独立，第二棵树的最优连接点可复用。
