# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

切比雪夫距离为 `k` 的传感器覆盖边长至多 `2k+1` 的轴对齐方块。行方向至少需要 `ceil(n/(2k+1))` 个覆盖带，列方向同理；规则网格放置可同时达到两个下界，答案为两者乘积。

## 复杂度

时间与空间均为 `O(1)`。

## 边界条件

`k=0` 时每格一个传感器；覆盖边长大于网格两维时答案为 1。覆盖题目两组示例。
