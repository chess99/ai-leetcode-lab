# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

预处理每次投票后的领先者，平票时最新得票者领先；查询时二分最后一个不超过 t 的时间。

## 复杂度

初始化 `O(n)`，查询 `O(log n)`，空间 `O(n)`。

## 边界条件与本地验证

- 平票以最新获得该票数的候选人领先；题目保证查询时间不早于首票时间。对 2000 组随机投票序列各执行 100 次查询，与逐票计数和朴素时间定位对照。
