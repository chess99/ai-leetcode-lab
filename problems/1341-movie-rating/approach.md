# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

第一段查询按用户评分次数降序、姓名升序取一人；第二段限定 2020 年 2 月，按影片平均分降序、标题升序取一部，使用 `UNION ALL` 输出两行。

## 复杂度

设评分记录数为 `R`。两段查询都需要扫描并分组评分记录，时间复杂度取决于数据库执行计划，通常可视为 `O(R log R)`；聚合与排序使用的额外空间通常为 `O(R)`。

## 边界条件与本地验证

- 并列时分别按姓名和标题字典序处理。
- 日期使用左闭右开范围。
- 本地构造评分次数并列、二月均分排序及月初/月末边界数据，逐个验证两个查询分支的选择结果。
