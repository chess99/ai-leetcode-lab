# 解题记录

## 思路

对二叉搜索树进行迭代中序遍历，访问顺序即升序。每访问一个节点就清空其 `left`，并把前一访问节点的 `right` 指向它，首个节点作为链表头。

## 复杂度

设节点数为 `n`、树高为 `h`。时间复杂度为 `O(n)`，显式栈空间复杂度为 `O(h)`。

## 边界条件与本地验证

空树返回空；迭代方式避免深树递归栈溢出。已对根为 2、左右子为 1 和 3 的树断言生成 `1->2->3`，且所有 `left` 均为空。
