# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从 `[[0]]` 迭代扩展。把旧网格复制到右上、右下、左下、左上四象限，并依次加上 `0、1、2、3` 个旧面积，保证题目要求的四象限数值顺序。

## 复杂度

最终网格边长为 `2^n`，时间和空间均为 `O(4^n)`，与输出大小一致。

## 边界条件与本地验证

`n=0` 直接返回单格；覆盖 `n=1` 与 `n=2` 的象限偏移关系。
