# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium

## 思路

异或计算无进位和，按位与后左移计算进位，在 32 位掩码下迭代至进位为零。掩码使用按位取反与左移构造，整个候选不使用 `+`、`-` 或负数字面量。最高位为一时，通过按位运算恢复 Python 的有符号整数。

## 复杂度

时间 `O(1)`，空间 `O(1)`。

## 边界条件与本地验证

覆盖零、正负混合、两个负数和题目边界；用独立 oracle 穷举题目范围，并额外检查抽象语法树中不存在加减运算符。
