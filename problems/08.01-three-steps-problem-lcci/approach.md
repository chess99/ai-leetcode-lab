# 解题记录

## 思路

到达第 `i` 阶的最后一步可来自 `i-1`、`i-2` 或 `i-3` 阶，因此 `dp[i]=dp[i-1]+dp[i-2]+dp[i-3]`。用三个滚动变量保存最近状态并每轮取模。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

基础值为 `dp[1]=1`、`dp[2]=2`、`dp[3]=4`。已对 `n=3` 断言得到 4，并对 `n=5` 断言得到 13。
