# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

可交换索引的连通块内元素可任意排列。每块将最大值放到偶下标的正系数位置，其余放到负系数位置。

## 复杂度

时间 `O(n log n)`，空间 `O(n)`。

## 边界条件与本地验证

无交换时保留原排列；连通块中的偶下标数量决定正位数。
