# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

无法访问前驱时，将后继节点的值复制到当前节点，再跳过后继节点，等价于删除当前节点。

## 复杂度

时间 `O(1)`，额外空间 `O(1)`。

## 边界条件与本地验证

- 题目保证待删节点不是尾节点。

本地断言 `[4,5,1,9]` 删除值为 5 的节点后为 `[4,1,9]`，并通过 `py_compile`。
