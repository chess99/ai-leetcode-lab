# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

动态规划维护处理到当前位置时“不接当前预约”和“接当前预约”的最大时长。接当前预约只能衔接前一个不接的状态；不接则取前一轮两种状态的较大值。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

空预约序列返回零；本地断言覆盖三个题目样例和空数组。
