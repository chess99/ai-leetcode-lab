# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

长度 L 的连续正整数和为 `L*x+L(L-1)/2=n`。枚举满足最小起点仍为正的 L，若扣除三角数后能整除 L，就存在唯一正整数起点。

## 复杂度

L 只到 `O(sqrt n)`，时间 `O(sqrt n)`、空间 `O(1)`。

## 边界条件与本地验证

长度 1 恒合法；循环条件严格保证起点正。本地 n≤若干千与直接枚举起点、累加连续数的 oracle 对拍。
