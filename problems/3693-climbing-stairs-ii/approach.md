# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

令 `dp[j]` 为到达第 `j` 级的最小总成本。最后一步只能来自 `j-1、j-2、j-3`，枚举合法前驱，加上落在 `j` 的台阶成本和跳跃距离平方。

## 复杂度

每级只检查至多 3 个前驱，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

第 0 级成本为 0；靠近起点时只枚举非负前驱。验证三个题面样例及 `n=1`。
