# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

区间 DP 从左端点倒序展开。若两端字符相同，答案为内部区间加二；否则取去掉左端或右端后的较大值。用一维数组保存上一行，`diagonal` 保存更新前的内部区间值。

## 复杂度

时间 `O(n²)`，空间 `O(n)`。

## 边界条件与本地验证

空串返回 0，单字符返回 1。对 800 组随机短串枚举全部子序列并筛选回文，对拍通过。
