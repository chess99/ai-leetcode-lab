# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；Created：2026-08-11T22:15:55Z

## 思路

先筛出质数。滑动窗口内以两个单调队列维护质数的最小和最大值，左端收缩至差值不超过 `k`；再用质数位置队列计算以当前右端结尾、且至少含两个质数的合法左端数。

## 复杂度

筛法和窗口均为 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

窗口没有两个质数时不计数；非质数不影响极值。已覆盖两个样例及全非质数数组。
