# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

只有两种策略：保持下标逐项修改，或先支付一次 `k` 后任意重排。后一种情况下排序后按序配对可最小化绝对差之和，取两者较小值。

## 复杂度

时间 `O(n log n)`，额外空间为排序所需 `O(n)`。

## 边界条件与本地验证

验证不重排更优、重排更优、数组元素重复及 `k` 很大。
