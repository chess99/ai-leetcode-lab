# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

直接以每行第 `k` 列成绩为键进行降序排序，返回排序后的行。

## 复杂度

时间 `O(m log m)`，排序结果额外空间 `O(m)`。

## 边界条件与本地验证

`k` 可为第一列或最后一列；题目保证所有成绩不同，因此无需处理同分规则。已验证两个题面样例。
