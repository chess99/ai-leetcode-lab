# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

扫描前缀和，双端队列保存前缀和严格递增的候选下标。当前前缀减队首达到 k 时不断弹首并更新最短长度；队尾前缀不小于当前值时，它对未来既更早且更大，必劣，弹出。

## 复杂度

每个前缀入队出队一次，时间 `O(n)`、空间 `O(n)`。

## 边界条件与本地验证

负数使普通滑窗失效；无解返回 -1，单元素达标返回 1。本地随机短数组枚举所有子数组和对拍。
