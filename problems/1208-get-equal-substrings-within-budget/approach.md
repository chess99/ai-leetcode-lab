# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

滑动窗口维护当前子串转换成本。右端扩张后若成本超预算，持续移动左端并扣除离开字符的成本；每个合法窗口都用于更新最大长度。

## 复杂度

时间复杂度为 `O(n)`，额外空间为 `O(1)`。

## 边界条件与本地验证

- 预算为 0 时只能保留对应字符相同的连续段。
- 单字符和整个字符串均可作为窗口。
- 本地验证了题目示例和零预算情况。
