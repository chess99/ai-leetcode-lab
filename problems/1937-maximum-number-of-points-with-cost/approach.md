# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

逐行 DP。用左右扫描分别维护 `dp[j]+j` 和 `dp[j]-j` 的最大值，在 `O(n)` 时间得到每列扣除距离成本后的最优前驱。

## 正确性

任一前驱位于当前列左或右，两次扫描分别覆盖这两类并取最大，等同完整 DP 转移。

## 复杂度

时间 `O(mn)`，空间 `O(n)`。

## 边界条件与本地验证

单行直接取最大值；使用大负初值处理任意分数。已用样例验证。
