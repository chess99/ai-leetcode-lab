# 解题记录

## 思路

使用 `melt` 固定 `product` 标识列，把四个季度列的列名收集到 `quarter`，对应值收集到 `sales`，得到每个产品每个季度一行的长表。

## 复杂度

设有 n 个产品、q 个季度列，时间与输出空间复杂度均为 O(nq)。本题 q = 4。

## 边界条件与本地验证

验证了多产品和单产品输入，确认输出列严格为 `product`、`quarter`、`sales`，并且按季度列原顺序展开。本地运行环境为 Pandas 2.2.3。
