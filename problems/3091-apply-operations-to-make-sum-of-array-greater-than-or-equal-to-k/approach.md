# 解题记录

## 思路
若先把一个元素增加到 `value`，需要 `value-1` 次操作；复制该元素直到总和至少为 `k`，还需 `ceil(k/value)-1` 次。枚举 `value=1..k` 取最小值。

## 复杂度
时间复杂度 `O(k)`，空间复杂度 `O(1)`。

## 边界条件与本地验证
`k=1` 无需操作；整除与非整除分别由向上取整处理。已覆盖 `k=11` 与 `k=1`。
