# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

单调递减栈保存尚未找到更大值的下标。遍历两倍数组并用模运算模拟环形；当前值更大时持续弹栈并填写答案，只在第一轮压栈，避免重复状态。

## 复杂度

每个下标至多入栈、出栈一次，时间 `O(n)`，空间 `O(n)`。

## 边界条件与本地验证

覆盖单元素、重复值、严格递增和严格递减。对 2000 组随机数组从每个位置向后环形枚举首个更大值，对拍通过。
