# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

操作后最多只保留一个 0。它位于原首个 0 的位置加上零总数减一处，其余位置全为 1。

## 正确性

可通过操作把零不断向右合并；唯一保留的零越靠右字典序越大，公式给出其最右可达位置。

## 复杂度

时间 `O(n)`，空间 `O(n)`（构造结果）。

## 边界条件与本地验证

没有 0 时字符串不变。已用题目三个样例验证。
