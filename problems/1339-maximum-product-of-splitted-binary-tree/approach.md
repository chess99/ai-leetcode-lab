# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

后序遍历记录每个子树和。切断该子树与父节点的边后两部分和为 `sum` 和 `total-sum`，枚举取最大乘积后取模。

## 复杂度

时间和空间均为 `O(n)`。

## 边界条件与本地验证

- 乘积比较必须在取模前完成，整棵树对应的零乘积不影响最大值。
- 对 1000 棵随机正值树独立枚举每条父子边对应的子树和，验证最大乘积；延迟类型注解保证模块可独立加载。
