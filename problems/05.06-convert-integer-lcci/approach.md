# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

异或结果中为一的位正是需要翻转的位。将结果限制为 32 位后，反复清除最低位的一个一并计数。

## 复杂度

时间复杂度为 `O(k)`，其中 `k` 为不同位数，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

相同整数结果为零，负数按 32 位补码处理；本地断言覆盖两个示例和相同整数。
