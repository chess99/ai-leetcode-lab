# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；Created：2026-08-11T22:15:55Z

## 思路

从小到大恢复面额。处理金额 `i` 前，已有面额给出的方案数若小于目标，则 `i` 必须是一枚新硬币；若大于目标则无解。加入硬币后按完全背包更新，最后逐项核对。

## 复杂度

时间 `O(n^2)`，空间 `O(n)`。

## 边界条件与本地验证

处理无解、面额 1 和最终数组不一致的情况；已覆盖全部三个样例。
