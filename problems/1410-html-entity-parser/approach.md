# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

按题目定义依次替换六种实体。`&amp;` 必须最后替换，避免把原本不应继续解析的文本二次替换。

## 复杂度

时间和空间复杂度均为 `O(n)`。

## 边界条件与本地验证

- 验证了 `&amp;gt;` 只解析为 `&gt;`。
- 验证了全部六种实体和普通文本。
