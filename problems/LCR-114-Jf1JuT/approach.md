# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

比较相邻单词的第一个不同字符建立有向边；若长单词在短前缀相同的情况下排在前面则直接非法。随后用 Kahn 拓扑排序输出所有出现字符，未能输出全部表示存在环。

## 复杂度

总字符数为 `C`，建图 `O(C)`，拓扑排序 `O(V+E)`，空间 `O(V+E)`。

## 边界条件与本地验证

重复约束边只计一次入度；无约束字符也必须输出。本地验证三个样例及前缀非法、环边界。
