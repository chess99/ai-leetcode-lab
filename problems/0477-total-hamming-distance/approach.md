# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

逐位统计。某一位有 `ones` 个数为 1、其余 `zeros` 个为 0，则恰有 `ones × zeros` 个无序数对在该位不同。累加题目范围内的 30 个二进制位即可。

## 复杂度

时间 `O(30n)`，即 `O(n)`；额外空间 `O(1)`。

## 边界条件与本地验证

单元素和全相同数组返回 0。对 2000 组随机数组枚举所有数对并计算异或的置位数，对拍通过。
