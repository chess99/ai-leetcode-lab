# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
逐行 DP。上一行只需找最小值及其列和次小值；当前列若与最小值同列就接次小值，否则接最小值。

## 复杂度
时间 `O(n²)`，滚动数组空间 `O(n)`。

## 边界条件
单格矩阵直接返回该值；负数不影响最小值转移；同列禁止连续选择。
