# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

动态规划状态为到达当前格且已中和 0、1、2 个负数时的最大收益。状态仅从上方或左方转移；正常收取当前值，或在剩余次数允许时跳过当前负数的影响。

## 复杂度

时间 `O(mn)`，空间 `O(n)`（每列保留三个状态）。

## 边界条件与本地验证

验证起点为负数、两次中和都被使用、全正网格和单行/单列。
