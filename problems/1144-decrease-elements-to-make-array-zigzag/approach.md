# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

分别计算偶数下标和奇数下标作为谷底的成本。谷底必须严格小于两侧邻居，若当前值不满足，只能递减到较小邻居减一；各谷底彼此不相邻，因此成本可直接累加，取两种模式的较小值。

## 复杂度

数组扫描两次，时间为 `O(N)`，额外空间为 `O(1)`。

## 边界条件与本地验证

- 首尾只有一个邻居。
- 相等元素也需至少递减一次。
- 本地覆盖题目两个示例、单元素和全相等数组。
