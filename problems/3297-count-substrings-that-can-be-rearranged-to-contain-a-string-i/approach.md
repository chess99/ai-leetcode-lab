# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

滑动窗口维护频次。窗口覆盖 `word2` 后，固定右端的所有更长子串也有效，累加后收缩左端。

## 复杂度

时间 `O(n * 字符集大小)`，空间 `O(字符集大小)`。

## 边界条件与本地验证

覆盖重复字符需求、无有效窗口和整个串恰好有效。
