# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
加入当前最高木棍：放在最左端会新增一根可见木棍；放在其余 `n-1` 个位置不会改变可见数。因此 `dp[n][k]=dp[n-1][k-1]+(n-1)dp[n-1][k]`，滚动计算。

## 复杂度
时间 $O(nk)$，空间 $O(k)$。

## 边界条件与本地验证
一根木棍恰有一种排列且可见数为一。已枚举小 `n` 全排列对拍。
