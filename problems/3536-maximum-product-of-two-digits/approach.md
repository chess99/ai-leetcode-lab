# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

将整数转换为数字列表并按降序排序。所有数字均非负，最大的两个数字相乘一定得到最大乘积。

## 复杂度

设整数位数为 `d`。排序时间复杂度为 `O(d log d)`，额外空间复杂度为 `O(d)`。

## 边界条件与本地验证

题目保证至少有两位数字；相同数字出现多次时可各自作为两个位置参与乘积。
