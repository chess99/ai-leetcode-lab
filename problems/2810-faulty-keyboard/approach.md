# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

用双端队列存储已输入的字符，用布尔值记录当前书写方向。遇到 `i` 只翻转方向，普通字符加入对应一端。

## 复杂度

时间复杂度 O(n)，额外空间复杂度 O(n)。

## 边界条件与本地验证

连续 `i` 会抵消方向变化。已进行 Python 语法检查及样例断言。
