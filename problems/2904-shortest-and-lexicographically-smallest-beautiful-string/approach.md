# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

滑动窗口维护至多 k 个 `1`。当恰有 k 个时，移除左端多余零得到以当前右端结尾的最短候选；比较长度，再比较字典序。

## 复杂度

时间 `O(n^2)`（候选切片比较），空间 `O(1)`。

## 边界条件与本地验证

没有 k 个 `1` 时返回空串；前导零不会属于最短候选。
