# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

迭代遍历树并恢复节点值：根为 0，左孩子为 `2x+1`，右孩子为 `2x+2`。同时将恢复后的值写入集合，使查询可常数时间完成。

## 复杂度

初始化时间和空间为 `O(n)`，每次查询为 `O(1)`。

## 边界条件与本地验证

- 空树的查询均为假。
- 本地验证了根、左右子节点及不存在值。
