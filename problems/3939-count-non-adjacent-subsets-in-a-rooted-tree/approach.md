# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

树形 DP 按和模 `k` 统计。`no` 表示不选当前点，合并子树的选与不选；`yes` 表示选当前点，只能合并子树不选根的状态。最后排除空集。

## 复杂度

`O(nk^2)` 时间，`O(nk)` 递归状态空间。

## 边界条件与本地验证

题面两例通过，并与 500 组小树子集枚举对拍通过。
