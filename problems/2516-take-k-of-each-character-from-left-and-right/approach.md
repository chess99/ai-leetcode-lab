# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

从两端取走的字符之外必为一个连续中间窗口。最大化保留窗口，且每种字符的窗口计数不超过总数减 `k`；滑动窗口维护该限制，答案是总长减最大窗口长。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`（仅三个字符计数）。

## 边界条件与本地验证

任一字符总数不足 `k` 时返回 `-1`；`k=0` 可保留整个字符串。已验证题面样例及不足情形。
