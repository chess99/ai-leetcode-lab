# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

列和为 2 的列必须上下都填 1，先扣除两行配额。对列和为 1 的列，将 1 分配给当前剩余配额更多的一行；最后两行配额都恰为 0 时构造成功。

## 复杂度

时间复杂度为 `O(n)`，额外空间为 `O(n)`。

## 边界条件与本地验证

- 列和为 2 可能直接使行配额变负，最终判定失败。
- 无法耗尽两行配额时返回空数组。
- 本地验证了题目示例和无解情形。
