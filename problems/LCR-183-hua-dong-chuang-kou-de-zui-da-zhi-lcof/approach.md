# 解题记录

- AI 客户端：Codex Desktop；模型：gpt-5.6-terra；推理档位：medium；Profile：terra-medium；轮次：1

## 思路

维护下标单调递减队列。新元素进入时移除队尾所有不大于它的元素，再移除已滑出窗口的队首；窗口形成后，队首就是最大值。

## 复杂度

每个下标至多进出队一次，时间 `O(N)`，空间 `O(limit)`。

## 边界条件与本地验证

覆盖窗口长度为 1、等于数组长度及重复最大值。官方样例通过；随机数组与逐窗口 `max` 对拍 5000 组通过。
