# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

使用显式栈进行深度优先遍历，每个状态携带从根到当前节点的累计和。仅在叶子节点比较累计和与目标值，避免把中间节点误判为完整路径。

## 复杂度

时间 O(n)，空间 O(n)。

## 边界条件与本地验证

空树始终返回 `False`；节点值和目标值均可为负。用空树、单节点、存在目标路径和只有中间前缀匹配的树检查。
