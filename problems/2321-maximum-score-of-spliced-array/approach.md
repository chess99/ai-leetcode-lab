# 解题记录

## 思路

若以 nums1 为最终计分数组，交换区间的收益是该区间 `nums2[i]-nums1[i]` 的和，用 Kadane 求最大非负子段收益；反方向同理。两种原数组总和加对应最大收益后取最大值。

## 复杂度

两遍线性扫描，时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

允许不交换，故收益下界为零。三个题面样例得到 `210、220、31`；随机小数组枚举全部交换区间及两个计分方向，对拍 1000 组。
