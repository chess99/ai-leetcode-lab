# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

题目要求重复值时返回原数组中最小下标，而旋转点可能把同一数值拆到数组两端；仅靠常规二分无法安全排除左侧同值。按下标从左到右扫描，第一次命中立即返回，因此直接保证最小下标。

## 复杂度

最坏时间 O(n)，额外空间 O(1)。题目允许重复值，二分在此约束下同样可能退化。

## 边界条件与本地验证

目标不存在返回 -1；全重复数组和目标跨旋转点重复的情形均正确。
