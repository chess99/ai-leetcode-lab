# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

遍历矩阵，遇到未访问的零就用栈进行八方向深度优先搜索，访问过的水域原地标记为非零并累加面积；最后排序全部面积。

## 复杂度

每格最多入栈一次，时间 O(mn log p)（末尾排序 p 个池塘），搜索空间最坏 O(mn)。

## 边界条件与本地验证

八方向包含对角连接；原地标记避免重复统计。补齐 `List` 类型导入，并验证模块可在无预注入
环境中独立加载。
