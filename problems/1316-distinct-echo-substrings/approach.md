# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
枚举重复单元长度和起点，比较相邻两个等长片段；相等时把重复单元加入集合，集合负责按字符串内容去重。

## 复杂度
切片比较计入后最坏时间 `O(n³)`，集合保存结果的空间最坏 `O(n²)`。

## 边界条件
重复单元长度最多为字符串一半；相同回声子串在不同位置只计一次。
