# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

令最小值为 `m`。若存在元素不能被 `m` 整除，可通过取模造出比 `m` 更小的非零值，最终可缩至长度 1。否则所有数都是 `m` 的倍数，只有成对消去最小值后才能留下零，答案为最小值出现次数的上取整一半。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

最小值只出现一次时答案为 1；全数组相等时答案由元素个数的奇偶性决定。
