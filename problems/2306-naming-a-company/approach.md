# 解题记录

## 思路

按首字母分组保存后缀。对任意两组，公共后缀换首字母后仍是已有名字，不能使用；两边各自非公共后缀可任意配对，并有两种有序排列，因此贡献 `2*(sizeA-common)*(sizeB-common)`。

## 复杂度

26 组两两求集合交集，时间与后缀总量的常数倍同阶，空间 `O(n)`。

## 边界条件与本地验证

所有名字互异，但不同首字母可有相同后缀。题面样例得到 `6、0`；随机小名字集合直接枚举所有有序名字对并交换首字母，对拍 1000 组。
