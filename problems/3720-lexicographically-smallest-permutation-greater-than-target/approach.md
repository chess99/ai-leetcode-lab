# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:50Z

## 算法

先尽量用 `s` 的字符匹配 `target` 前缀。若无法继续或完整匹配，则从当前位向左回退，恢复该位字符，并尝试放入严格更大的最小字符；剩余字符升序排列，即为该前缀下的最小答案。最靠右可成功的位置产生全局最小答案。

## 复杂度

最多回退 `n` 位，每位扫描 26 个字母并构造一次最终答案，时间 `O(26n+n)`，空间 `O(n)`。

## 边界

最大排列仍不大于目标时返回空串；正确处理重复字符和完全相等的排列。
