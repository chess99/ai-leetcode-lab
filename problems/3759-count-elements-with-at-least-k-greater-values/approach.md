# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:54Z

## 算法

当 `k>0` 时，降序排列后的第 `k` 个值作为阈值。元素严格小于该阈值，必有至少前 `k` 个元素严格大于它；大于等于阈值则严格更大元素不足 `k`。统计小于阈值的原数组元素。

## 复杂度

时间 `O(n log n)`，空间 `O(n)`。

## 边界

`k=0` 时所有元素都合格；重复阈值必须用严格小于判断。
