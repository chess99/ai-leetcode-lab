# 解题记录

## 思路

若两根木材都不超过 k，无需切割。可运输条件保证至多一根木材超过 k；设其长度为 L，因为 L 不超过 2k，只需切一次。两段都不超过 k 时，乘积成本在可行区间端点最小，因此切成 k 和 L-k，成本为 `k * (L-k)`。

## 复杂度

时间复杂度 O(1)，额外空间复杂度 O(1)。

## 边界条件与本地验证

覆盖了无需切割、n 超限和 m 超限三种情况，并对小规模合法输入枚举所有切割位置核对最小成本。
