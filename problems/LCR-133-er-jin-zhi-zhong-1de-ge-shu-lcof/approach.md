# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

利用 `n & (n - 1)` 每次会清除最低位的一个 `1`。重复此操作直到 `n` 为零，执行次数即为汉明重量。

## 复杂度

时间复杂度为 `O(k)`，其中 `k` 是二进制中 `1` 的数量；额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

零的汉明重量为零；本地断言覆盖了三个题目样例。
