# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
加入第 `i` 组订单时，取件可插入已有序列的 `2i-1` 个位置，送件再插在取件之后的 `i` 个等价位置，总乘数为 `i(2i-1)`，逐组相乘。

## 复杂度
时间 `O(n)`，空间 `O(1)`。

## 边界条件
每个送件必须位于对应取件之后；每步乘积对 `1_000_000_007` 取模。
