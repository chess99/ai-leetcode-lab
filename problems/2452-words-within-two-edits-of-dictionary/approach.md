# 2452. 距离字典两次编辑以内的单词
## 算法
对每个查询词与每个字典词逐位比较，若存在不同位置数不超过 2 的字典词，就保留该查询词。
## 正确性说明
所有单词等长且编辑仅允许替换，因此最少编辑次数恰等于不同位置数。算法直接检查这一充要条件，并保持查询原顺序。
## 复杂度分析
时间 `O(QDL)`，输出外空间 `O(1)`。
## 边界情况
完全相同需 0 次编辑；恰有两处不同仍合法。
