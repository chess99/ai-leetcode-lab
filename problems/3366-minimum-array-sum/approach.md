# 3366. 最小化数组和

## 思路

## 算法

动态规划状态 `dp[a][b]` 为处理完前若干元素、已用操作 1 和操作 2 次数时的最小和。每个元素可不操作、单独用任一操作，或两种操作都用；双操作要分别检查“先减 k 再除二”和“先除二再减 k”的合法结果。

## 复杂度

时间复杂度 `O(n*op1*op2)`，空间复杂度 `O(op1*op2)`。

## 边界

操作 2 必须在操作当时元素不小于 `k`；两种操作可作用在同一元素但各至多一次，故需考虑两种顺序。
