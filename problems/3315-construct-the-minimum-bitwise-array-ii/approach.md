# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

偶数不能写成 `x | (x+1)`。对奇数，清除末尾连续 `1` 中最高的那一位，得到最小可行 `x`。

## 复杂度

时间 `O(n)`，空间 `O(1)`（不计返回数组）。

## 边界条件与本地验证

覆盖偶数、`1` 和连续低位为 `1` 的奇数。
