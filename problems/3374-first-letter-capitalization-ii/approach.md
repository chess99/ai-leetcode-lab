# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

用递归 CTE 将每个内容串展开为字符位置。首字符以及前一字符为连字符的位置使用大写，其余字符使用小写；按原位置 `GROUP_CONCAT` 拼回字符串，并保留原表记录的标识列。

## 复杂度

设所有文本总长度为 `L`，时间和递归中间结果均为 `O(L)`。

## 边界条件与本地验证

覆盖单词、多个连字符、原本全大写或全小写的字符串；SQL 使用显式字符位置保持原序。
