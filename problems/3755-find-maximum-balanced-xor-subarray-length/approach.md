# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:53Z

## 算法

维护前缀异或与偶奇数量差（偶数 `+1`、奇数 `-1`）。两个前缀状态 `(xor,balance)` 相同，则它们之间子数组异或为 0 且偶奇数相等。记录每个状态最早下标以最大化长度。

## 复杂度

时间 `O(n)`，空间 `O(n)`。

## 边界

初始空前缀状态放在下标 `-1`；不存在合法非空子数组时返回 0。
