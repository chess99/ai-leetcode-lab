# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

在给定范围内，`1` 到 `999` 不含逗号，而 `1000` 到 `n` 的每个数恰好含一个逗号。因此答案就是这一段的元素个数。

## 复杂度

时间复杂度为 `O(1)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

`n < 1000` 时结果为 `0`；本地断言覆盖了无逗号、刚到阈值和样例范围。
