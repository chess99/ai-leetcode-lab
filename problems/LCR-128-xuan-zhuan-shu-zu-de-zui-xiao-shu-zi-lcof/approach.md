# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

二分比较中点与右端点：中点更大说明最小值在右半边，更小说明在左半边（含中点）；相等时右端点可安全左移一位以消除重复。

## 复杂度

平均时间复杂度为 `O(log n)`，重复元素较多时最坏为 `O(n)`；额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

数组可含重复值且可能未旋转；本地断言覆盖了两个样例及重复元素。
