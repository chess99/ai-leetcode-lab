# 解题记录

## 思路
枚举每个字符串的所有子串，用集合记录该子串出现在哪些数组下标。随后对每个字符串按长度递增检查自己的子串：只属于当前下标的候选中取字典序最小者；首个有候选的长度即为答案。

## 复杂度
设字符串最大长度为 `L`、数组长度为 `n`，时间复杂度 `O(nL^3)`，空间复杂度 `O(nL^2)`。

## 边界条件与本地验证
重复字符串没有独有子串；同长度答案取字典序最小。已覆盖 `["cab","ad","bad","c"]`。
