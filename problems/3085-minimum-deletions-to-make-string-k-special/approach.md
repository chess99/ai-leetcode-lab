# 解题记录

## 思路
统计各字符频次，并枚举保留下来的最小正频次 `base`。小于 `base` 的字符必须全部删除，大于 `base+k` 的字符删到上界；取最小删除数。

## 复杂度
设不同字符数为 `u≤26`，时间复杂度 `O(u^2)`，空间复杂度 `O(u)`。

## 边界条件与本地验证
只保留一种字符时一定可行；`k=0` 要求所有保留频次相同。已覆盖 `"aabcaba", k=0`。
