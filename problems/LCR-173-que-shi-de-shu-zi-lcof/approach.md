# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

缺失数前的位置满足 `records[i] == i`，之后不满足。二分寻找第一个不满足的位置，其下标即缺失数。

## 复杂度

时间复杂度为 `O(log n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

缺失末尾编号时返回数组长度；本地断言覆盖两个样例及该边界。
