# 解题记录

## 思路

每个字符由其右侧第 k 个字符替换，整体效果等价于把字符串循环左移 k 位。先对字符串长度取模，再拼接切分后的右半段和左半段。

## 复杂度

设字符串长度为 n，时间复杂度 O(n)，结果字符串所需空间为 O(n)。

## 边界条件与本地验证

当 k 是字符串长度的倍数时结果保持不变；单字符和重复字符也能自然处理。已用逐位置取模构造的 oracle 核对不同长度与位移量。
