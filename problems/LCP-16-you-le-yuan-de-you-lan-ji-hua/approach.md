# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 结论

本轮未实现。问题等价于对每个重点顶点，选两个允许重复的三角形，使两个三角形顶点并集的点权和最大。朴素枚举三角形或三角形对最坏均为超线性高次复杂度；本轮没有证明可靠的 `O((N+M) polylog N)` 枚举/截断方法，因此保留明确的 `NotImplementedError`，避免提交未经证明的启发式解。

## 复杂度

未实现，无复杂度声明。

## 边界条件与本地验证

已核对无三角形时应返回 0，以及同一个三角形可在上午、下午重复选择；当前实现会明确抛出异常，不会静默返回错误答案。
