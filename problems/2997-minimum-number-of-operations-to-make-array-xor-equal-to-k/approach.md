# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先计算数组的异或和，再与 `k` 异或。差值中每个为 1 的二进制位都必须翻转一次；翻转任意一个元素的该位都会且只会改变总异或的该位，因此这些操作也足够。

## 复杂度

时间 `O(n)`，额外空间 `O(1)`。

## 边界条件与本地验证

异或已经等于 `k` 时差值为零。前导零同样可以翻转，故只需统计差值的所有置位。
