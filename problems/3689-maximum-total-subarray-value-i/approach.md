# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

任意子数组的极差不超过整个数组的 `max(nums)-min(nums)`；包含全局最大值和最小值之间区间的子数组达到该上界。同一个子数组允许重复选择，所以将该最优区间选择 `k` 次即可。

## 复杂度

时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

单元素或所有元素相同时极差为 0；`k` 可大于子数组数量但允许重复。验证两个题面样例。
