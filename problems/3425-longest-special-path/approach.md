# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

把树根定为 0。DFS 路径上记录每个节点值最近出现的深度，当前合法祖先起点必须越过重复值上一次出现的位置。根到当前点的距离前缀可常数时间算出窗口长度，并按长度最大、节点数最少更新答案；回溯时恢复最近深度。

## 复杂度

时间复杂度 O(n)，空间复杂度 O(n)。

## 边界条件与本地验证

重复值会把左端点单调右移；全重复树得到 [0,1]。随机小树已枚举所有祖先到后代路径对拍。
