# 解题记录

- Profile：terra-medium；Created：2026-08-12T02:47:50Z

## 算法

枚举 `k=1..n`，分别构造反转长度为 `k` 的前缀和后缀所得字符串，维护字典序最小值。

## 复杂度

构造和比较字符串均为 `O(n)`，总时间 `O(n^2)`，空间 `O(n)`。

## 边界

`k=1` 允许字符串不变，因此长度 1 和本来最优的字符串都能正确处理。
