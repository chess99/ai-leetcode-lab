# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
按天数做分组 DP。枚举当天结束位置，再反向枚举当天开始位置并维护区间最大难度，与前一天的最优前缀代价相加。

## 复杂度
时间 `O(dn²)`，滚动数组空间 `O(n)`。

## 边界条件
每天至少安排一个任务；任务数少于天数返回 -1；任务顺序不可改变。
