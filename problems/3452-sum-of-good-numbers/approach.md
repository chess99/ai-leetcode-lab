# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

逐个检查元素。若 `i-k` 或 `i+k` 在边界内，则当前值必须严格大于对应值；不存在的比较条件自动满足。满足左右条件的元素累加到答案。

## 复杂度

设数组长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

靠近两端的元素可能只需比较一个位置；相等不满足严格大于条件。
