# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

不同分数的简历相对位置固定，必定在两人的同一位置。对于同一分数的一组简历，两份独立随机排列期望匹配数为 `1`。因此总期望等于不同分数的个数。

## 复杂度

时间复杂度为 `O(n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

所有分数相同的期望为 `1`，全部不同则为数组长度；本地断言覆盖了三个题目样例。
