# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

递归编码每棵子树；子树进入和返回分别记为 `0`、`1`。为使字典序最小，排序每个孩子的完整编码；根的最终返回不需记录。

## 复杂度

`O(n log n)` 时间，`O(n)` 空间。

## 边界条件与本地验证

单节点返回空串；提高递归深度以覆盖 1e4 节点的链。题面两例返回 `00110` 与 `00101100`，随机小树与枚举所有子节点访问顺序的 oracle 对拍。
