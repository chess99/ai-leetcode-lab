# 2369. 检查数组是否存在有效划分

## 算法

令 `valid[i]` 表示前 `i` 个元素能否有效划分。逐个结尾检查最后一段是否为两个相等数、三个相等数或三个连续递增数，且此前前缀有效时标记为真。

## 正确性说明

有效划分的最后一段只能是题目规定的三种形式。算法逐一检查它们并要求剩余前缀有效，既不会接受无效划分，也不会遗漏任何有效划分。

## 复杂度分析

时间复杂度为 `O(n)`，空间复杂度为 `O(n)`。

## 边界情况

- 长度为 2 时只能由两个相等数构成。
- 连续三数的相邻差必须为 1。
- 无法覆盖完整数组时返回假。
