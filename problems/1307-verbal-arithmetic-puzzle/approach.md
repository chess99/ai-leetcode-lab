# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
按竖式从低位到高位、每列按单词逐个回溯。加数列完成后，结果位数字由列和模 10 唯一确定，并把整除 10 的部分作为进位递归下一列。

## 复杂度
最多为 10 个字母的排列搜索，最坏 `O(10!)`，映射与递归栈空间 `O(字母数+列数)`。

## 边界条件
多位单词首字母不能为零；不同字母不能共用数字；超过 10 个不同字母立即失败。
