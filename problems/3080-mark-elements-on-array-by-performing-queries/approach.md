# 解题记录

## 思路
先按 `(数值, 下标)` 排序，维护指向尚未检查最小元素的指针、标记数组和未标记元素总和。每个查询先标记指定下标，再从排序序列跳过已标记项并标记所需数量，记录剩余总和。

## 复杂度
排序 `O(n log n)`，所有查询中的指针总共前进 `O(n)`，总时间 `O(n log n+q)`，空间复杂度 `O(n)`。

## 边界条件与本地验证
指定下标已经标记时不可重复扣减；`k` 超过剩余元素时自然停止。已覆盖题目示例。
