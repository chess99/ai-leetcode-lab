# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

逐行维护每列向上连续 1 的高度。固定每个右边界向左扩展，沿途高度最小值就是该宽度可形成的全 1 子矩形数量，累加即可。

## 复杂度

时间 `O(m*n^2)`，空间 `O(n)`。

## 边界条件与本地验证

0 会将对应高度清零。已用题目两个样例验证。
