# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

从目标反推最后一步：较大坐标要么由自身翻倍得到，要么由另一坐标相加得到；相等坐标只可能由坐标轴上的状态抵达。每步严格减小目标，无法满足反向规则即不可达。

## 复杂度

每步至少使较大坐标减半或减少另一坐标，时间 `O(log max(tx,ty))`，空间 `O(1)`。

## 边界条件与本地验证

处理零坐标、相等坐标和起终点相同。小坐标全量 BFS 对拍通过。
