# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

维护末尾连续等于 `value` 的长度。新数相等则加一，否则清零；长度达到 `k` 后返回真。

## 复杂度

每次 `consec` 为 `O(1)` 时间和 `O(1)` 空间。

## 边界条件与本地验证

达到 `k` 后后续连续相等仍为真；任一不等数字会重置计数。已验证题面操作序列。
