# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium

## 思路

分别累加偶数下标和奇数下标上的数字。两个和相等时字符串平衡，否则不平衡。

## 复杂度

设字符串长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(1)`。

## 边界条件与本地验证

字符串长度可以为偶数或奇数；最后一个字符只会归入其对应奇偶下标的和。
