# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

高于 n 的位固定。可选择位从高到低处理：原位相同可令异或后的两位都为 1；原位不同时，这一位只能给其中一个数，给当前高位前缀较小者以平衡乘积。

## 复杂度

时间 O(n)，额外空间 O(1)。

## 边界条件与本地验证

n 可为 0；原位同为 1 时应令 `x` 的该位为 0，才能让两个异或结果位都保持为 1；仅最终乘积取模。
