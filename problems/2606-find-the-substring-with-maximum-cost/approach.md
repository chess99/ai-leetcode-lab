# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先建立字符的实际价值映射，未覆盖的字符使用字母序号。将字符串变为价值序列后应用 Kadane 算法：维护以当前位置结尾的最大非负和，若其变负便从下一位置重新开始，并记录全局最大值。

## 复杂度

时间 `O(|s| + |chars|)`，额外空间 `O(|chars|)`。

## 边界条件与本地验证

空子串允许且价值为零，因此当前和与答案都从零开始；所有字符为负值时返回零。
