# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

先找最长严格递增前缀；若整数组递增，所有删除区间都合法。否则从右向左扩展严格递增后缀，并用单调指针统计能与当前后缀首值衔接的前缀保留长度，每个后缀位置对应一批删除区间。

## 复杂度

双指针均单调，时间 `O(n)`，空间 `O(1)`。

## 边界条件与本地验证

删除后可为空或只剩一侧，比较必须严格小于。题面样例得到 `10、7、3`；随机短数组枚举所有删除区间对拍通过。
