# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
维护全局仿射变换 `x·mul+add`，追加时用模逆归一化原值。乘零会使所有已有元素坍缩为同一前缀值，此时重置仿射基准并记录前缀长度，后续元素重新归一化。

## 复杂度
各操作摊还 `O(log MOD)`（模逆），查询 `O(1)`，空间 `O(append次数)`。

## 边界条件与本地验证
下标越界返回 -1；必须专门处理乘数为零，因为零没有模逆；所有值取模。
