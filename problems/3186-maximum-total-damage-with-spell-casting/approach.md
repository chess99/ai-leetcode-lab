# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

相同伤害值互不冲突，应全部选择或全部不选。按不同伤害升序动态规划；选择值 `v` 时只能衔接不大于 `v-3` 的状态，二分找到该状态并加上 `v * frequency[v]`。

## 复杂度

时间 `O(n log n)`，空间 `O(n)`。

## 边界条件与本地验证

伤害差正好为 3 可以同时使用；相同伤害值的所有咒语都可累计。
