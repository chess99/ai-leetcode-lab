# 解题记录

## 思路

统计不同字符的频次。若不同字符数超过 k，必须完整删除若干字符的所有出现；为了让删除数最少，应保留频次最大的 k 种字符，删除频次最小的其余种类。

## 复杂度

设字符串长度为 n、不同字符数为 u，时间复杂度 O(n + u log u)，额外空间复杂度 O(u)。

## 边界条件与本地验证

当不同字符数不超过 k 时返回 0；相同频次的字符删除任意一种成本相同。对小字符串枚举所有保留字符集合并与结果核对。
