# 解题记录

- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路

后序 DFS 为每个节点返回“选择该节点”和“不选该节点”两个收益。选择节点时只能加两个孩子的不选收益；不选节点时各孩子取两种收益的较大值。根节点取两者最大。

## 复杂度

每个节点计算一次，时间 `O(n)`，递归栈 `O(h)`。

## 边界条件与本地验证

- 空节点收益为两个零。
- 本地断言：两个题目示例分别为 7 和 9，单节点返回该节点值。
