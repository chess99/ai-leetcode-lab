# 解题记录

## 思路

统计频次，累加所有达到最大频次的元素出现数。

先统计每个不同数字的出现次数并求最大频次。题目要求的是这些最高频数字在原数组中的元素总数，而不是最高频数字的种类数，所以要把所有等于最大频次的计数相加。

## 复杂度

设数组长度为 `n`。时间复杂度为 `O(n)`，额外空间复杂度为 `O(n)`。

## 边界条件与本地验证

当所有数字只出现一次时，每个元素都达到最大频次，答案为数组长度；多个数字并列最高频时需要全部累加。使用题目样例和这两类边界输入验证。
