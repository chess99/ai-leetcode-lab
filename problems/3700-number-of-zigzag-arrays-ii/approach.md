# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
## 思路
将“末值、上一步方向”作为 `2*(r-l+1)` 维状态，构造固定转移矩阵并快速幂处理极大的长度。
## 复杂度
朴素矩阵乘法为 `O(V^3 log n)`，`V=2(r-l+1)`；在 Python 对最大值域性能风险较高。
## 边界条件与本地验证
长度一单独返回值域大小；题面两样例通过。
