# 解题记录
- AI 客户端：Codex Desktop
- 模型：gpt-5.6-terra
- 推理档位：medium
- Profile：terra-medium
- 轮次：1

## 思路
求每种颜色最终覆盖的最小矩形。矩形中出现另一颜色说明前者必须先打印，建立颜色依赖图；存在拓扑序即可以按序打印。

## 复杂度
颜色最多 60，扫描各矩形时间 `O(Cmn)`，图空间 `O(C²)`。

## 边界条件与本地验证
同色不产生自依赖；依赖图有环即不可打印；单色网格总可行。
