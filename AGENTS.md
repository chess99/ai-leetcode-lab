# AI 刷题实验协议

本文件是仓库内所有 AI 客户端、模型和自动化代理的最高优先级项目规范。目标是独立、可审计、有限反馈地完成 LeetCode 全量题目，并保留足够数据来判断“算法面试对 AI 到底还有多少区分度”。

## 1. 实验边界

1. 只在本仓库工作。禁止读取 `D:\code\LeetCode` 中的旧解答来帮助做题。
2. 不搜索或读取官方题解、讨论区、他人代码、答案仓库、搜索引擎答案以及模型记忆外的解题提示。
3. 本地归档只作为题面来源；归档中不得加入题解或讨论内容。
4. 尚未收到“开始刷题”的明确指令时，只能建设、验证和维护环境，不得开始解题或正式提交。
5. 不得声称通过，除非本仓库 CLI 记录到 LeetCode 判题结果 `Accepted`。

## 2. 开始前必做

每次接手任务先执行：

```powershell
.\ai-lc.ps1 doctor
.\ai-lc.ps1 status
```

然后检查 `.ai/identity.env`。其中必须是当前客户端和当前模型的真实名称；若无法精确获知模型小版本，写可确认的上位名称，不得猜测。身份会进入代码注释和实验记录。

## 3. 选题与文件

1. 用 `.\ai-lc.ps1 next` 选择下一题，除非用户指定题目。
2. 用 `.\ai-lc.ps1 start <题号或 slug>` 创建工作目录，禁止手工复制旧答案。
3. 默认语言是 Python 3；可根据题目改用其他 LeetCode 支持的语言：`.\ai-lc.ps1 start <slug> --language <langSlug>`。
4. 只修改该题目录、追加式实验日志和自动生成的统计。不要顺手重构无关文件。
5. 保留 CLI 生成的署名注释；切换模型继续作答时，在 `approach.md` 写明交接，不得冒用原作者身份。

## 4. 单题流程

严格按以下顺序：

1. 阅读 `problem.md` 与 `meta.json`，在 `approach.md` 写清算法、复杂度和边界条件。
2. 完成 `solution.*`。先做语法检查和可行的本地自测；本地修改次数不限，但不得获得隐藏测试反馈。
3. 远程试跑只能使用 `.\ai-lc.ps1 test <slug>`，每轮最多 5 次。
4. 正式提交只能使用 `.\ai-lc.ps1 submit <slug>`，每轮最多 3 次。
5. Accepted 后立即运行 `.\ai-lc.ps1 stats`，检查记录，并用 `scripts/commit.ps1` 只提交本题和本次生成的日志/统计。
6. 三次提交均失败后，本轮结束。若还未用过第二轮，可用 `retry` 写明新的思路后开启；禁止只是改写同一份代码来规避预算。
7. 两轮仍未通过时停止，状态记为 `review_required`，留给以后人工复盘；不得继续直接请求接口。

## 5. 预算语义

- 每题每轮：最多 5 次远程试跑、3 次正式提交。
- 每题最多 2 轮，因此上限是 10 次试跑、6 次提交。
- 请求已发出但进程中断，保守计入预算；明确的认证失败、网络失败或平台异常由 CLI 标记为基础设施错误，不计入实验尝试。
- 不得编辑或删除 `data/attempts.jsonl` 来恢复次数，不得手工修改轮次。
- 不得使用浏览器、VS Code 插件、临时脚本或直接 HTTP 请求绕过 CLI。

## 6. 统计完整性

`data/attempts.jsonl` 是只追加的事实日志。每个远程动作记录：

- 题号与 slug
- AI 客户端与模型
- 语言、轮次、动作序号
- UTC 时间与代码 SHA-256
- 判题状态、通过用例数、运行时间、内存或错误类别

禁止删除、改写、重排历史事件。统计文件可由 `.\ai-lc.ps1 stats` 重建，不应手工粉饰。

## 7. Git 规范

1. 直接在当前分支工作，除非用户明确要求切分支。
2. 每个完整迭代完成后主动提交；每道 Accepted 题通常独立一个提交。
3. 使用 `scripts/commit.ps1 -Message "solve: <id> <slug>" -Paths <明确文件列表>`，只暂存自己本轮修改的文件。
4. 提交前必须查看 `git diff --staged`，确认没有密钥、其他代理或用户的并行修改。
5. 禁止提交 `.secrets/`、`.ai/identity.env`、`.runtime/`，禁止跳过 pre-commit hook。
6. 未经用户明确要求，不创建远程仓库、不推送、不公开题库归档或账号数据。

## 8. 异常处理

- `doctor` 报认证失效：停止远程操作，只报告需要更新本地凭证。
- 接口或题面结构变化：修复环境并测试，不把平台故障算成解题失败。
- 付费题无权读取完整题面：保留目录元数据并标为 `locked`，不要从第三方补题面。
- 并发运行冲突：尊重 `.runtime/remote-action.lock`；不要删除仍有效的锁。
- 任何记录与网页状态不一致：以可追溯的判题响应为准，保留原始错误摘要。
