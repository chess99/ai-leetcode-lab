# 复制到 identity.env；每个 AI 客户端应写入自己的真实客户端名称。
AI_CLIENT_NAME=your-ai-client
# 推荐从 config/profiles.json 选择；CLI 的 --profile 会覆盖此默认值。
AI_PROFILE_ID=sol-medium
# 仅兼容尚未采用 Profile 的外部客户端：
# AI_MODEL_NAME=your-model
# AI_REASONING_EFFORT=medium
